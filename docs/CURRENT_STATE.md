### 2026-07-28 — three screens, three holds, and where the residual actually lives

**Nothing adopted. Anchor `sc69a` unchanged.** Twenty-one commits, all pushed;
`audit_rules` clean, `run.py --cli` exits 0, production digest
`4aab205fbb99635db7c607db` unchanged. Read the 2026-07-27 entry below first — the
metric finding it records is the frame for everything here.

#### The meta-result: the shelf is mostly correct judgement

Going in, the working hypothesis was that this project's forty default-off gates
held under-appreciated faithful work, blocked by a metric that cannot tell
fidelity from convergence toward the mean. **The metric problem is real. The
hypothesis about the shelf is disconfirmed, three for three.**

| gate | screen | verdict |
|---|---|---|
| `SWEG_OBJ_HOME` | full matrix N=80 | HOLD — wave 185's blocker confirmed and *larger* |
| `SWEG_EC_RANGED_HOLD` | scoped N=80 | DO NOT ADOPT — overshoots +24.4 on a faction needing +13.6 |
| `SWEG_SECONDARY_PURSUIT` | full matrix N=80 | HOLD — both largest poles worsen, spread widens |

Each was faithful, each was genuinely shelved on incomplete evidence, and each
now carries a decision-grade verdict where it previously carried a pending note.
The new ordering diagnostics overturned **none** of them. Before spending more
time re-litigating held gates, weigh that record.

#### What the screens produced beyond their verdicts

- **`SWEG_OBJ_HOME` gave the dispersion mechanism its first test.** Spread ratio
  fell 1.75 → 1.60, confirming a prediction recorded in
  `METRIC_SKILL_AND_DISPERSION.md` *before* the run: give both sides a
  guaranteed objective and primary stops being winner-take-all. The map geometry
  **is** a dispersion source; it just cannot be fixed alone, because the
  artificial intelligence never contests an enemy home objective (queue debt
  NE-17).
- **`SWEG_EC_RANGED_HOLD` established that a held gate's ON behaviour has a shelf
  life.** Its byte-identical-off proof from July still holds — off is off. But
  the catalogue drifted underneath it: the comment states the Defiler is *not*
  held; it now is, at 12.01 damage over 48 inches, and is the likeliest cause of
  the overshoot. **Any default-off gate whose evidence predates the last
  catalogue regeneration needs its membership re-checked.**
- **`SWEG_SECONDARY_PURSUIT` demonstrated why bundles are a bad screening unit.**
  Recorded before the run: "a bad result will not say which part did the damage."
  It is bad in a specific way — Death Guard +11.1 → +14.8, Astra Militarum −8.8 →
  −11.3 — and cannot be attributed to any of its three parts.

#### The live chain: secondary scoring

Each link measured, and it is the most complete diagnosis this session produced.

1. **The simulator scores half the secondary it should**, hidden by compensating
   primary over-scoring so the total looks healthy: primary 37.7 / secondary
   11.8 / total 49.5, against a real 29.1 / **22.7** / 51.8 (Goonhammer Pariah
   Nexus, ~7,600 games). Secondary share 23.8 percent against a real 43.8.
2. **The shortfall is per-card zero-scoring, not missing cards.** All 19 cards
   are drawn; eight score zero on more than 75 percent of evaluations.
3. **Extend Battle Lines is not broken** — it is a conjunction, and its two
   halves multiply out exactly (0.20 × 0.82 ≈ 0.16 measured).
4. **Root cause: map geometry.** Only 4 of 12 rotation maps place any objective
   in a deployment zone; the other 8 put all five in No Man's Land. Real Pariah
   Nexus layouts give each player a home objective — the same Goonhammer article
   refers to "the objective in your deployment zone" and names three primary
   missions that score for holding it.

#### Aeldari and T'au are isolated to mechanics BY MEASUREMENT

Aeldari is the largest ordering failure in the game (simulated rank 6 of 22, real
rank 22). **Two structurally different real tenth-edition lists — a 2025 Phoenix
Lord spine and a 2026 Wraithlord/Fire Prism build — both produce 52-55 percent
against a real 41.5.** The list layer is ruled out by substitution, not
assumption. Its army rules are measurably inert: Strands of Fate is pool-bounded
at six dice rolled once, Battle Focus has almost nothing to spend on, five of six
Agile Manoeuvres are unmodelled. T'au is the mirror case — the healthiest
template in the audit and still eleven places mis-ranked.

#### Corrections made this session, recorded so they are not re-derived

- **A Battle Focus carry-over defect I reported does not exist.**
  `SWEG_AELDARI_BF_DISCARD` is default-**ON**; production discards tokens
  correctly. A stale comment saying "default-off" caused a probe to label its
  arms backwards. Both that and `SWEG_AELDARI_FATE_FAITHFUL`'s docstring are
  fixed.
- **The list-population proposal is withdrawn.** One list per faction is
  *correct* under the project's goal, and averaging over several would have
  driven the headline down while adding no fidelity — measured: optimal spread
  rescaling reaches 3.31 against the constant null's 3.36, a dead heat.
- **"The artificial intelligence abandons its home objective" is withdrawn.** It
  holds one about 70 percent of the time when one exists; the maps usually
  provide none.
- **The primary-profile blind spot is negligible** — 2 fielded units of 284, one
  in a default-on faction. I called it urgent; it is not.
- **Catalogue profile ≠ fielded profile, for 53 percent of units.** A probe
  reasoning about runtime must build armies; `UNIT_CATALOG` answers data
  questions only. This produced two wrong answers before it was measured.

#### Tooling added

`scripts/gate_inventory.py` reads every gate's real default from code — 147
gates, 107 on, 40 off, and **19 whose surrounding comment contradicts the code**,
all from adoption without a comment sweep. Its own blind spot is documented: it
would not have caught the drift that motivated it.

#### Open, in rough priority order

1. **Emperor's Children needs a tenth-edition list.** `SWEG_EC_LIST3` is
   condemned — its source event was played under eleventh edition (launched
   20 June 2026). The faction is the number two ordering target with no usable
   sourced list.
2. **Aeldari mechanics** — the input layer is ruled out and the army rules are
   inert, so the eleven-point over-rating is elsewhere. Unexplored.
3. **The map-geometry cap** needs a home-contesting artificial intelligence
   before `SWEG_OBJ_HOME` can be revisited.
4. `SWEG_CULL_PICK_AWARE` remains built, cited, byte-identical and unscreened.

### 2026-07-27 — the headline metric was measuring the wrong thing

**Nothing committed. No simulator behaviour changed.** The only code change is
additive reporting in `scripts/evaluate_vs_meta.py` plus one corrected docstring
in `code/archetypes.py`. `audit_rules` clean, `run.py --cli` exits 0, anchor
`sc69a` untouched.

#### The finding

Scored in the evaluation's own frame — army-A, field-weighted, through the same
noise gate — the simulator reads gated **2.85** with 7 of 22 factions in band.
Answering a constant 48.8 percent for every faction reads gated **0.66** with 11
in band. Skill score **−3.33**. The reconstruction reproduces the recorded anchor
2.85 exactly, and the result survives the symmetrized frame.

**This is not automatically damning, and the owner's framing is why.** The
simulator's job is to field the best list available and pilot it as a tournament
player would. The Warp Friends aggregate averages over hundreds of lists and
every level of player skill. A simulator doing its job correctly *should* be more
dispersed than that target — it currently is, by 1.75 times. So losing to a
constant on DISTANCE is expected.

**What survives the reframing is the ORDERING.** Spearman rank correlation is
**+0.28**. A simulator answering the optimal-play question correctly would be
more spread out than reality while ranking the factions in roughly the same
order. This one does not. That is the honest indictment.

Consequence for the project plan: `CLAUDE.md` gates the Stage 2 handoff on mean
absolute error ≤ 2.0, and a constant scores 0.66. **The stated criterion for
declaring Stage 1 converged is already met by a model containing no simulation.**
Changing it is an owner decision and has NOT been made.

#### Built: ordering diagnostics (task #37)

`evaluate_vs_meta` now prints an ORDERING DIAGNOSTICS block and persists it under
a `diagnostics` key in the snapshot: Spearman (the new headline) and Pearson,
spread ratio, skill against the constant null, and sampling-corrected matchup
spread. Purely additive — `report` is untouched, so every historical figure still
means what it said. A `--factions` run refuses to print rather than correlate
across factions that never played. Verified by `scripts/_verify_diagnostics.py`
against a separate code path; all five values agree.

#### The queue was overturned (task #38)

Ranking by ordering gain disagrees with ranking by residual — only two of five
overlap. **Leagues of Votann has the largest gated error in the game (10.35) and
an ordering gain of −0.003**; Astra Militarum scores −0.010. Both were top
priorities under the old headline and are worth nothing under the new one.
Adeptus Mechanicus reads gated 0.61, near "solved", while being 12 places
mis-ranked.

| target | sim rank | real rank | gain | audit verdict |
|---|---|---|---|---|
| Aeldari | 6 | 22 | +0.145 | list from the **wrong era** (2025 sources) |
| Emperor's Children | 20 | 5 | +0.130 | real list, **cost-infeasible**, never realised |
| Adeptus Mechanicus | 9 | 21 | +0.098 | **no list at all** — a codex inventory |
| T'au Empire | 13 | 2 | +0.090 | **list layer healthy** — mechanics defect |
| Chaos Daemons | 17 | 6 | +0.076 | not audited |

#### Five global hypotheses tested, five dead

Durability per point (−0.16/−0.23/+0.09), list population (withdrawn — one list
per faction is *correct* under the goal framing), melee piloting competence
(+0.004, stable across three yardsticks), template realization (−0.05), army
breadth (−0.29, sign backwards). **The residual is not a property of what kind of
army a faction fields.** That is what makes the per-faction list defects the live
lead rather than a convenient one, and it vindicates the faction-at-a-time
method — only its scoring was broken.

#### The structural finding (task #40)

`SEED_FRACTION` is 0.3, so a template may seed at most 600 points of a 2,000
point army. **All 22 templates overrun that slice**, from 1.17 times (Adeptus
Astartes) to 6.51 (Imperial Knights). No faction's template is ever seeded whole;
roughly seventy percent of every army is `_random_fill`, which draws from the
same pool and multiplies cheap declared entries — Aeldari Rangers are declared
once and field 3.2 squads. This damps list work but does not nullify it (Tyranids
still gained +9.49). **Do not propose raising `SEED_FRACTION`** — a settled
negative: iter16 tried 0.5 and 0.75 and both regressed, and `SWEG_TEMPLATE_REALIZE`
was rejected at −2.78/−3.74.

#### ⚠ EDITION BOUNDARY — 20 June 2026 (verified 2026-07-27)

**This project simulates TENTH edition against a May 2026 target.** Eleventh
edition launched **20 June 2026** (Armageddon launch box; core rules 27 June);
tenth edition ran June 2023 to June 2026. **Any tournament played after roughly
20 June 2026 is eleventh edition and is disqualified as a source**, however
recent and well-reported it looks. Today's date is not a guide — searching now
returns mostly eleventh edition results.

Check the **event** date, never the article or lookup date. That conflation has
already happened once in this repository (see below).

Cleared: the Warp Friends May 2026 target; the Aeldari Oklahoma City list
(9 May 2026, and its source article is titled "Competitive Innovations in 10th:
Wind Down"); the Leagues of Votann sourced list (its block records "Tenth
edition, before the edition change"); every archetype citation dated 2025 or
2026-01 through 2026-05.

**Not cleared: `SWEG_EC_LIST3`.** Its source (Kyle Sams, Battle For The Capital
Grand Tournament 2026) published results on **2 July 2026**, which puts the event
most probably in late June — after the change. The gate's comment records a
"live source lookup" on 2026-07-08, but that is the lookup date, not the event
date. See task #39.

#### Highest-value action available

**Confirm the Battle For The Capital Grand Tournament 2026 event date**, because
it gates everything else. If it was played before 20 June 2026, `SWEG_EC_LIST3`
(task #39) becomes the highest-value screen available: cost-verified at 1,865
points, measurably more faithful than the cost-infeasible union it replaces, held
on a stale verdict measured when the faction's residual was 2.6, and on the
winning side of the pattern that every successful list intervention was a
**sourced re-derivation** (Orks +5.03, Tyranids +9.49) while the one failure was
an **arbitrary trim** (`SWEG_AM_2K`, −2.34). Twelve minutes scoped.

If the event was played after 20 June 2026, the gate is built on an **eleventh
edition** list and must be rebuilt from a tenth edition source before it is
screened at all.

Both paths are blocked on the owner: the date needs a Best Coast Pairings lookup
(automated fetch is refused), and the screen needs evaluation authorisation.

Aeldari and Adeptus Mechanicus additionally need a real May 2026 list; Best Coast
Pairings and listhammer.info both refuse automated fetch.

Full account: `docs/METRIC_SKILL_AND_DISPERSION.md`. Tasks #35 to #42.

### 2026-07-26 (later) — the Tyranid under-pole is closed, and it was the input

**New anchor `sc69a` running.** Production now has `SWEG_TYRANID_LIST_SOURCED`
default-ON. Canonical narrow digest unchanged at `4aab205fbb99635db7c607db` (it
cannot see Tyranid changes — see below); wide digest `f243047dbb6a7f45d64aae66`,
kill-switch `91a2c9d431b0f8e85d1712e1`. Nothing committed.

**Result.** Tyranids +9.49 A-frame (35.9 to 45.4 against a real 47.4) and 31.0 to
44.6 both-sides (against 47.0). Faction error 15.9 to 2.4. Overall gated mean
absolute error 3.21 to 2.85. The number-one residual was an input defect: the
archetype did not field the list it cites. No rule changed.

**Byte-identity coverage was much narrower than assumed.** `_detcheck` covers six
factions of twenty-two and never plays Tyranids, so it could not see this gate at
all and every byte-identical claim about it was vacuous. `_detcheck_wide.py` now
covers all twenty-two. Any faction-scoped gate outside Death Guard, Astra
Militarum, Aeldari, Adeptus Astartes, Orks and T'au Empire has never actually been
verified.

**`SWEG_FALLBACK_CAPABILITY` screened +0.92 worse, but on a bug of mine** — HEAVY
was made unconditionally eligible, stripping the melee-heavy protection, and
Imperial Knights (−9.03) and Chaos Knights (−6.33) duly collapsed. Guard is now
universal; needs a fair re-screen against `sc69a`. Astra Militarum +4.30 and Death
Guard −4.97 moved toward real even under the bug.

**Next.** Re-screen the fixed fall-back gate and `SWEG_CULL_PICK_AWARE` against
`sc69a`. Re-screen `SWEG_MELEE_CHARGE_HOLD` too — it was judged against the wrong
Tyranid army. Then the same source audit for Death Guard (+12.2, no source cited),
Adeptus Astartes (+8.9, declares only 700 of 2000 points) and Leagues of Votann
(+13.0), per task #32.

### 2026-07-26 — the input was wrong, not just the mechanics

**Standing anchor unchanged: `sc68a`, gated mean absolute error 3.21, raw
both-sides 6.31, production digest `4aab205fbb99635db7c607db`. Nothing has been
committed. Four gates are built and default-off; three are unscreened.**

**The headline.** The Tyranid archetype does not field the list it cites. Its own
comment block names the GW Open Maastricht 2026 winning Subterranean Assault army;
the builder produces something sharing three entries with it, omitting the Trygon
that the detachment is named for along with all three Tyranid Primes, Old One Eye,
the Maleceptor, every Ravener, the Carnifex and both Lictors, and substituting
about 47 Termagants and 9 Ripper Swarms that the real list does not contain. All
three failing kill cards follow from this, and so does the otherwise-puzzling
result that doubling Tyranid charge connection moved their win rate +0.34. Built
as `SWEG_TYRANID_LIST_SOURCED`; see `docs/TYRANID_LIST_FIDELITY.md`. **Unscreened
— every Tyranid mechanics conclusion in this session rests on a wrong input,
including this session's own.**

**Screened and decided.** `SWEG_MELEE_CHARGE_HOLD`: gated 3.21 to 3.63, +0.42
worse, fifteen decisive movers. A redistribution, not a regression — Adeptus
Astartes −4.36, Aeldari −3.65 and Chaos Space Marines −5.09 move toward real while
Genestealer Cults +12.66, World Eaters +9.59 and Drukhari +8.25 move away. Held
default-off. Its blast radius had been recorded as 12 units; the true figure is
132 melee-only units of 1385, and the 12 belongs to `SWEG_MELEE_ONLY_ENGAGE`.
Fifteen decisive movers is what exposed it.

**Built, measured, awaiting a screen.**

- `SWEG_FALLBACK_CAPABILITY` — Fall Back eligibility asked a capability question
  of a `roles.classify` body-class label, so a gun-carrying unit labelled SUPPORT
  or HORDE could never break off and shoot. The label excludes 55.0 percent of
  pinned ranged-primary activations (SUPPORT 30.8, HORDE 15.2, DUAL 9.0); the
  melee-primary test excludes 0.0. Gate on: pinned gunlines that break off rise
  35.1 to 87.2 percent, and units pinned three or more activations fall 2.9 to
  0.5. The Leagues of Votann DUAL special-case at the same site was a symptom of
  the same under-inclusion. Full-matrix arm running.
- `SWEG_CULL_PICK_AWARE` — the Fixed-pair picker is composition-aware for Bring It
  Down and Assassination but takes Cull the Horde as the fallback for both slots
  without checking the enemy can concede it. Across all 1386 ordered faction
  pairs, 231 of 294 Cull picks (78.6 percent) face a roster with zero qualifying
  thirteen-model squads. Gate on drives that to zero.
- `SWEG_TYRANID_LIST_SOURCED` — above.

**Three retractions.** The charge-hold blast radius (12 → 132, above). The
pinned-gunline attribution: `strategy._is_melee_class` was reported as causing
40.8 percent of Fall Back exclusions; the branch short-circuits on the role test
first and the true figure is 0.0 percent — a gate that only swapped that estimator
was built, measured inert, and replaced. And the archetype sweep's "31 declared
but never built across 21 factions", which named Emperor's Children as the next
big candidate: **withdrawn**, because seven templates declare more than a
2000-point army and are menus the builder samples rather than lists it must field
(Imperial Knights 3380 points, Emperor's Children 2625, Grey Knights 2405 and four
others), accounting for 22 of the 31 hits. The genuine residue is nine entries in
templates that do fit — Killa Kans, Celestian Insidiants, Allarus Custodians,
Typhus, Pteraxii Skystalkers, Foul Blightspawn, Acolyte Hybrids with Hand Flamers,
Serberys Sulphurhounds — and the price hypothesis for those was tested and only
partly supported (never-built median 125 against built 95).

**The standing lesson from all three.** Each was a number measured for one thing
and reused for another: one gate's blast radius quoted for a different gate, an
exclusion counted before the test that actually excluded it, a menu's absence read
as a list's absence. Before quoting a measurement, check that what it measured is
what is being claimed.

**Next.** Screen the sourced Tyranid list scoped to Tyranids (42 of 462 cells,
about 12 minutes) as soon as the lock frees — it is the highest-value outstanding
question and it gates re-reading everything else about Tyranids. Then the Cull
pick arm. Then audit the remaining twenty templates against their cited sources,
which is where the Tyranid defect actually lived and which no mechanical sweep can
find.

# SwegHammer calibration — current state

> **⭐⭐ STANDING ANCHOR IS NOW `data/_anchor_sc68a_n80_log.json` (2026-07-26, owner ruling "implement it") — full faithful adoption stands; gated mean absolute error 3.21, raw 6.31; production digest `4aab205fbb99635db7c607db`; all seventeen kill-switches together restore `db13417fb7e3b2d47cef9867`.**
>
> **THE TRADE, ACCEPTED KNOWINGLY.** Adoption is NOT the best-metric configuration — the Astra Militarum package alone reads gated 2.51 / raw 5.46, and `cand1` (that package plus the four Astra Militarum rules and the Battle-shock aura fix) reads 2.58 / 5.56. Full adoption costs +0.70 gated against the best. It is adopted because every gate in it is faithful, on the `SWEG_TERRAIN_DENSE` precedent (adopted at +0.46 on the same argument), and because it is the ONLY configuration that improves BOTH historic poles at once: **Astra Militarum error 11.5 → 7.5** and **Death Guard 14.2 → 12.2**.
>
> **THE NEW NUMBER ONE RESIDUAL IS TYRANIDS: 31.0 against a real 47.0, error 15.9** — it has overtaken Death Guard (12.2) and Aeldari (13.0). It was 34.5 at `sc67a`, so this session's adoption cost it 3.4 points.
>
> **ITS CAUSE IS NOT YET IDENTIFIED — an earlier claim that Fire Overwatch explains it is RETRACTED.** The direct test (Tyranids-scoped arm, `SWEG_OVERWATCH_MOVE=0` against `sc68a`, `data/_scr_tyranids_ow_log.json`) gives **35.9 → 34.7, paired delta −1.19 with a 3.09 interval: NOT decisive, and the wrong sign.** The earlier inference came from comparing `cand1` to `cand2`, which also differed in whether `SWEG_RANGE_BASEEDGE` was on, so it could not attribute cleanly. Death Guard's gain from Fire Overwatch IS confirmed (`cand1` 14.5 → `cand2` 12.4); the Tyranids collapse is a separate, open question.
>
> **STARTING POINTS FOR THE TYRANIDS INVESTIGATION.** Tyranids was ALREADY the deepest under-pole before this session (34.5 vs real 47.0, error 12.5) — the adoption deepened an existing hole rather than digging a new one. It is the largest-model-count army measured (113 models), it is melee-and-advance shaped, and the same one-`Unit`-per-model representation that produced two defects for Astra Militarum applies to it with more force. The Astra Militarum method transfers directly: instrument what the units actually DO (`scripts/_am_unit_audit.py`, `_am_lockout_probe.py`, `_am_intent_probe.py` are all faction-parameterisable), find the mechanism, then look for the misapplied or unwired rule behind it.


> **⭐⭐ THE FAITHFUL-ADOPTION WAVE (2026-07-25) — the Astra Militarum under-pole is SOLVED and DIAGNOSED (error 11.5 -> 7.5, its best ever), Death Guard moved for the first time in the campaign (14.2 -> 12.2), and adopting every faithful mechanic costs the frame — a REDISTRIBUTION, not a regression.**
>
> **THE ASTRA MILITARUM ANSWER.** The registered-but-never-run order-coverage question was measured: the army issues **2.93 Orders per round against a legal ceiling of 8.77 (33 percent)**, and the dispatcher was never the bottleneck. Four defects, each found by asking a question of the WHOLE codebase rather than of the faction:
> (1) the 6-inch Voice of Command aura was measured **centre-to-centre** while the 10e core measuring rule — cited in this repository and already applied to Engagement Range and objective control — is base-edge; a foot Officer reaching a Leman Russ got 6.00 inches instead of the legal 9.00, **44 percent of the legal area**;
> (2) the **entire battleline is piloted as sacrificial chaff** — every Astra Militarum infantry datasheet is under the 15-points-per-model bar, and AI-9's only safety gate declines once a friendly has ALREADY ARRIVED in the enemy deployment zone, so while the first unit walks (four-plus rounds) the whole army marches, Advances, forfeits its Shooting phase and dies. Cadian Shock Troops dealt **0.4 wounds a game**; Death Korps of Krieg dealt damage on **zero of 254 activations**;
> (3) the advance-suppression guard's 0.5 expected-damage floor was applied **per model** though the simulator stores one `Unit` per model, so **no multi-model squad in the catalogue could ever pass it**;
> (4) four rules the army OWNS and could not use were switched off — Cadia Stands!, Duty and Honour!, Lord Castellan, and the Reinforcements! stratagem.
> **`SWEG_ORDER_AURA_BASEEDGE` alone is +3.33 — 87 percent of the whole result from one comparison operator.** Four piloting heuristics built the same night were worth nothing; see `docs/AM_ORDER_COVERAGE_FINDINGS.md` and `docs/AM_INFANTRY_NEVER_FIRES.md`.
>
> **THE GENERALISED METHOD (`docs/MEASUREMENT_DEFECT_AUDIT.md`).** Ask the codebase a question and let the answer name a CLASS. *What rule does it state but not apply everywhere?* -> the measurement class, four verdicts, and the rule that **impact tracks FREQUENCY OF CONSEQUENCE, not distortion size** (Contagion and the Order aura are identically distorted — both keep 44 percent of legal area — and are worth 0.00 and +3.33). *What does it implement but leave switched off?* -> eleven mechanics enabled; of eighteen default-off rules gates across all twenty-two factions, **seven were Astra Militarum**, the most of any faction.
>
> **THE NUMBERS.** Raw mean absolute error (both-sides, `scripts/_config_compare2.py`): four-gate **5.46**, sc67a 5.48, weapon-range-off 5.93, sc68a (all gates) 6.31. Gated: four-gate **2.51**, sc67a 2.76, 3.13, 3.21. **The two metrics disagree — gated subtracts noise bands, so weapon range costs 0.08 gated but 0.38 raw. Always quote both.** sc68a is the best configuration yet for BOTH biggest poles (Death Guard 12.2, Astra Militarum 7.5) while six mid-table factions absorb the cost: Votann +4.1, Astartes +3.2, Chaos Knights +2.9, Necrons +2.8, Sororitas +2.7, Tyranids +2.3.
>
> **THE STRUCTURAL FINDING, with numbers on both sides: individually faithful mechanics can make calibration WORSE by inflating factions already over-performing.** Weapon range measured base-edge (+5.5 on Adeptus Astartes alone) and Kindred Hero's [LETHAL HITS] grant (Votann, already +8.4 over). Neither is a defect in the fix — both say those factions over-perform for a reason the faithful mechanic amplifies, the sharpened diagnosis fidelity-first predicts (`SWEG_TERRAIN_DENSE` precedent, adopted at +0.46).
>
> **CAVEATS THAT COST REAL TIME — read before trusting any sweep.** FOUR instruments produced confident wrong answers this session, each caught only by checking the CODE: a gate sweep that inferred defaults from the comparison direction reported Fire Overwatch as disabled (it is on; the line is a guard clause); the chaff cap counted `Unit` instances (one per MODEL) where Behind Enemy Lines scores per UNIT; a confession sweep reported the Aeldari Fate-dice and Battle-Focus over-counts as live when both were adopted on 2026-07-08 (**citations keep their pre-fix narrative — the "ADOPTED" note is in the code comment**); and a fabrication sweep scanning effect prose returned 126 hits with the top six all false. **Also: three of the seventeen "adopted" gates are INERT** (identical digest on and off) — `SWEG_CHALLENGER_GAP_CAPPED` is only a sub-option of `SWEG_CHALLENGER_CARDS`, which is default-off by a deliberate 2026-07-04 reversion. **Check a gate's PARENT and path reachability before adopting it.**
>
> STATE: fourteen gates effective; all kill-switches together restore digest `db13417fb7e3b2d47cef9867` exactly; production digest `4aab205fbb99635db7c607db`. New anchor `data/_anchor_sc68a_n80_log.json`. NOTHING COMMITTED (rule 3). OPEN: the candidate configuration (Astra Militarum gates + aura, global redistributors off) is screening; if it recovers the frame it is the recommendation, leaving weapon range as a separate doctrine call with its measured price.


> **⭐⭐ THE ASTRA MILITARUM MEASURING DEFECT (2026-07-25) — the deepest under-pole moves 33.8 -> 37.6 and the frame improves to gated 2.51, and 87 percent of it is ONE range test measured centre-to-centre.** The registered-but-never-run order-coverage question ("does the sim under-issue Orders vs the real officer economy?") was measured: Astra Militarum issues **2.93 Orders per round against a legal ceiling of 8.77 (33 percent)**, and the dispatcher is not the bottleneck. The cause is that `code/orders.py` measured the 6-inch Voice of Command aura centre-to-centre, while the 10e core measuring rule this repository already cites — and already applies to Engagement Range and objective control — is base-edge. A foot Officer to a Leman Russ hull legally reaches 9.00 inches and was reaching 6.00 (44 percent of the legal area). **`SWEG_ORDER_AURA_BASEEDGE` alone: +3.33 DECISIVE, gated 2.76 -> 2.59.** A per-unit audit then found the infantry corps contributing under one wound a game (Death Korps of Krieg: damage on ZERO of 254 activations) because **the entire battleline is piloted as sacrificial chaff** — every Astra Militarum infantry datasheet is under the 15-points-per-model chaff bar, and AI-9's safety gate only declines once a friendly has ALREADY ARRIVED in the enemy deployment zone, so the whole army marches while the first unit walks, Advances (forfeiting non-[ASSAULT] shooting) and dies. Three further gates built default-off, byte-identical-off, cited: `SWEG_CHAFF_COMMIT_CAP` (cap at two CODEX UNITS — the sim's own Behind Enemy Lines table pays nothing for a third), `SWEG_AM_INFANTRY_FIRE`, and `SWEG_SQUAD_DAMAGE_FLOOR` (the advance-suppression guard's 0.5 floor was applied per MODEL though the sim stores one `Unit` per model, so **no multi-model squad could ever pass it**). FULL PACKAGE N=80 paired: **Astra Militarum 33.8 -> 37.6, gated 2.76 -> 2.51**, all collateral inside noise. REJECTED: `SWEG_TARGET_ECONOMICS` re-screen (gated 2.90; the hold stands). INSTRUMENT FIX: probes must apply `SWEG_SIDE_ROLLOFF` or they measure a one-sided frame. CORRECTED: the remaining Astra Militarum gap is EQUAL halves primary (5.6) and secondary (5.3) — the primary half has never been separately investigated. NOTHING COMMITTED (rule 3). Detail: `docs/AM_ORDER_COVERAGE_FINDINGS.md`, `docs/AM_INFANTRY_NEVER_FIRES.md`, DECISION_LEDGER head. **THE GENERALISED FINDING — `docs/MEASUREMENT_DEFECT_AUDIT.md`:** the Order-aura bug is one of a CLASS. The 10e core measuring rule (base edge to base edge), already cited in this repository and already applied to Engagement Range and objective control, was NOT applied to weapon range, the Rapid Fire / Melta half-range trigger, or the Battle-shock auras. The audit inventories every distance comparison in the simulator (2 fixed and screened, 1 fixed and screening, 1 built-unscreened, 4 open, 4 confirmed non-defects). **`SWEG_RANGE_BASEEDGE` (weapon range) is the first FIDELITY-VERSUS-METRIC fork of this campaign: it is unambiguously faithful and it screens gated 2.76 -> 3.26 (+0.50 WORSE), almost entirely by inflating Adeptus Astartes +5.49; Astra Militarum gains nothing.** Precedent for adopting a faithful mechanic at a metric cost is `SWEG_TERRAIN_DENSE` (+0.46, adopted 2026-07-14). OWNER RULING NEEDED. `SWEG_AURA_BASEEDGE` (Synapse, Shadow in the Warp penalty, Contagion, Harbingers) is built, cited and byte-identical off but UNSCREENED; its direction is genuinely mixed.


> **⭐⭐ THE COUNTERPLAY FRONTIER (2026-07-15, owner-directed, from real battle-report tape) — the #1 residual (Death Guard +16) is NOT a bug in durability, list, or the scoring formula; it is that the sim's WORLD lacks the COUNTERPLAY real opponents use, and it under-scores the secondary economy. THIS IS THE NEW FRONTIER: add counterplay to the sim's AI.** Evidence: FIVE real 10e Death Guard battle reports (MiniWarGaming, transcripts via `yt-dlp`) — **real Death Guard went 0-5** (Astra Militarum 55-82, Sisters conceded, Black Templars ~69-96 [11th-ed caveat], Space Wolves [Mortarion shot off], Harlequins [devastating wounds]) while the sim gives DG **63.5%** (real ~47.6%, real peak 59%). Even the pillar lists (Mortarion + Virulent Vectorium) lost, so the archetype list over-stack is minor. Real opponents beat durable armies by playing AROUND them: (1) MOBILITY / outmaneuvering the slow 5" blob (Space Wolves, Harlequins); (2) durability-BYPASS firepower (Harlequins devastating wounds ignore saves; AM multi-melta AP-4); (3) contesting + trading cheap to deny value (Sisters: "your bodies were untouchable, I won on trades"); (4) winning the SECONDARY race. The sim's AI fights durable armies head-on and loses an attrition war it shouldn't fight — every internal lever (canary loop ×9, target-economics, staging, contest-routing) failed because they improved play SYMMETRICALLY (banking to the durable side) instead of adding ANTI-durable counterplay. The tractable, ground-truth-anchored sub-lever is the **secondary economy (11.4 → 22.7 VP/game; kill-cards fire ~1 VP/call vs face 2-5)** — asymmetric toward the aggressive under-poles (AM, T'au) the sim starves. Also worth a fidelity check: devastating-wounds modelling + field AP/melta throughput. OWNER ARCHITECTURE NOTE: the crude posture objective boosts (`strategy.py` ~4105 ×1.3 / ~4132 ×1.25) should be folded into the parked LAYER program (value/threat/job, measured currency), not hardcoded magic numbers. RESEARCH SPREAD dispatched on how to add the counterplay. `SWEG_TARGET_ECONOMICS=1` is a confirmed clean +1.79 AM lift, HELD for banking. Reference: auto-memory `sim-counterplay-frontier`.

> **⭐ THE DENSE-TERRAIN RE-ANCHOR (2026-07-14) — NEW STANDING ANCHOR `data/_anchor_sc67a_n80_log.json` (N=80), gated mean absolute error 2.76 (A-frame) / 2.77 (symmetrized), UP +0.46 from sc66a's 2.30 — and that rise is the honest, fidelity-first result: the faithful terrain raised the goal pole and inflated the durable over-poles, sharpening the diagnosis.** `SWEG_TERRAIN_DENSE` **ADOPTED default-on** (`code/maps.py:108`, `=0` byte-identical kill-switch): the sourced Games Workshop Pariah Nexus competitive layouts (eight large 6–10" line-of-sight-blocking ruins, no clean deployment-zone-to-deployment-zone sightline) REPLACE the homebrew Wave-97 small-scatter field whose clean firing lanes over-credit the static gunline. Adopted **fidelity-first per the owner's doctrine** (it is the real competitive standard, strictly more faithful than the approximation the code otherwise carried), reversing the prior metric-first "hold default-off" call — even though terrain-density had been refuted THREE times *as the gunline/over-pole fix* (the AM-under-pole response was the one sub-question those over-pole screens never isolated). **THE GOAL MOVED: Astra Militarum 30.3 → 33.8 (+3.5), gated 11.82 → 8.29** — the first faithful lift on the deepest under-pole via terrain (blocked line-of-sight relieves the alpha-strike). **Necrons landed dead on target** (49.8 → 53.4 vs real 53.5 — the N=40 "+8 overshoot" was pure noise). THE HONEST COST: the faithful terrain inflates the DURABLE over-poles — **Death Guard 59.8 → 63.5 (now the unambiguous #1 residual, gated 13.78** — the Plagueburst gunline survives more under blocked line-of-sight, the durability-over-reward wall in its clearest form), Aeldari grew; **T'au −4.1** (51.5 → 47.4, a new small residual — its gunline genuinely needs cross-board sightlines). Residual ladder at sc67a (symmetrized gated): **Death Guard 13.78, Astra Militarum 8.65, Aeldari 7.28, Votann 5.90, Emperor's Children 5.44, Chaos Space Marines 5.31, Tyranids 4.74, Grey Knights 3.74, T'au 3.68**; 11/22 inside noise. **SESSION REJECTS (all removed for cleanliness): the AM deployment/cover thread is EXHAUSTED** — `SWEG_ADVANCE_HORIZON` (AM −4.2, destabilised 15 factions — AM advancing ~87% is not a defect), `SWEG_COVER_DEPLOY` (AM −8.45, blind cover-hug pulls the gunline off its firing lanes), `SWEG_DEPLOY_VALUE` (offense-preserving version rescued AM to flat but no lift) — WHERE/HOW AM moves is not the gap; AM's seed play is already good. **NEXT (the productive forward thread): work the dense-terrain residuals — the Death Guard durable over-pole (#1, the durability-over-reward's clearest manifestation) and T'au −4.1 (its gunline vs blocked line-of-sight) — both cleaner targets than AM's structural wall.** Commit/push pending owner go (rule 3). Full detail: DECISION_LEDGER LANDED (terrain adoption) + REVERTED (the three AM deployment rejects).

> **⭐ ASTRA MILITARUM — THE "DURABILITY/ECONOMY QUESTION" IS ANSWERED (2026-07-12, hand-pilot per PILOT_PROTOCOL): it is the ASSASSINATION BLEED via unmodelled 10e Leader attachment, not a durability floor.** Full PILOT_PROTOCOL pass on sc63a: the pole is REAL (23.8 A / 24.2 sym), a broad bleed worst vs the NON-melee armies (Adeptus Astartes 5.0%, Genestealer Cults 7.5%, Chaos Knights 7.5%, Sororitas 8.8%, Imperial Knights 10.0%). In the close losses Astra Militarum ends with EQUAL/MORE points-remaining and loses the VICTORY-POINT race (seed-24 vs Astartes: 1188 vs 1136 points left, lost 21-46) — a conversion problem. Primary is near-even; the loss is SECONDARY, and per-card it is almost entirely ASSASSINATION (Astartes secondary 26.7 = assassination 22.5; Imperial Knights 27.1 = assassination 19.0). The feed is Astra-Militarum-SPECIFIC (Astartes scores 26.9 sec vs AM but 8.0 vs Custodes / 11.9 vs T'au — horde armies feed, elite starve) because Astra Militarum fields ~10 standalone CHARACTERs and the sim does NOT model 10e Leader attachment (`code/secondaries.py:356`). NO piloting call touches it (contest tactics −5pp, holds-off washes, chase_vp +2.5 sub-noise). SIZING PROBE (N=40 paired, suppress Assassination-off-AM = perfect Leader protection, UPPER BOUND): **Astartes 5%→55% (+50pp, 20-0 flips), Imperial Knights 7.5%→40% (+32.5, 13-0)**, Sororitas +5, T'au +5, Genestealer Cults +0 (its crater is the separate primary-blowout disease) — the largest Astra Militarum lever effect in the campaign. CANDIDATE LEVER (unbuilt): model 10e Leader attachment (attached CHARACTER not an eligible Assassination target until its bodyguard dies) — faithful, citable, GLOBAL, disproportionately lifts the most character-dense army, and attacks the known secondary-VP over-production (38.9 vs real 22.7). PENDING (rule 10): verify the Leader/Assassination target-eligibility rule + the Cadian Command Squad CHARACTER keyword + list character-count realism, then an N=80 sc63a screen (the probe is mechanism/upper-bound evidence, not a rate). Full narrative `docs/PILOT_FINDINGS.md` (2026-07-12 section) + `docs/DECISION_LEDGER.md`.

> **⭐ THE CORRECTION WEEKEND (2026-07-12, Fable) — NEW STANDING ANCHOR `data/_anchor_sc63a_n80_log.json` (ships in-repo), gated mean absolute error 2.46 (A-frame) / 2.43 (symmetrized) — the best A-frame in project history, on a more faithful frame.** Three arcs closed. (1) THE CANARY PROGRAM (owner-directed, Astra Militarum vs Death Guard, ten registered iterations): movement piloting was made fully honest and EXONERATED; the shoot-value consumer was built (held-faithful at +0.07, three poles toward real); and the kill-arithmetic hand-audit verified the per-activation numbers faithful end-to-end — the cell's 47-vs-25 primary race is arithmetic, not artificial intelligence, and the durability question is now bracketed by evidence on every side. (2) THE GROUND-TRUTH CORRECTION: "the sim has no line-of-sight blocking" was FALSE since May — the docs were wrong, the code was right (73 percent candidate denial measured); terrain density is faithful; the genuinely-open pieces were built the same day — angle-aware Benefit of Cover in resolution, and line-of-sight consumption in the calibrated threat field (its biggest single calibration step: most over-predictors moved into or toward the band; the residual is the named swarm-concentration structure). (3) THE ADOPTION BATCH, all screened wash-or-better, defaults flipped in one commit with digest-verified kill-switches: `SWEG_STRAT_ONCE_PER_PHASE` (the cited once-per-phase stratagem rule; Command Re-Roll was firing six times a round), `SWEG_WARGEAR_MUTEX` (ten Wahapedia-verified units stop firing mutually-exclusive wargear; 129 more documented for a follow-up lane), `SWEG_COVER_ANGLE` (+ the Woods fix; −0.13 on its own screen). Also found and fixed along the way: the accidental `_screen_target_bonus` mislabelling of Khorne Berzerkers (open question: World Eaters may be calibrated-by-bug), the Rogal Dorn firing 3.1× its legal loadout, and two catalogue hygiene defects. RESIDUAL LADDER at sc63a: Astra Militarum (≈27-29 points under, unchanged — the isolated durability/economy question), Death Guard (+12-13), Aeldari (+5-6), everything else thin — 10-11 of 22 factions inside their noise bands per half. OPEN DECISIONS: the swarm-concentration allocation structure (new registration); the 129-unit wargear follow-up; the World Eaters calibration re-examination; the parked layer stack (threat+value+offense+jobs+deny, all calibrated, awaiting a consumer that survives its screens).

> **⭐ THE ALLOCATION DAY (2026-07-11, Fable, daytime) — STANDING ANCHOR UNCHANGED (`sc62a`, 2.60/2.42); the owner's risk-splitting threat design was built, calibrated in three registered steps, and closed at its stop rule — and the layer program's ORIGINAL falsifier now passes for the first time.** The owner's design rule (an enemy with one eligible target sends everything at it; several eligible targets split the risk) was implemented as attractiveness-proportional allocation plus a measured attack-propensity term, in three registered structures: allocation alone (cures the bulk of the 6-300× saturation; the isolated-unit and two-Berzerker cases price exactly as designed), flat participation (failed on shape exactly as pre-stated), opportunity-conditioned participation (cures shape — Imperial Knights 1.04 dead-centre — but fails the [2/3, 3/2] out-of-sample band one-sidedly: 10/16 in band, six over, zero under, cause isolated to a selection effect on the zero-opportunity bucket). THE BEHAVIOURAL RESULT THAT OUTLIVES THE BAND: on the calibrated field, **walked-into-it falls for every probe faction on both consumers** — threat-charge (0.242→0.220 / 0.196→0.181 / 0.216→0.212) and the job layer (0.242→**0.106** / 0.196→0.167 / 0.216→**0.089**, with units surviving ~25 percent more end-turns) — the first clean pass of the threat proposal's original falsifier in the program's history. THE OWNER'S DECISION: (a) override the screen gate on this behavioural evidence and run the two consumer screens against sc62a (the field's residual over-read is one-sided conservative, and consumers price RELATIVELY); (b) fund a selection-corrected propensity lane (new registration); (c) park the program. All five lane builds sit byte-identical-off on the layer branch (head 435e38e); two permanent instruments (`diag_fire_allocation`, `diag_threat_calibration`) and the pull request 90 sync are already on the mainline.

> **⭐ THE EXCHANGE-RATE NIGHT (2026-07-11, Fable, overnight) — STANDING ANCHOR UNCHANGED (`sc62a`, 2.60/2.42); the layer program ran four registered iterations, parks with FIVE attributed failure mechanisms, and produced two permanent assets: a MEASURED price of violence and a ~1.27× faster simulator.** The headline research result: the wounds-to-victory-points exchange was fitted from 18,480 instrumented games (`scripts/fit_exchange_rate.py`, `data/_margins_sc62a_n40_log.json`) — **measured β = 0.0152 victory points per enemy point destroyed, against an asserted 0.114 at four rounds remaining: the entire artificial-intelligence stack had been over-pricing violence four-to-seven-fold**, with near-universal per-faction slopes (0.015-0.024) and kills explaining only a fifth of scoring variance. The job/commitment layer then failed four registered screens (4.65 → 4.27 → 4.23 → 3.86 paired, each fail isolating one mechanism — see the ledger's five-mechanism program summary), ending on the deepest one: **the threat field SATURATES — it counts every enemy's full output against every cell simultaneously, over-pricing incoming fire by roughly the enemy-count factor**, flattening every survival discount in the stack. THE NAMED NEXT LANE (registered, not run): allocation-aware threat — divide each enemy's projection across its plausible targets — which un-saturates the threat-charge consumer, staging, value contestability, and both job-layer discounts in one fix. Also landed on the rolling branch: the speed pass (~1.27×, byte-identity proven), margin logging (`--log-margins`), the fit tool, and (from the previous evening) the per-turn replay recap and the stratagem once-per-phase rule — both of the co-owner's watch-a-game reports resolved. The layer stack (threat + value + offense + jobs, measured exchange, both speed passes) is parked byte-identical-off at head `919728f`. OWNER DECISIONS OPEN: (1) the five-faction value mask (1.95/1.79, still the only sub-target configuration) — adopt as stopgap or hold; (2) the stratagem once-per-phase flip (adopt-candidate) awaiting the batch re-anchor; (3) fund the allocation-aware threat lane. Astra Militarum remains the pole (gated ~14.6, ~half of all remaining error).

> **⭐ THE SUB-TARGET NIGHT (2026-07-10, Fable, overnight) — STANDING ANCHOR UNCHANGED (`sc62a`, 2.60/2.42) but THE ≤2.0 STAGE-1 TARGET WAS CROSSED ON SCREENS, adoption pending the owner's doctrine fork.** The trade-evaluator lane STOPPED per its pre-stated rule (both reply re-pricings failed the walked-into-it falsifier; the move-consumer reply is structurally the wrong lever for exposure — see the ledger). The pivot: `SWEG_VALUE_MOVE_FACTIONS` (per-faction enable mask on the value field, build fa034ed) was screened across a CLOSED four-cell grid — global / five / three / four factions — and read **1.95/1.79 (five-mask), 1.49/1.47 (three-mask), 1.46/1.36 (four-mask)** vs the anchor's 2.60/2.42, all N=40 frame-checked. THE ROBUST SIGNAL: value-aware movement for the durable trio (Death Guard, Imperial Knights, Chaos Knights) closes their over-poles in EVERY cell (−4 to −6.4 each; Imperial Knights lands INSIDE NOISE for the first time). THE UNSTABLE ARTIFACT: World Eaters' pole is a compensating pair (on-target only in the exact five-mask; breaks +9.5 in the three-mask, −6.9 in the four-mask), and Astra Militarum is untouchable by any mask (gated 13.6–14.6 everywhere; its lift only ever comes from opponents playing victory-point-focused — maximal under global-on, which is also the faithful shape). ONLY the five-mask survives pre-registration (sub-target at 1.95/1.79; known cost: Astra Militarum −1.92 deeper). **THE OWNER FORK: (a) adopt the five-mask stopgap and re-anchor sub-2.0, (b) reject masks and fund a global-field reprice (the principled fix — v1 pricing breaks kill-tempo factions, a pricing not scope defect), or (c) both.** Also tonight: `SWEG_STRAT_ONCE_PER_PHASE` built+screened ADOPT-CANDIDATE (the cited 10e core rule the sim was violating — Command Re-Roll fired up to 6×/round; wash +0.01 with World Eaters +1.61 toward real; flips at the next re-anchor batch); `SWEG_THREAT_CHARGE` screened wash but HELD on its own failed falsifier; both of Ed's watch-a-game reports resolved (the "alternation" was the round-recap bucketing both turns by event type — fixed, per-turn sub-chapters merged at 4285018; the stratagem repeat was real, fixed above). Residual ladder at the four-mask read: **Astra Militarum 13.6 (≈half of ALL remaining error, no mask touches it — its dedicated lever is still missing), Aeldari 5.4, Adeptus Mechanicus 2.9, Death Guard 2.8.** NEXT: the owner's mask/global doctrine call → one batched re-anchor (stratagem flip + whichever value shape wins) → the Astra Militarum lever hunt on the new frame.

> **⭐ THE LAYER-PROOF DAY (2026-07-09, Fable) — NEW STANDING ANCHOR `data/_anchor_sc62a_n80_log.json` (ships in-repo), gated mean absolute error 2.60 (A-frame) / 2.42 (symmetrized).** Three things happened. (1) **The durability wall is empirically a HORIZON ARTIFACT**: the value-field move consumer (`SWEG_VALUE_MOVE`, HELD-foundation on its worktree branch) moved ALL FIVE wall poles decisively toward real simultaneously at N=40 — Astra Militarum +6.58 (the first lever ever to lift it), Death Guard −7.32, Imperial Knights −7.47, Chaos Knights −9.17, World Eaters −5.74 — the both-sides signature no reflex-level lever ever produced; held not adopted because v1 pricing breaks five calibrated factions (the trade evaluator is the designed refinement and THE next build). (2) **The upstream charge-trade veto was REJECTED on its first-ever measurement** (−0.52 gated; the World Eaters pole was entirely its artifact) and flipped default-off. (3) **Terrain collision rejected in both shapes** (dead-clamp AND routing — geometry without planning intelligence is a pure detour tax; reopens on the layer stack). Also shipped: the distributed-screening job-ticket harness (`scripts/screen_job.py`, anchors now ship in-repo, identity-check ticket zero) for two-machine screening with the co-owner, and five real eval-lock bugs fixed (atomic, global across worktrees, recycling-proof, stale-unlink). THE RESIDUAL LADDER: **Astra Militarum 16.7 and Death Guard 13.7 (the horizon pair — now with a PROVEN lever family aimed at them), Imperial Knights 8.6, Aeldari 5.0, Chaos Knights 4.0.** NEXT: build the trade evaluator on the value foundation; regenerate screens against sc62a; Ed's pull request 86 is mergeable and current.


> **⭐ THE PILOT-SWEEP WAVE (2026-07-08, Fable) — NEW STANDING ANCHOR `data/_anchor_sc60a_n80_log.json`, gated mean absolute error 2.61 (A-frame) / 2.42 (symmetrized). THE ORKS POLE IS CLOSED (gated 0.00 both frames).** The owner-ordered full-protocol pilots on all three under-poles each came back "not a floor" and resolved into TEMPLATE INFEASIBILITY: the under-pole templates were cost-infeasible unions (~2,080 to ~2,920 points vs the 2,000 budget), fielding degraded random subsets — the "realization lottery". Both draw-mechanism cures were built and REJECTED empirically (shapes 1+2, held); the template-side re-derivations split: **`SWEG_ORKS_LIST2` ADOPTED default-on (Snikrot + Kommandos + Zodgrod, sourced, 1,920-point feasible; N=80 +5.03 DECISIVE, Orks 37.6 → 42.6 vs real 45.3, cross-cutting Death Guard/World Eaters/Daemons deflation; reproduction 0-flip)**, `SWEG_AM_2K` REJECTED (−2.34 wrong-sign — the Taurox trim cut a load-bearing transport), `SWEG_EC_LIST3` (exact Kyle Sams 1,865-point list) HELD inconclusive inside EC's 5.7 noise floor. Week trajectory: 3.60 → 3.08 → 3.01 → 2.85 → **2.61**. THE HONEST RESIDUAL: **Astra Militarum 16.8 and Death Guard 13.7 — now ~72 percent of all remaining error and the only big poles left** — then Imperial Knights ~7.8, Aeldari ~5.9, Chaos Knights ~3.9. Astra Militarum has now survived EVERY angle (datasheets, orders, lists ×3 shapes, piloting ×5 channels, realization ×3 shapes) — it and Death Guard are the durability-equilibrium pair, the sharpest possible Stage-2 boundary evidence. NEXT: the Stage-2 fork (owner), or the last thin Stage-1 threads (EC seeds extension; Aeldari unexplained +5.9; the held free-fidelity riders fold at the next re-anchor).

> **⭐ THE SIDE-ROLLOFF RE-ANCHOR (2026-07-08, overnight, Fable) — NEW STANDING ANCHOR `data/_anchor_sc59a_n80_log.json`, gated mean absolute error 2.85 (A-frame) / 2.62 (symmetrized) — the frames now agree because the estimator bias is fixed at source.** `SWEG_SIDE_ROLLOFF` ADOPTED default-on (`=0` kill-switch): each game rolls off which faction takes which deployment side (the real-rules mechanism; all five rotation maps verified 180-degree symmetric in objectives AND terrain, so the fixed side assignment was the unfaithful piece), implemented as an eval-harness slot swap with winner re-orientation. Prediction test passed at N=20 (values landed on the symmetrized estimator) and confirmed at N=80: per-faction side bias collapsed from ±5.0 to within ±1.7 for ALL 22 factions; the Chaos Daemons pole is GONE (gated 0.95 A / 0.00 sym — it was measurement artifact). Riders adopted in the same re-anchor (both flat-screened fidelity): `SWEG_AELDARI_FATE_FAITHFUL`, `SWEG_AELDARI_BF_DISCARD`. Committed-default reproduction 0-flip. Week trajectory: 3.60 → 3.08 → 3.01 → **2.85**. THE HONEST RESIDUAL LADDER: **Astra Militarum 17.0 and Death Guard 14.4 (the structural durability pair, ~55 percent of all error), Imperial Knights 8.8, Aeldari 5.3, Orks 4.8, Chaos Knights 4.0, Emperor's Children 2.6** — everything else inside noise. QUEUED against this anchor: the going-second family re-screens (`SWEG_OVERWATCH_MOVE` first — its +0.40 rejection is flagged frame-confounded), the Blitzing Firepower 12-inch gate screen (`SWEG_AELDARI_BLITZ_RANGE`, built default-off), and then the Stage-2 fork on the Astra Militarum / Death Guard durability equilibrium (every fidelity surface — datasheets, lists, orders, scoring, movement/tempo — is now audited faithful for both).

> **⭐ THE PROTOCOL + FRAME WAVE (2026-07-07/08, Fable) — NEW STANDING ANCHOR `data/_anchor_sc58a_n80_log.json`, gated mean absolute error 3.01 (A-frame) / 2.57 (symmetrized).** Three things happened. (1) **The measurement frame was audited for the first time** (`scripts/diag_frame.py`): the metric reads army-A cells only, per-faction A/B positional bias runs to ±5 points, and under the unbiased symmetrized estimator the sc57a MAE was 2.61 not 3.08 — **the entire Chaos Daemons pole was frame artifact (5.19 → 0.23)**, while Astra Militarum (~16) and Aeldari (~6.4) are real and understated; the bias is MAP-CONCENTRATED (deployment-side geometry, e.g. Sororitas/T'au/Imperial Knights +18..+23 as army A on map 4 only), so it is also a fixable Stage-1 fidelity bug (real 40k rolls off for sides) — the going-second lever family's rejections are flagged CONFOUNDED pending a sym-lens re-screen. (2) **EMPEROR'S CHILDREN LIST REALISM 2 ADOPTED (`SWEG_EC_LIST2` default-on, fidelity-first)** — second Daemon Prince (was structurally capped at 1), 3x Infractors, 2x Rhinos, 1x Tormentors, Sorcerer dropped, Lord Exultant seeding fixed; sourced from five tournament lists; screens mildly positive (N=80 +0.76 non-decisive, EC 45.2 → 45.9 toward 53.3), gated 3.08 → 3.01, promotion reproduction check 0-flip. (3) **The Aeldari over-pole SURVIVED its fidelity audit** — the Strands-of-Fate scope fix (real rule: one substitution per phase, army-wide) and Battle Focus token discard both screened flat (the six-die pool was always the binding constraint); both held default-off as free fidelity for the next re-anchor; Aeldari (+6.4 sym) remains open with thin suspects. **New operating layer: `docs/LEVER_PROTOCOL.md` (family pre-flight, measured-vs-estimate, minimum-N, frame caveat), `docs/PROTOCOL_REVIEW_2026-07.md`, and a Windows-hardened serial-eval lock.** NEXT: the positional-geometry root-cause (side roll-off or symmetric layouts — moves A-frame toward sym at source, then re-anchor), the confounded going-second re-screens, and the DG/AM durability axis (the real remaining floor: DG ~14, AM ~16 under the honest estimator).

> **⭐ THE LIST-FIDELITY WAVE (2026-07-04) — NEW STANDING ANCHOR `data/_anchor_sc57a_n80_log.json`, gated mean absolute error 3.08 (best faithful frame). THREE list-composition wins today: Magnus (Thousand Sons), Coterie of the Conceited (Emperor's Children), and the Orks War Horde reshape — the axis the play-quality pilots all assumed faithful, and the biggest source of Stage-1 headroom found in the honest era.** Trajectory: 3.60 → 3.51 (Magnus) → 3.37 (Emperor's Children detachment) → 3.08 (Orks reshape). The Orks fix drops the mis-composed Killa-Kan wall for the real mobile War Horde (Ghazghkull + Trukks/Battlewagon/Stormboyz, sourced from tournament lists) — Orks 31.8 → 36.6, the mobile matchups rising and the durability-floor cells (Death Guard, Thousand Sons) correctly staying flat. **METHOD LESSON (owner-driven, twice): a flat single-lever screen is NOT a diagnosis — the full manual pilot is. It found the Thousand Sons Magnus gap and the Ork mis-composition that "structural" would have buried.** The residual is now genuinely two things: (1) the durability/survivor-uptime floor on the FAITHFUL over-poles (Death Guard +18, Imperial Knights +12, Chaos Knights +10, Aeldari +9 — Stage-2 pricing or a scoring-representation remodel), and (2) any remaining under-built lists worth a manual pilot (Chaos Daemons −8.2 next; Astra Militarum −16.4 is confirmed structural, not list). NEXT: Chaos Daemons pilot/list check; then the Stage-2 fork on the faithful floor.

> **⭐ THE LIST-COMPOSITION REVISION (2026-07-04) — NEW STANDING ANCHOR `data/_anchor_sc55a_n80_log.json`, gated mean absolute error 3.51 (best faithful frame). MAGNUS THE RED ADOPTED (`SWEG_TSONS_MAGNUS` default-on): the Death Guard elite-infantry crater was NOT a durability floor — it was the sim fielding Thousand Sons WITHOUT its best anti-brick tool.** Real 2026 competitive Thousand Sons run Magnus (sourced); the template excluded him on stale 1000-point logic + the epic-hero random_fill bar (0 percent fielded). Fixed → Thousand Sons 46.3 → 53.0 (landing on real 53.9), gated 3.60 → 3.51, and **Death Guard deflates −1.02 toward real** (the cross-cutting win — fixing the opponent's list chips the over-pole from the other side); the Death-Guard-vs-Thousand-Sons cell collapses 90 → 52.5 percent. Bring-It-Down instrumented clean (Magnus survives 60 percent, Death Guard secondary DROPS with him present — not the Canis-Rex liability). **THE REVISION: all four play-quality pilots + the manual hand-pilot concluded "durability floor" — but they were ALL on the Magnus-less list. Death Guard's +19 over-pole (45 percent of the gated error) is partly a LIST-COMPOSITION artifact of under-built OPPONENTS, not pure floor.** EMPEROR'S CHILDREN Coterie of the Conceited detachment ADOPTED (`SWEG_EC_DETACHMENT` default-on) — the sim ran NO detachment; the real one (sourced, 7-0 tournament win) moves Emperor's Children 42.0 → 45.3 toward real 53.3, gated 3.51 → 3.37 (NEW ANCHOR sc56a), Death Guard flat. Closes a third of the −11.3; the rest is Emperor's Children's wider list. **TWO list-composition wins banked today (Magnus, Emperor's Children detachment); the list-fidelity axis the play-quality pilots assumed faithful is the reopened Stage-1 surface, and gated MAE has fallen 3.60 → 3.51 → 3.37 on it.** IN FLIGHT: the systematic COMPOSITION AUDIT (two read-only agents ranking under-pole and over-pole list divergences vs sourced real 2026 lists — which factions are under-built like Thousand Sons/Emperor's Children, which over-built like a phantom force-seed). QUEUED off the audit: targeted list-fix builds in priority order; re-screen Votann Hearthband (Votann now near-real); Astra Militarum detachments (order-coverage-blocked). The residual (Death Guard +18, Astra Militarum −16.4, Orks −13.4, Imperial Knights +12, Chaos Knights +10, Aeldari +9.2) is being re-tested as list-fidelity, not pure pricing floor.

> **CHALLENGER-CARD REVERT (2026-07-04, "we can do better") — anchor `data/_anchor_sc54a_n80_log.json`, gated mean absolute error 3.60 (from sc53a's 3.96, −0.36). The best faithful-frame result.** The "missing challenger mechanic" lead was a stale-diagnostic error (challenger had been default-on since wave 252); the reconciliation found the real lever — challenger is OUT-OF-REFERENCE (the competitive Tournament Companion the Warp Friends target uses EXCLUDES it, EVAL_PROTOCOL rule 9) AND metric-negative on the faithful frame, so it was REVERTED to `SWEG_CHALLENGER_CARDS` default-off (`=1` re-enables). Full-frame: Adeptus Astartes −4.63 toward real (undoes most of the self-inflicted ranged-hold over-pole), Genestealer Cults +3.59 / Tyranids +4.01 toward real, T'au lands on real; small losers AdMech / Chaos Space Marines / Thousand Sons. **11 of 22 factions are now within their noise band.** The gated error is now cleanly the model-count / survivor floor: Death Guard +19.0 (gated 16.4, ~45 percent of the total), Astra Militarum −16.6, Imperial Knights +12.0, Orks −12.6, Chaos Knights +10.3, Aeldari +9.6 — all audited faithful across rules/damage/scoring/lists. The scoring signatures re-open is resolved: primary is a faithful equilibrium, the secondary channel (14 vs 22.7) is the one remaining ground-truth-anchored headroom, and challenger (the third "gap") was a diagnostic error now corrected. The Stage-2 pricing fork stands over the model-count floor.

> **THE SECONDARY-ECONOMY RE-ANCHOR (2026-07-03, night) — NEW STANDING ANCHOR `data/_anchor_sc52a_n80_log.json`, gated mean absolute error 4.23. Honest trajectory: … 3.70 → 3.67 → 3.59 → 3.82 (durability wave) → 4.23 (secondary-economy wave). ⚠ THE METRIC ROSE +0.42 AND THAT IS THE HONEST RESULT — six verified printed-rule corrections (the tactical deck completed to 19 cards, card-shedding, hand-gated actions, the 5-card Fixed pool, and the doubled command-point generation) were adopted fidelity-first under the no-knobs ruling; the broken deck was MASKING three real problems that are now visible.**
>
> **The three-way split (sc51a → sc52a):** (1) THESIS CONFIRMED for the gun-durables — **Chaos Knights −3.41 and Imperial Knights −1.61, both DECISIVE toward real**: the restored secondary economy lets the field claw back against primary-banking durables. (2) THESIS FAILED for **Death Guard (+0.80 flat, still +22.5 over, the top over-pole)** — its own secondary rose as much as its opponents', so the counter-weight doesn't cap a faction that banks BOTH primary and secondary; a deeper scoring-shape problem. (3) NEW CRATER — **Orks −3.78 and Genestealer Cults −3.94 DECISIVE away** (hordes now deeper under), Sororitas +6.07 over: ranked hypothesis is horde action-CONVERSION (they hold the cards but the artificial intelligence never performs the actions to score them, while their elite opponents now draw Cull the Horde against them). The horde crater is now DIAGNOSED and the verdict is **FAITHFUL** (docs/_HORDE_CONVERSION_FINDINGS.md, winner-reproduction 3,360/3,360): no conversion bug — the hordes score MORE action victory points than the over-converting elite; the crater is the completed printed deck correctly punishing target-rich rosters, and for Genestealer Cults it is MAJORITY a primary/board-control swing, not the deck. **THE UNIFYING FINDING: the durable OVER-poles (Death Guard +22.5, Imperial Knights +15.7, Chaos Knights +11.1) and the horde UNDER-poles (Orks −8.7, Genestealer Cults −7.0) are ONE phenomenon — the PRIMARY/KILL economy over-paying survivor uptime** (audit D's survivor-snapshot over-reward, now confirmed from both poles). It has passed the damage-math, points-out, and secondary-economy audits, so the remaining suspects are narrow: army-composition/points fidelity, or the Stage-1/Stage-2 boundary (faithful rules + real lists genuinely producing this equilibrium — what the points equation exists to price). **THIS IS THE OWNER-DECISION FORK and may be the edge of Stage 1.** The Astra Militarum −12.4 under-pole is a sibling of the same economy (its bodies get ground down).
>
> **ARMY-LIST FIDELITY AUDIT — CLOSED (owner-chosen surface, 2026-07-03 night): the lists are FAITHFUL; the axis is MODEL-COUNT, not durability.** Two audits converge on a negative (docs/_LIST_AUDIT_DEATHGUARD.md + docs/_LIST_AUDIT_FILL_MECHANISM.md, 100 percent winner reproduction). Horizontal: durable-brick share does NOT track the pole (r=+0.23 n.s.); Astra Militarum is the biggest under-pole yet the most durable, Aeldari a +10.5 over-pole with zero bricks; the real correlate is **wounds-per-point r=−0.49 NEGATIVE — over-poles are low-model elite, under-poles are high-body hordes**. Vertical: Death Guard's list is durability-faithful (marginally BELOW real), 1.2 percent off-template, zero durable phantoms — no unit to remove; the sim already prices durable hulls ABOVE GW (the real Tacoma list costs 2145 sim points). **THE UNIFIED VERDICT: representation, damage, secondary economy, AND army lists are ALL now audited faithful — every Stage-1 fidelity surface is cleared. The residual poles are the model-count / survivor-snapshot primary-scoring economy, which is the STAGE-1-FLOOR / STAGE-2-PRICING BOUNDARY.** The fork is now sharp and sits with the owner: (a) declare the Stage-1 floor and begin Stage-2 pricing (the points equation exists to price exactly this elite-over-horde equilibrium), or (b) name a GROUND-TRUTH-ANCHORED scoring-representation hypothesis to keep chasing in Stage 1 (attrition-rate / objective-resolution — but no real per-game casualty ground truth exists to anchor it without a forbidden tune-to-win-rate dial, per the retracted-floor lesson). Piloting the non-durable under-poles (Adeptus Astartes −5.6) remains a smaller Stage-1 option that does not touch this boundary.
>
> **ADEPTUS ASTARTES RANGED-HOLD ADOPTED (2026-07-03, the owner-chosen piloted work) — NEW STANDING ANCHOR `data/_anchor_sc53a_n80_log.json`, gated 3.96 (from sc52a's 4.23, −0.27).** The eighth scoped ranged-hold: the Marine fire line was Advancing into the enemy and forfeiting its shooting (Eradicators marching past melta shots; the Repulsors that held were the most productive units). N=80: Adeptus Astartes +14.03 (42.0 → 56.1). DISPOSITION — the Chaos Knights / Votann profile: the faction OVERSHOOTS real 47.6 to +8.5 over, and the metric improves via over-pole collateral deflating toward real in the Astartes matchup. The +8.5 overshoot is the model-count / survivor over-reward economy expressing through Marines' now-effective shooting — the Stage-1 floor again. This is the last clean piloted MAE gain. **THE PILOTED LANE IS NOW EXHAUSTED (2026-07-04):** the Emperor's Children ranged-hold (a real observed misplay — Noise Marines / Sorcerer / Tormentors Advancing) screened at N=80 as +20.88 → 63.0, a HARD OVERSHOOT to +9.7 over its live real 53.3 for a −0.04 (within-noise) gated gain — the Tyranids profile, HELD default-off (the model-count / survivor economy expressing through another elite army; note the live Emperor's Children target is 53.3 not the stale hand-table 47.9). Chaos Daemons: clean NULL (flagships already hold correctly). Also this session: the stale `TOURNAMENT_SOURCES` hand table in `evaluate_vs_meta.py` was bannered — it has zero consumers (the live target is `warpfriends_rolling.json`) but had misled agent briefings. **STANDING STATE: anchor sc53a, gated 3.96. Every Stage-1 fidelity surface — rules, damage, scoring economy, army lists — is audited faithful; the piloted under-pole levers are worked out (Astartes adopted, the rest either overshoot on the floor or are hordes in the economy or near-noise). The residual poles ARE the model-count / survivor pricing floor. The Stage-2 fork is the live decision: freeze the faithful sim and fit the points equation (the pricing signal is already visible — the sim prices durable hulls above Games Workshop), or accept 3.96 as the Stage-1 floor.**

> **THE DURABILITY FIDELITY WAVE RE-ANCHOR (2026-07-03, evening) — anchor `data/_anchor_sc51a_n80_log.json`, gated mean absolute error 3.82. Honest trajectory: … 3.81 → 3.82 → 3.70 → 3.67 → 3.59 → 3.82 (the +0.23 is the priced, accepted cost of completing nine verified rule divergences fidelity-first — every one cited, kill-switched, and recorded in the ledger's durability-wave entries).**
>
> **The wave's decisive movers (sc50a → sc51a, full-frame paired):** Death Guard +6.29 (the completed army rule: contagion escalation 3/6/9, Afflicted −1 Toughness, chosen Plague — now +21.7 over its real 46.1, and audit D proved the over-pole is SCORING-shaped, so this is the honest number); Imperial Knights +3.20 (the Defender line-of-sight screen + Rotate Ion Shields outweigh the phantom-melee-save removals and drawable Bring It Down; +17.3 over); Adeptus Custodes +2.79 (elite melee infantry gain the real untargetable-while-pinned protection); Adeptus Mechanicus −5.95 TOWARD real; Astra Militarum −3.86 AWAY (−14.0, the biggest under-pole — ⚠ ATTRIBUTION CORRECTED SAME EVENING: the reciprocal-rule hypothesis was tested and EXONERATED (kill-switch counterfactual +0.24, instrument shows guns simply re-target, zero footprint vs Knights); the −3.86 is the net of the wave's opponent-side defensive completions, and the thread points at the secondary-economy audit, NOT screen piloting — no screen-management lever built, per the six-deep hold-rejection precedent); Adeptus Astartes −2.89. Near-closed for free: Tyranids −1.7, World Eaters +0.3, Sororitas +0.4, Necrons +0.8, Emperor's Children −1.3. **The residual poles are now: Death Guard +21.7 / Imperial Knights +17.3 / Chaos Knights +14.5 over (all scoring-shaped per the audits, representation clean); Astra Militarum −14.0 under.** IN FLIGHT: the Tyranids ranged-hold gauge re-screen vs sc51a. NEXT: an Astra Militarum screen-management piloted pass (the reciprocal-rule loser); the scoring-shaped over-pole question is the owner-decision follow-on with the audit-D evidence.

> **PILOTED WAVE 2 (2026-07-03, afternoon) — anchor `data/_anchor_sc50a_n80_log.json`, gated mean absolute error 3.59. Honest trajectory: … 3.81 → 3.82 → 3.70 → 3.67 → 3.59.**
>
> **ADOPTED: `SWEG_SOROR_RANGED_HOLD`** (sixth scoped ranged-hold, watched-replay: all three Castigators Advanced round 1 firing nothing): **Adepta Sororitas +5.98 DECISIVE (46.2 → 52.1 vs real 50.4), gated 3.67 → 3.59** — the strongest landing since Votann; collateral small and toward-real. The family lesson is now six-for-six: gunline factions under-pole because their fire platforms Advance away their Shooting phases; the melee/commitment factions (Orks, World Eaters) reject every delay lever. REMAINING under-poles: Astra Militarum −10 (attrition-shaped, see the crater findings), Orks −6.0 (structural), Tyranids −3.3, Adeptus Astartes −2.3. Over-poles unchanged: Death Guard +15.4, Imperial Knights +14.1, Chaos Knights +13.9, Adeptus Mechanicus +12.4, Aeldari +11.0.
>
> **⚖️ THE OWNER DECISION IS NOW FULLY TEED UP — the durability wall's last behavioural counter is refuted.** Multi-unit melee caging (`SWEG_MELEE_CAGING`, the coordinated wrap + faithful fall-back surround block) was built, provably fires, and read at N=80: Chaos Knights +3.31 DECISIVE AWAY (the gun-Knight grinds the cage off and cages back), Death Guard flat, **Orks −8.53 DECISIVE AWAY** (diverting the horde onto bricks strips it from objectives — the nomination law in melee form). With offense-boost, attention-direction, and uptime-denial all now refuted at N=80, the sim's bricks are proven NO-WIN INTERACTION NODES, which the real ~47 percent Knight rates say real bricks are not. The residual durable over-pole (~+14 to +15 on three factions) lives in the damage-in versus points-out substrate of durable platforms — the structural remodel question, owner's call, evidence file complete (this entry + the ledger's caging entry + `docs/_EC_CRATER_FINDINGS.md` + the waterfall/uptime counterfactuals). QUEUED behind it: Emperor's Children-side crater instrument; held re-screens (Hearthband, Chaos Knights template) contingent on the remodel.
>
> **⚖️ OWNER RULING RECEIVED (2026-07-03): the durability remodel is only viable RULES-ACCURATE — NO KNOBS.** The remodel is therefore a fidelity-audit program: a FIVE-SURFACE AUDIT WAVE is dispatched (A ranged damage pipeline into bricks, B defensive stratagem/ability coverage, C the points-out side, D Death Guard's defensive economy, E melee pipeline into bricks) — read-only, verbatim-verification, divergences fixed as cited corrections regardless of metric direction; a clean audit closes the durability question as real sim behaviour. **`SWEG_TYRANIDS_RANGED_HOLD` BUILT + HELD default-off** (the seventh family member; Exocrine/Tyrannofex Advance-forfeit observed; filter holds 20 monsters, zero swarm): N=20 probe +24.27 DECISIVE but lands at ~76 vs real 47.4 — the wall in acute form. It is the natural GAUGE of the audit corrections: re-screen it after the divergence fixes land; if the substrate is fixed, its overshoot should collapse toward its real number.

> **THE PILOTED PER-FACTION WAVE (2026-07-03, midday) — anchor `data/_anchor_sc49a_n80_log.json`, gated mean absolute error 3.67. Honest trajectory: … 3.81 → 3.82 → 3.70 → 3.67.**
>
> **ADOPTED: `SWEG_TSONS_RANGED_HOLD`** (watched-replay, fifth scoped ranged-hold): Thousand Sons +2.40 toward real 54.6 (48.2 → 50.6), collateral flat, gated 3.70 → 3.67. The psychic economy and All Is Dust were checked and ARE modelled — the deficit was the mis-pilot (the gunline Advanced into melee). **REJECTED, code not merged: Orks WAAAGH declaration-timing** (−3.76 decisive away at N=20; deferring the WAAAGH strips +1-to-charge from the vanguard) — with the crucial negative finding that Orks already commit in coordinated waves and NO hold-family lever is observation-supported; the Orks −6.0 joins the structural durability family. **CLOSED: `SWEG_AM_ANTITANK_HOLD`** (0 flips at defaults — subsumed by the three adopted holds; liveness proven with them killed). **EMPEROR'S CHILDREN CRATER DIAGNOSED** (`docs/_EC_CRATER_FINDINGS.md`, 160/160 winner reproduction): Astra Militarum wins 18.1% of that cell on symmetric damage — a scoring loss driven by light infantry dying piecemeal before acting; the derived lever `SWEG_AM_CHAFF_STAGING` (widen adopted staging to the exempted chaff) is in build. QUEUED: multi-unit melee caging; the durability-in-contest remodel (owner decision); Adepta Sororitas piloted pass.

> **THE CORRECTED-FRAME WAVE (2026-07-03, morning) — anchor `data/_anchor_sc48a_n80_log.json`, gated mean absolute error 3.70. Honest trajectory: 4.71 → 5.27 → 4.60 → 4.32 → 4.39 → 4.52 → 4.21 → 3.81 → 3.82 → 3.70.**
>
> **The frame-integrity incident (read this before trusting any Astra-Militarum-scoped number dated 2026-07-02/03):** the `SWEG_AM_SECONDARY_PURSUIT` adoption default was silently LOST in a later merge, so anchors sc45a–sc47a embedded a +6.74 Astra Militarum lift their live defaults did not produce — every Astra-Militarum-scoped screen since sc45a carried a ~6-point phantom offset. Fixed and re-screened: **`SWEG_AM_STAGING` (+1.66) and `SWEG_AM_FIRE_SUPPORT_HOLD` (+0.64) ADOPTED default-on** (combined Astra Militarum +1.23, gated 3.82 → 3.70) — both were watched-replay levers, the pilot-observation method's first two products, and both offset-era "rejections" were phantoms. Honest rejections confirmed clean: nomination −5.37, final-round discipline. `SWEG_AM_ANTITANK_HOLD` re-read: **inert at production defaults (0/3,360 flips) — subsumed by the three adopted holds** (liveness proven with the holds killed: +3.99, 86/840). The "bodies-forward law" is STRUCK from the ledger; the corrected law: Astra Militarum units hold when they have real shooting work. NEW PROTOCOL RULE: after every adoption flip, verify the COMMITTED default reproduces the anchor gates-off before promoting.
>
> **The sc48a error surface (sim − real):** over-poles Death Guard +15.5, Imperial Knights +14.4, Chaos Knights +14.1, Adeptus Mechanicus +12.7, Aeldari +11.4, Chaos Space Marines +8.8, Drukhari +7.3; under-poles Astra Militarum −9.9, Thousand Sons −6.4, Orks −6.0, Adepta Sororitas −4.3. **IN FLIGHT (owner-directed piloted per-faction wave):** Orks and Thousand Sons pilot-observation lever derivations (worktree agents, watched renders + move logs, levers only from named observed misplays) and the eval-seeded Emperor's Children crater diagnostic (Astra Militarum wins 29/160 = 18.1% of that anchor cell vs an expected ~47%; the pilot harness did not reproduce it, so the replay uses the anchor's own seeds with a mandatory winner-reproduction gate). QUEUED: multi-unit melee caging (the one un-built faithful counter to the durability wall — deliberately held one wave so the Orks agent's watched games against Knights feed its design); the durability-in-contest structural remodel (owner decision, evidence file complete); Adepta Sororitas piloted pass.**

> **GAP 2 COMPLETION TERM (FOCUS-FIRE) BUILT + HELD default-off (2026-07-03) — `SWEG_FOCUS_FIRE`, the cross-activation "finish the wounded target" companion to the already-adopted `simulator.target_economics` factor (code/simulator.py `_focus_fire_bonus`, wired into both shooting pickers alongside `_target_economics_bonus`; cited `simulator.focus_fire_completion`).** The substrate-accounting diagnostic found the anti-Knight fire budget is sufficient but fire SCATTERS (86-95 percent of shooting into a Knight deals zero, and hits rarely repeat on the same platform two activations running). `simulator.target_economics` already fixed the per-activation half (never dump fire into a hull the weapon cannot wound); this term supplies the missing cross-activation memory using nothing but current-vs-starting health (10e has no in-battle healing, so "below starting health" is durable proof fire already landed): any candidate already wounded AND still woundable by this attacker scores a bonus from 1.0x (untouched) to 3.0x (near death), class-shaped (no single designated target), composing multiplicatively with target economics and gated on the same can't-hurt-it screen even when `SWEG_TARGET_ECONOMICS` is separately unset. Byte-identical off (a `scripts/sim_motion_proof.py` fingerprint match against the unmodified base — identical sha256 both before and after), audit green, run.py --cli clean. **PROBE (`SWEG_FOCUS_FIRE=1 SWEG_TARGET_ECONOMICS=1 SWEG_CK_REALISM=1`, N=20, Chaos-Knights-scoped): 87.5% → 88.5% (paired +1.05, non-decisive, confidence interval ±1.39, 7/840 games flipped) — AWAY from real 44.7%, the same durability-over-reward direction as Staging and the ranged-hold family.** A companion 40-game fixed-seed pilot check (Chaos Knights vs eight opponents, identical seeds both arms) found mean Knights-dead-per-game UNCHANGED at 0.625 in both arms — individual games' kill timing shifted (confirming the term fires) but the aggregate kill count did not rise. HONEST READING: the fire-scatter diagnosis is real and this term measurably changes targeting behaviour, but convergence onto an already-wounded target does not, by itself, convert into more dead Knights at this sample size — the residual bottleneck is most likely the same durability-in-contest substrate the whole "adopted-as-is, overshoots but improves the metric via collateral" family keeps surfacing, or the pre-existing collective-crack gate (`SWEG_FOCUSFIRE`, default-on, a different within-phase mechanism) was already capturing most of the achievable concentration. HELD default-off pending an N=80 re-anchor and a sharper within-round concentration-ratio instrument. Full detail in the DECISION_LEDGER PARKED entry.

> **GAP 1 (STAGING) BUILT + HELD default-off (2026-07-02) — `SWEG_STAGING`, the faction-neutral threat-range-aware commit heuristic, the closing-side counterpart of the ranged-hold family (code/simulator.py `_do_move`; cited `simulator.staging_discipline`).** A closer whose commit would end EXPOSED inside an enemy threat envelope (Move + max(range, charge reach), capped 30") while it cannot act this turn and no friendly mass commits with it STAGES at the edge (Advance sprint suppressed, melee bubbles capped) but still Normal-moves forward (board presence preserved); overrides for objective-claiming-this-turn, chaff, and a commit-mass release. Byte-identical off (0/168 flips vs base), audit green, run.py --cli clean. PILOT (vs Chaos Knights `SWEG_CK_REALISM=1`): fires 13–50/game, heavily round 1 (Orks 45 units / Berzerkers / Screamers held at range instead of trickling in), all forward-EDGE (0 frozen). **CK-scoped probe N=20: 86.5 → 87.5 (+0.95 AWAY from real 44.7) — does NOT close the gun-Knight gap; it slightly inflates the durable Knights (the durability-over-reward wall, the ranged-hold failure mode from the closing side).** Full-frame N=10: gated MAE 6.49 → 4.85 but raw mean-error a wash (6.98 → 6.83) and the durable Knights inflate (Imperial +6.4 / Chaos +3.3 away) — high-variance, not robust. HELD default-off; reopen at an N=80 re-anchor and after the durability-over-reward is corrected. Full detail in the DECISION_LEDGER PARKED entry.

> **FIDELITY-FIRST WAVE 4 — THE CATALOGUE-TRUTH BATCH (2026-07-02, evening). New standing anchor `data/_anchor_sc45a_n80_log.json`, gated mean absolute error 3.81 — the best honest frame, now 0.9 below the first honest re-base. Honest trajectory: 4.71 → 5.27 → 4.60 → 4.32 → 4.39 → 4.52 → 4.21 → 3.81.**
>
> **ADOPTED this wave:** (1) **the weapon-choice TARGET-BASKET fix** — the choice scorer had NO WOUND ROLL AT ALL (Strength ignored in every weapon choice catalogue-wide); the basket scorer (archetype-derived class weights, wound roll restored, one-shot discount) flips 185 units toward their real picks (Ravager fires Dark Lances) — settled-frame N=80 vs sc43a **−0.33** with World Eaters −7.87 / Tyranids −6.19 decisively toward real; (2) **the multiplicity + Dominus-bundle mapper fixes** — min=2/max=2 weapons never expanded and carapace bundles parsed to zero; 228 units gain their legal copies (Castellan/Valiant fire their full carapace at last), zero net-negative; (3) **the override staleness adjudication** — 19 of 27 clobbered hand overrides RETIRED against BSData verbatim (several would have stripped real weapons if restored), 8 kept as live defects, 3 more retired by the multiplicity fix; (4) **Myphitic Blight-hauler re-enabled** (cited 100 points + two mapper-dropped weapons restored; Death Guard −1.73 toward real); (5) **`SWEG_AM_SECONDARY_PURSUIT`** — the secondary card-pursuit package army-scoped to Astra Militarum per the Principle-2 recipe: **Astra Militarum +6.74 DECISIVE (27.6 → 34.3 toward real 45.3), gated 4.21 → 3.81 (−0.39)** — the biggest single headline move of the honest era; the universal package (`SWEG_SECONDARY_PURSUIT`, AM +5.17 but Necrons/Daemons +5) stays held.
>
> **HELD with named findings:** `SWEG_VOTANN_HEARTHBAND` (real detachment, +2.71 away on a durability-inflated faction — re-screen after the durability question resolves); `SWEG_TERRAIN_DENSE` (faithful published layouts; terrain REFUTED as the gunline cause a third time); `SWEG_OVERRIDE_MELEE_PRECEDENCE` (mechanism right, but gate-on reads +0.31 with Astra Militarum −6.49 — suspected interaction with the battleline-specials load-time reconstruction; investigate before adopting; the underlying data is now clean so the gate guards future overrides only); `SWEG_TAC_VOLUNTARY_DISCARD` + `SWEG_ACTION_ECONOMY` (revisit stacked with further card-achievability work).
>
> **THE LAST STANDING STRUCTURAL QUESTION, now confounder-free:** gun-Knights (Chaos Knights template arm: 85.8% vs real 44.7) win 6 of 7 sim games with legal loadouts, sourced lists, faithful scoring, dense terrain, and a field that now carries real anti-tank (which did NOT deflate them — probe 84.9 → 88.6). Remaining suspects: what real opponents DO against gunlines that the sim's artificial intelligence does not (counterplay quality — the diagnostic showed units fed piecemeal into the kill zone), and the durability-in-contest substrate (72.8% of shooting into Knights deals zero). NEXT: the counterplay diagnostic; then re-screen the held Chaos Knights template + Hearthband on whatever it yields. — MAPPER CHOICE-GROUP FIX + WORLD EATERS TEMPLATE ADOPTED (2026-07-02). New standing anchor `data/_anchor_sc40a_n80_log.json`, gated mean absolute error 4.39 (sc38a 4.60 → sc39a 4.32 mapper fix → 4.39 with the World Eaters sourced template). Honest-frame trajectory since the fidelity-first ruling: 4.71 → 5.27 → 4.60 → 4.32 → 4.39.**
>
> **The mapper choice-group fix (ADOPTED, ungated regen — commits `33ec484` + `f04b4a7`).** The third mapper-layer bug found by chasing honest residuals: mixed choose-one wargear groups equipped BOTH their best ranged AND best melee candidate (`_collect_single_model_weapons` scored the two types independently). The fix scores whole OPTIONS on one expected-damage basis then partitions — correctly preserving 29 legitimate dual-profile weapons (Singing Spear class). Diff audit: 69 units changed, removal-only, 9 legitimate type flips, Despoiler/Defiler now legal, Cerastus/Rampager/Desecrator untouched, test-failure set byte-identical to base. Full-frame N=80: **4.60 → 4.32 (−0.28), Imperial Knights −5.82 decisive toward real.**
>
> **`SWEG_WE_REALISM` ADOPTED default-on (both gate reads — the template filter AND the Khârn force-seed pre-pass).** Re-screened on the fixed frame: World Eaters +5.46 (46.5 → 52.0 vs real 44.9), gated 4.32 → 4.39 (+0.08) — the sourced list honestly exposes the residual World Eaters melee over-modelling (down from +12.17 pre-fix). Adopted per the list-realism precedent; the exposed melee family is the open target.
>
> **`SWEG_CK_REALISM` STILL HELD — the reason sharpened.** Re-screened: Chaos Knights +10.90 (59.3 → 70.2 vs real 44.7) even with legal loadouts. Root cause found: the mapper's expected-value picker resolves the Knight Despoiler to its MELEE arms (chainsword + claw beat the guns on expected damage), but sourced competitive Despoilers are GUN platforms — a loadout-CHOICE fidelity gap (the anti-tank-picker-bias class). **QUEUED NEXT: a sourced Despoiler gun-build `model_loadouts` override (the Knight Tyrant precedent), then the Chaos Knights template re-screens.** Note the Despoiler's melee-resolved build also changes its ranged-hold classification (`_ad_ck` reads extra_melee_profiles) — the override restores it as the ranged platform that lever was built for.
>
> **The remaining queue:** Despoiler gun-build override + Chaos Knights re-screen; the Chaos Daemons Be'lakor/Scintillating-Legion archetype; Votann Hearthband detachment; Myphitic Blight-hauler re-enable; the override-precedence bug (per-model weapon-field rebuilds discard hand `extra_melee_profiles`); the secondary-economy under-shoot (12.2 vs real 22.7 — deck-churn / action artificial intelligence); Grizzled Company re-screen once Astra Militarum can convert Orders into scoring. Residual leaders on sc40a: Astra Militarum under, Chaos Knights (held) and the melee family over.

> **FIDELITY-FIRST PHASE B WAVE 1 — TEMPLATE-FIRST FILL + THREE TEMPLATE CORRECTIONS ADOPTED (2026-07-02). New standing anchor `data/_anchor_sc38a_n80_log.json`, gated mean absolute error 4.60 (sc36a 4.71 → sc37a 5.27 fill re-base → 4.60). The honest frame is now beating the pre-fill honest frame, with the two biggest template phantoms gone.**
>
> **Adopted this wave (each `=0`/unset kill-switch):** `SWEG_FILL_TEMPLATE_POOL` (template-first random fill — the archetype now governs ~the whole army; N=40 aggregate wash hiding a faithful ±15 redistribution; N=80 re-base read 5.27 as anchor sc37a); then, screened against sc37a and flipped together (combined scoped N=80: **gated 5.27 → 4.60, −0.67**, composition ≈ sum of parts): **`SWEG_DG_REALISM`** (Death Guard −9.90, 69.5 → 59.6 toward real 47.6 — phantom Typhus/Blightspawn out, Daemon Prince + bodies in), **`SWEG_EC_REALISM`** (Emperor's Children −8.91, 65.3 → 56.4, nearly ON its real 53.3 — Fulgrim/Keeper/Daemonettes out, 3× Lord Exultant + Defilers in), **`SWEG_IK_REALISM`** (Imperial Knights −2.01 toward real — Canis Rex out of the template, Cerastus Lancer/Atrapos + Knight Defender in). All template gates now flow through the shared `_effective_template` layer, which also plugged the wave-250 gate leak (the fill previously received the RAW template).
>
> **HELD, with reasons (do not treat as rejected):** `SWEG_WE_REALISM` (World Eaters +12.17 away — the sourced list is genuinely stronger; ALSO stacks 2× Defiler, see the bug below) and `SWEG_CK_REALISM` (Chaos Knights +13.31 away; stacks 2× Knight Despoiler) — **both held pending the mapper choice-group fix, as sequencing, not rejection**; they re-screen the moment the fix lands. `SWEG_AM_GRIZZLED` (the real top-meta Astra Militarum detachment, built + verbatim-cited; screened Astra Militarum −2.07 non-decisive / +0.10 — the sim's Astra Militarum cannot yet convert the Order economy into scoring; re-screen after the unit fixes + secondary-economy work; the Infernal Lance don't-swap precedent).
>
> **THE NEW BUG CLASS (third mapper-layer bug found by chasing honest residuals): choice-group ranged/melee independence.** `code/bsdata/mapper.py _collect_single_model_weapons` picks each BSData choose-one wargear group's best RANGED and best MELEE candidate INDEPENDENTLY, so mixed-type groups contribute BOTH. Proven cases (verified verbatim vs BSData): the **Knight Despoiler** fires thermal cannon + rocket pod + meltagun AND fights with claw + chainsword + feet (~19 ranged + ~41 melee expected damage — no legal build); the **Chaos Defiler** fires FOUR ranged weapons and carries a DUPLICATED electroscourge. Cerastus Lancer/Atrapos/Rampager/Desecrator audited clean. A second adjacent bug: `_add_squad_per_model` weapon-field rebuilds DISCARD hand-written `extra_melee_profiles` overrides (the old Despoiler override was inert). **The structural fix (pick per group from the COMBINED pool, then partition) is being built by a dispatched agent with a full parsed.json diff audit** — it deflates Despoiler/Defiler to legal builds, after which the held World Eaters / Chaos Knights templates re-screen. The override-discard bug is a separate queued fix.
>
> **Next:** land + screen the mapper choice-group fix (full-frame — it touches every affected chassis), re-screen WE/CK templates, then the remaining audit items (Daemons Be'lakor archetype; Votann Hearthband; Myphitic Blight-hauler re-enable; secondary-economy under-shoot 12.2 vs 22.7 owned by deck-churn/action artificial intelligence).

> **FIDELITY-FIRST PHASE A — THE SCORING-TRUTH BATCH (2026-07-02, branch `claude/sim-calibration-17`). Three scoring-rule corrections ADOPTED default-on under the owner's fidelity-first ruling; the frame RE-BASES to the honest scale. New standing anchor `data/_anchor_sc36a_n80_log.json`, gated mean absolute error 4.71 at N=80 (from sc35a's 2.23 — the +2.47 is the quantified compensating-error scaffolding, removed on purpose). ⚠ ALL PRIOR LEVER NUMBERS ARE OLD-SCALE; compare nothing across this re-base.**
>
> The three adoptions (each `=0` kill-switch; all-off validated 0/18,480 flips vs sc35a before screening; N=40 screen deltas below are on the 2.37 N=40 baseline):
> * **`SWEG_TAC_DECK_CONSUMER_FIX`** (+2.72 alone — THE prop). The wave-210-adopted 2-card tactical secondary deck never actually ran: the secondaries-module gate-read (`== "1"`, default-off) disagreed with the simulator-side read (`!= "0"`, default-on), so production scored the full legacy card union, uncapped, every round (~39 secondary points per player per game vs real 22.7). The fix aligns the consumer read. Durables surged (Imperial Knights +17.9, Chaos Knights +11.1) and hordes cratered (Astra Militarum −12.5, Genestealer Cults −9.3) — the secondary over-count was the scaffolding holding the fragile factions up.
> * **`SWEG_R5_SECOND_LAST`** (+0.44 alone). The Chapter Approved 2025-26 round-5 going-second exception (primary scores at end of turn, not Command phase) — rebuilt from the rule text (the wave-253 build was worktree-only and never committed). Cited `simulator.primary_vp_round5_second_player`.
> * **`SWEG_OC_PER_MARKER`** (+0.13 alone). Removes the wave-67 one-marker-per-squad Objective Control clamp; per the verbatim core rule, Level of Control is computed per marker over all models in range. Cited `simulator.oc_per_marker`.
>
> **THE SIGNATURE WINS (scripts/diag_signatures, three gates on): going-first fell from ~69% to 50.0% (real 49-52%)** — the tempo axis that was a "DECLARED STRUCTURAL FLOOR" is RESOLVED by scoring truth, exactly as the 2026-07-01 scoring audit predicted (it was the secondary/timing root expressed through tempo; do NOT resurrect the rejected going-second artificial-intelligence mechanisms). Primary points per player per game 35.0 (real ≈ 29, closer than before). **The open honest gap: secondaries now UNDER-shoot — 12.2 vs real 22.7** — the deck path is live but its scoring economy is thin; owned by future deck-churn / action-performing artificial-intelligence work, not by re-inflating the card pool.
> * **`SWEG_ACTION_ECONOMY` RE-SCREENED on the honest frame and HELD default-off again**: stacked on the three fixes it read 5.46 vs 5.01 (+0.45 worse), Astra Militarum flat (23.9 → 24.2), Imperial Knights unchanged (+17.9) — the three real Tactical action cards still fail their anti-durability purpose. THE FAILURE IS NOW LOCALIZED: the cards are faithful; the action-performing artificial intelligence (the spare-unit contract) does not convert horde bodies into scoring. A future piloting lever, not a card problem.
>
> **The honest residual surface (combined arm vs real): Imperial Knights +22.9, Chaos Knights +18.6, Astra Militarum −21.1, Genestealer Cults −8.1, Chaos Space Marines −4.7, Aeldari +10.0(→51.7 vs 41.5)...** — this is what the archetype layer must now answer for. PHASE B (owner-authorized, fidelity-first): the archetype corrections from `docs/ARCHETYPE_FIDELITY_AUDIT.md` — template fixes (Angron→Khârn, Emperor's Children overhaul, Death Guard phantoms, Knights chassis menus), the random-fill scoping/weighting mechanism, the wrecker-cap fix, and the missing real detachments (Astra Militarum Grizzled Company, Votann Hearthband, Emperor's Children Coterie of the Conceited). The Imperial Knights +22.9 in particular is the Canis-anchored template + missing Cerastus chassis; the Astra Militarum −21.1 awaits its real top detachment.

> **CANIS REX PHANTOM SECOND MODEL REMOVED — ADOPTED FIDELITY-FIRST (2026-07-01, evening). New standing anchor `data/_anchor_sc35a_n80_log.json`, gated mean absolute error 2.23 (up from sc34a's 2.02, +0.21 — a faithful data correction adopted per the never-gate-a-faithful-mechanic-off rule; see the recoup note). ⚠ RULE-14 CHECKPOINT REACHED: this is the ~15th wave commit on `claude/sim-calibration-16` — mark the pull request merge-ready; the next wave starts on `claude/sim-calibration-17`.**
>
> **The bug** (found by the owner-prompted archetype-fidelity audit, `docs/ARCHETYPE_FIDELITY_AUDIT.md`): BSData folds Sir Hekhtur — the ejected-pilot replacement, a 3-wound INFANTRY character who per the datasheet only exists AFTER the Canis Rex model is destroyed ("Your Canis Rex unit is not considered to be destroyed until Sir Hekhtur is also destroyed", BSData unit id 5174-754e-83a-30f) — into the Canis Rex entry as a second model_loadouts slot, and the mapper emitted min_models=max_models=2. The per-model squad builder spawned one Unit per slot with BOTH inheriting the shared 26-wound profile, so every simulated Imperial Knights game fielded TWO permanent 26-wound TITANIC combatants for 415 points (the archetype's deterministic seed guarantees Canis Rex in 100% of Imperial Knights builds). `SWEG_IK_CANIS_SINGLE` (default-on, `=0` kill-switch, code/bsdata/loader.py) sets the unit to one model and drops the Hekhtur slot; off-arm reproduces sc34a byte-identically (0 flips). Cited `simulator.ik_canis_single`.
>
> **The result is diagnostic gold: Imperial Knights got STRONGER without the phantom — +3.31 (55.3 → 58.6, AWAY from real 47.7), gated 2.02 → 2.23 (522 flips).** The phantom body was a net LIABILITY: a second TITANIC 26-wound kill for opponents to farm Bring It Down / Assassination secondary points from, while contributing only pistol-tier weapons. Adopted anyway — a verified real-datasheet correction is never gated off to protect the metric (the audit-2 Lucius-restore precedent). **Recoup note:** the Imperial Knights over-pole now clearly contains list-zone error — the same audit shows real 2026 Imperial Knights lists run Cerastus-class chassis (absent from the sim's template menu) and essentially never Canis Rex (force-seeded into 100% of sim builds by the greedy walk); those owner-reserved template corrections are expected to claw this regression back. Also this wave: `SWEG_AM_CHARGE_DISCIPLINE` REJECTED + REVERTED (Astra Militarum −2.41 decisive away, gated 2.02 → 2.17 — blocking "futile" charges strictly hurts; see the ledger).
>
> **THE AUDIT HEADLINE (owner-prompted, eight parallel read-only audits): the "structural durability over-reward" is NOT a distinct mechanism** — it decomposes into archetype-template curation errors (Angron / Fulgrim / Canis Rex / Typhus+Blightspawn force-seeded into 100% of games while appearing in ~zero sourced 2026 lists), the uniform whole-catalogue random fill (~55-70% of every army), the wrecker-cap formula blocking real-meta staples entirely (Be'lakor, Death Guard Daemon Prince, every Cerastus Knight), missing real detachments (Astra Militarum Grizzled Company, Votann Hearthband, Emperor's Children Coterie of the Conceited — the sim's Emperor's Children runs NO detachment and still over-performs +10.6), three concrete scoring deltas (round-5 second-player timing; the secondary gate-mismatch scoring ~39 points/game vs real 22.7; the NEW never-measured one-marker-per-squad Objective Control clamp the real rules do not have), and the Canis Rex data bug above. Counter-cases kept honest: Votann composition is in-range (its overshoot is real sim behaviour), Death Guard durable-share matches reality, and the corrected Astra Militarum list screened WORSE (though on a stale frame, and the held list gates have a random-fill leak — Angron re-enters ~27% of "fixed" World Eaters builds). Full synthesis + per-faction findings + the owner decision menu: `docs/ARCHETYPE_FIDELITY_AUDIT.md`. **Next autonomous lever: the Objective Control one-marker clamp faithful fix (unmeasured); next re-tests: round-5 timing + secondary cap on the current frame. The list-zone and detachment corrections are owner decisions.**

> **ADEPTUS CUSTODES OFFENSE BATCH ADOPTED FIDELITY-FIRST (2026-07-01) — three missing datasheet abilities from the re-dispatched Custodes offense audit; the combined effect is a WASH, which is itself the finding: the Custodes under-pole is not a datasheet-offense gap. New standing anchor `data/_anchor_sc34a_n80_log.json`, gated mean absolute error 2.02 (from sc33a's 2.01, +0.01 within noise).**
>
> Three gates, each default-on with a `=0` byte-identical kill-switch (the all-off Adeptus-Custodes-scoped N=80 arm reproduces sc33a with 0 of 3,360 flips; every rule text verified verbatim in the BSData cache before building):
> * **`SWEG_CUSTODES_MASTER_STANCES`** — Shield-Captain "Master of the Stances" (BSData profile id e6a6-1d13-3027-3fce): once per battle, the led Custodian Guard / Custodian Wardens squad fights with BOTH Ka'tah stances, adding the DACATARAI stance's [SUSTAINED HITS 1] on top of the already-modelled always-RENDAX [LETHAL HITS]. `Battle._apply_master_of_stances` (the Nova Charge once-per-battle pattern; led-detection via the leaders.py proximity + legal-host convention). Alone: Adeptus Custodes +0.79 toward real, gated 2.01 → 1.98 — the one metric-positive lever of the batch.
> * **`SWEG_CUSTODES_ADVANCED_FIREPOWER`** — Caladius Grav-tank "Advanced Firepower" (BSData profile id 60a4-507e-da36-683c): weapon-AND-target-conditional ranged [LETHAL HITS] (blaze cannon vs MONSTER/VEHICLE, accelerator cannon vs everything else), resolved per weapon profile inside the multi-weapon firing loop. Alone: Adeptus Custodes −0.24 flat, gated +0.04 — it fires (43 flips) but the blaze cannon already wounds most targets on 2s and 3s, so the critical-hit auto-wound adds nearly nothing.
> * **`SWEG_CUSTODES_SLAYERS_OF_TYRANTS`** — Allarus Custodians "Slayers of Tyrants" (BSData profile id d569-61db-406d-1d42): re-roll the Wound roll against CHARACTER / MONSTER / VEHICLE targets, both ranged and melee, through the TWIN-LINKED re-roll path. BSData carries the ability on the Allarus Custodians datasheet ONLY (not the Shield-Captain in Allarus Terminator Armour), hence the exact unit-name gate. Alone: Adeptus Custodes +0.12 flat, gated +0.02.
>
> **Combined Custodes-scoped N=80 vs sc33a: Adeptus Custodes +0.31 (46.1 → 46.5, toward its real 49.5 but far short of it), gated 2.01 → 2.02 (+0.01 wash)**; the only confidence-interval-clear collateral is trivial in magnitude (Tyranids −0.28, Grey Knights −0.55). Adopted fidelity-first — verified real rules, no empirical harm (the Tormentors / Assemblage of Might / Nova Charge disposition class). **THE FINDING: the Custodes durable-but-under anomaly (−5.0 vs real 49.5) is NOT closed by datasheet offense — the third confirmation that the lifts come from piloting, not datasheet abilities.** The audit's remaining candidates (Quicksilver Execution, Moment Shackle, Stand Vigil, Sentinel Storm) are lower-ranked datasheet effects with the same expected shape; the Custodes residual joins the structural family pending the parked representation work. Unit-test suite: 24 failures + 9 errors are PRE-EXISTING (identical counts with the three gates on and off). Audit + smoke clean. Build note: `Unit` uses `__slots__` — a once-per-battle marker must live on the `Battle` (the `_custodes_mots_used` UID set, Nova Charge pattern), never stamped onto Unit instances.

> **LEAGUES OF VOTANN RANGED-HOLD ADOPTED FIDELITY-FIRST (2026-07-01) — the advance-discipline piloting pattern, scoped to Votann. New standing anchor `data/_anchor_sc33a_n80_log.json`, gated mean absolute error 2.01 (down from sc32a's 2.09, −0.08). The biggest actionable under-pole lifts.**
>
> `SWEG_VOTANN_RANGED_HOLD` (default-on, `=0` kill-switch). The same `_suppress_advance` gunline-hold logic (a fourth entry point `_ad_votann` alongside Astra Militarum / Chaos Knights) scoped to Leagues of Votann: its heavy shooting platforms (Hekaton Land Fortress, Sagitaur, Hearthkyn Warriors) hold and shoot instead of Advancing and forfeiting their Shooting phase; the ranged-damage-per-activation ≥ 2.0 & range ≥ 18" filter excludes the short-range melee units (Cthonian Beserks). Votann-scoped N=80 vs sc32a: Votann +10.37 DECISIVE (42.9 → 53.3), gated 2.09 → 2.01. NB it OVERSHOOTS real 48.0 — the same durability over-reward amplification as the Chaos Knights ranged-hold (the durable Hekaton over-scores when held alive); the metric improves via broad over-pole collateral deflating toward real (World Eaters −0.90, Emperor's Children −0.77, Death Guard −0.64...). Adopted fidelity-first (same disposition as the Chaos Knights ranged-hold). Off byte-identical; audit + smoke clean. Cited `simulator.votann_ranged_hold`. **The scoped advance-discipline family is the session's highest-value lever kind** (Astra Militarum +5.75, Chaos Knights +11.27, Votann +10.37) — but on durable factions it overshoots via the durability wall, banking the metric through opponent collateral rather than target accuracy.

> **DRUKHARI INCUBI TORMENTORS ADOPTED FIDELITY-FIRST (2026-07-01) — a missing datasheet ability (near-inert). New standing anchor `data/_anchor_sc32a_n80_log.json`, gated mean absolute error 2.09 (down from sc31a's 2.11, −0.02).**
>
> `SWEG_DRUKHARI_TORMENTORS` (default-on, `=0` kill-switch). Incubi "Tormentors" (BSData ability id 8ffb-f801-cd2f-d05e, verified verbatim): at the start of the Fight phase, each enemy in Engagement Range of an Incubi unit takes a Battle-shock test, and Incubi melee attacks get +1 to Hit vs Battle-shocked targets. `Battle._apply_drukhari_tormentors_forced_tests` (fight-phase, reuses `_battleshock_test_squad`) + a +1 hit_mod in `Unit.attack` (mirrors the Chaos Knights Doom battle-shock check). Drukhari-scoped N=80 vs sc31a: near-inert (Drukhari −0.13 within noise — the +1-hit-vs-Battle-shocked is marginal since Incubi already kill what they hit; the 142 flips are mostly the RNG-stream shift from consuming battle-shock dice), gated 2.11 → 2.09. Adopted fidelity-first (verified real rule, metric-neutral). Off byte-identical. Cited `simulator.drukhari_tormentors`.

> **ADEPTUS CUSTODES MARTIAL KA'TAH [LETHAL HITS] ADOPTED FIDELITY-FIRST (2026-07-01) — a missing army rule, the biggest lever of the lever-hunt batch. New standing anchor `data/_anchor_sc31a_n80_log.json`, gated mean absolute error 2.11 (down from sc30a's 2.35, −0.24). The Custodes under-pole closes hard.**
>
> `SWEG_CUSTODES_KATAH_LETHAL` (default-on, `=0` kill-switch). Martial Ka'tah (BSData shared rule id e348-7090-3aff-ee2c, verified verbatim): a Custodes unit picks one Ka'tah stance each time it fights; RENDAX grants its melee weapons [LETHAL HITS]. The sim models the optimal always-RENDAX choice for all Custodes melee — a one-conditional `effective_lethal_hits = True` in `Unit.attack`, mirroring the World Eaters faction-scoped lethal-hits pattern. Custodes-scoped N=80 vs sc30a: Custodes +3.66 DECISIVE (42.8 → 46.4, toward its real 49.5, no overshoot), gated 2.35 → 2.11. Collateral toward-real (Chaos Knights −0.61 deflating its ranged-hold overshoot, Drukhari −0.55). APPROXIMATION: always-RENDAX (no per-fight/per-target stance variation). Off byte-identical; audit + smoke clean. Cited `simulator.custodes_katah_lethal`.

> **T'AU RIPTIDE NOVA CHARGE ADOPTED FIDELITY-FIRST (2026-07-01) — a missing datasheet ability. New standing anchor `data/_anchor_sc30a_n80_log.json`, gated mean absolute error 2.35 (down from sc29a's 2.37, −0.02). First lever from the lever-hunt audit; the T'au under-pole closes further.**
>
> `SWEG_TAU_NOVA_CHARGE` (default-on, `=0` kill-switch). The Riptide Battlesuit's Nova Charge (BSData id 8fb6-aff7-75ac-f206, verified verbatim): once per battle, when selected to shoot, its ranged weapons gain [DEVASTATING WOUNDS] for the phase. Modelled via `Battle._apply_nova_charge` (mirrors the Possessed Unholy Bloodshed once-per-battle pattern) + a `tau_nova_charge` profile flag set on the Riptide via `data/overrides.json` (wired through the loader). T'au-scoped N=80 vs sc29a: T'au +1.74 (51.2 → 52.9, toward its real 54.3), gated 2.37 → 2.35 (collateral Imperial Knights −0.70 toward-real). APPROXIMATION: whole-unit transient for the phase vs the codex's one-chosen-weapon (same as Unholy Bloodshed). Off byte-identical; audit + smoke clean. Cited `simulator.tau_nova_charge`.

> **CHAOS KNIGHTS RANGED-HOLD ADOPTED FIDELITY-FIRST, ASTRA MILITARUM OFFICER FRONT-LINE REJECTED (2026-07-01) — the deep-dive tail. New standing anchor `data/_anchor_sc29a_n80_log.json`, gated mean absolute error 2.37 (down from sc28a's 2.43, −0.07 — the best standing frame). Two of the three tail levers screened; the third (mid-game re-embark) is pending an owner decision.**
>
> **Chaos Knights ranged-hold (ADOPTED, `SWEG_CK_RANGED_HOLD`, default-on, `=0` kill-switch).** The Astra Militarum advance-discipline logic scoped to Chaos Knights RANGED platforms (no extra_melee_profiles — Knight Tyrant, Desecrator, War Dog Executioner/Stalker/Brigand/Huntsman): they hold and shoot instead of Advancing and forfeiting their Shooting phase; melee platforms (Rampager/Despoiler/Abominant/Karnivore) keep charging. CK-scoped N=80 vs sc28a: Chaos Knights +11.27 (39.1 → 50.3), gated 2.43 → 2.37. NB it OVERSHOOTS real 44.7 — the structural durability over-reward amplifies the durable TITANIC platforms held alive (the metric improves via opponent collateral, not CK accuracy — CK's error is unchanged, 5.6 under → 5.6 over). Adopted fidelity-first on the owner's call: the piloting is faithful; the overshoot is the over-reward's fault, to be corrected structurally. Cited `simulator.ck_ranged_hold`.
>
> **Astra Militarum officer front-line (REJECTED + REVERTED).** `SWEG_AM_OFFICER_FRONT_LINE` — suppress the DUAL melee-charge for AM OFFICER units to keep them within 6" issuing Orders — was built and screened but reverted: AM-scoped N=80 vs sc28a, Astra Militarum −2.19 (39.5 → 37.3, AWAY from real ~45), gated 2.43 → 2.57 (+0.14 worse), 13 of 21 matchups dipped. Holding officers back makes them passive; the "preserve Order economy" theory does not pan out (SWEG_OFFICER_FOLLOW already handles the drift). A faithful-sounding piloting change that empirically hurts.
>
> **Mid-game re-embark (`SWEG_REEMBARK`) — BUILT + HELD default-off (2026-07-01).** A disembarked unit was otherwise stuck on foot for the rest of the game; `_maybe_reembark_after_move` (code/simulator.py, end of `_do_move`) lets a unit re-embark into a friendly transport within 3" after its move (not if in Engagement Range or the sole objective controller). Full-matrix N=40 vs sc29a was NEAR-INERT (very low flip counts, no decisive movers, gated 2.58 → 2.69 within noise) — the 3" re-embark condition is rarely met in the archetype games. Held (the wave-248 pattern: the capability is implemented behind the gate, not adopted default-on, since it barely fires and is slightly metric-negative). Reopens if the archetype frame features transports more heavily, or once the durability over-reward is corrected. Cited `simulator.reembark`; off path byte-identical.

> **CHAOS KNIGHTS DREAD-CASCADE TARGET-BIAS ADOPTED FIDELITY-FIRST (2026-07-01) — the last lever of the per-faction target-selection batch. New standing anchor `data/_anchor_sc28a_n80_log.json`, gated mean absolute error 2.43 (down from sc27a's 2.46, −0.03 — the best standing frame). Confirms the batch's refined lesson: a target-selection bias that routes fire to a CLASS of targets helps; only single-target concentration drags.**
>
> **The lever.** `SWEG_CK_DREAD_FOCUS` (default-on, `=0` kill-switch) makes the Chaos Knights shooting picker prefer Below-Half-strength enemy targets (a 2.0x multiplier in `_ck_dread_cascade_target_bonus`, code/strategy.py), the class the Harbingers of Dread / War Dog Executioner cascade rewards (Doom's +1-to-Wound vs Battle-shocked, the Executioner's +1-to-Hit and Battle-shock-on-kill vs Below-Half, Delirium's mortal wounds to Below-Half units that fail Battle-shock). Artificial-intelligence heuristic grounded in those rules; cited `strategy.ck_dread_cascade_target`.
>
> **Result.** Chaos-Knights-scoped paired N=80 vs sc27a: Chaos Knights +0.57 (38.5 → 39.1, toward real 44.7), gated 2.46 → 2.43, no decisive collateral and no overshoot (well under 44.7 despite Chaos Knights being a durable faction). Off byte-identical (0/36,960 flips); run.py --cli clean; scripts/audit_rules.py passes.
>
> **BATCH COMPLETE — the per-faction target-selection thesis, resolved.** Five per-faction levers screened. The finding: army-scoped target-selection biases that route fire to a CLASS of high-value targets (Leagues of Votann on-objective, Chaos Knights Below-Half) improve the metric, but a bias that over-concentrates on a SINGLE designated unit (T'au Spotted/Markerlit) drags it via closed-matrix collateral and is rejected. The rule-bearing levers (T'au [IGNORES COVER], Adeptus Custodes Assemblage of Might) are adopted fidelity-first regardless of magnitude. Combined with the flagship Astra Militarum advance-discipline (the first per-faction lever) and the Drukhari deep-strike deploy fix, this session took the gated mean absolute error from 2.91 to 2.43 across seven commits.

> **ADEPTUS CUSTODES ASSEMBLAGE OF MIGHT ADOPTED FIDELITY-FIRST (2026-07-01) — a real detachment rule modelled at last, resolving a sim-calibration-6 no-op. New standing anchor `data/_anchor_sc27a_n80_log.json`, gated mean absolute error 2.46 (up from sc26a's 2.45, +0.01 — a near-inert wash adopted for fidelity). The last rule-bearing lever of the per-faction target-selection batch.**
>
> **The rule.** `SWEG_CUSTODES_ASSEMBLAGE_OF_MIGHT` (default-on, `=0` kill-switch). The Auric Champions detachment rule: at the start of the Command phase the Custodes player designates one enemy unit, and until the next Command phase every ADEPTUS CUSTODES CHARACTER attack against that unit adds 1 to the Wound roll. Modelled with a bespoke `assemblage_of_might` detachment flag (code/detachments.py), a per-round `_highest_threat_enemy` designation stored on `Army._assemblage_target_uid` (code/simulator.py, parallel to Oath of Moment / Machine Vengeance), and a +1 to wound_mod_delta in Unit.attack scoped to Custodes CHARACTER attackers vs the designated unit. The generic Detachment schema could not proxy the CHARACTER-only single-designated-target mechanic without fabrication, so it had shipped as a documented no-op since sim-calibration-6; the bespoke flag now models it faithfully. NB the old AURIC_CHAMPIONS note that Custodes over-shoots (~+5) is stale — post-audit Custodes sits ~44 vs its real 49.5 (under-pole), so the buff is toward-real.
>
> **Result.** Custodes-scoped paired N=80 vs sc26a: near-inert — 14 of 3360 flips, Custodes −0.19 (noise), gated 2.45 → 2.46. The +1-to-Wound for the ~3 CHARACTER models per list against one designated target is a small effect (they already wound most targets well). Adopted fidelity-first (a faithful rule, metric-neutral within noise). Off byte-identical (0/36,960 flips); run.py --cli clean; scripts/audit_rules.py passes.

> **LEAGUES OF VOTANN PRIORITISED-EFFICIENCY TARGET-BIAS ADOPTED FIDELITY-FIRST (2026-07-01). New standing anchor `data/_anchor_sc26a_n80_log.json`, gated mean absolute error 2.45 (down from sc25a's 2.49, −0.04 — the best standing frame). A per-faction target-selection AI heuristic that, unlike the rejected T'au single-target bias, HELPS — because it routes fire to a CLASS of targets, not one unit.**
>
> **The lever.** `SWEG_VOTANN_PE_TARGET_BIAS` (default-on, `=0` kill-switch) makes the Leagues of Votann shooting picker prefer on-objective enemy targets (a 1.5x score multiplier in `_votann_pe_target_bonus`, code/strategy.py, following the Astartes-Oath pattern), concentrating fire where the already-modelled Prioritised Efficiency / Hostile Acquisition +1-to-Hit compounds. Artificial-intelligence piloting heuristic grounded in the real army rule; cited `simulator.votann_pe_target_bias`.
>
> **Why it helps where T'au's bias dragged.** Votann-scoped paired N=80 vs sc25a: Votann +0.97 (toward real), gated 2.49 → 2.45, NO decisive collateral. The contrast with the rejected T'au guided bias is the batch's refined lesson: a bias that over-concentrates on a SINGLE designated target (T'au's markerlit unit) drags the metric via closed-matrix collateral, but a bias that routes fire to a CLASS of high-value targets (Votann's on-objective enemies) spreads across the board and helps. The STRUCTURE of the bias — a class versus a single unit — decides, not the mere presence of a bias.
>
> **Validation.** Off byte-identical (0/36,960 flips); sc26a built by merging the Votann-scoped cells into sc25a (the gate cannot affect non-Votann games). run.py --cli clean; scripts/audit_rules.py passes.

> **T'AU MARKERLIGHT [IGNORES COVER] ADOPTED FIDELITY-FIRST (2026-07-01) — the missing half of a real army rule. New standing anchor `data/_anchor_sc25a_n80_log.json`, gated mean absolute error 2.49 (down from sc24a's 2.50, −0.01 — a marginal, faithful improvement). The first output of the per-faction target-selection batch; its key lesson is that the target-selection BIASES drag the metric while the RULES they expose are the value.**
>
> **The rule.** `SWEG_TAU_MARKERLIGHT_IGNORES_COVER` (default-on, `=0` byte-identical kill-switch) gives a Guided T'au ranged attack the [IGNORES COVER] ability when its Spotted target was marked by a MARKERLIGHT-keyword Observer unit (For the Greater Good, BSData rule id eeb6-c42-48f7-cc21). The sim already modelled the Guided +1-to-Hit but not the [IGNORES COVER] half, so T'au guided fire was still reduced by the target's cover save. Implemented as a third OR-arm on the ignore_cover boolean in Unit.attack (the Spotted set is `guided_los_enemy_uids`, built exclusively from MARKERLIGHT carriers in `Battle._run_markerlight_phase`). Cited `simulator.tau_markerlight_ignores_cover`.
>
> **The batch lesson — biases drag, rules help.** A companion AI target-bias (concentrate guided fire on the Spotted unit, following the Astartes-Oath pattern) was built and screened but REJECTED: T'au-scoped paired N=80 vs sc24a, rule-only gated 2.50 → 2.49 (T'au +1.46 toward its real 54.3), but rule+bias gated 2.50 → 2.51 (worse), because the bias OVER-CONCENTRATES fire — doubling the collateral (137 → 279 flips) with decisive Adeptus Mechanicus / Drukhari drops as T'au beats them harder. This is the closed-matrix symmetry: a target-selection bias that raises a near-50 faction's win rate necessarily lowers its opponents', washing or dragging the metric. The faithful RULE is the value; the piloting bias is not. This reframes the remaining batch — Leagues of Votann and Chaos Knights levers are pure biases (expect drag/wash); Adeptus Custodes has a real rule (Assemblage of Might).
>
> **Verification caught a fabrication.** The adversarial verify-design workflow confirmed the rule verbatim in BSData AND caught that the prior investigation's "excluding KROOT units" clause does not exist on For the Greater Good (a misread of the separate Markerlight Precision boarding-patrol rule); KROOT units that carry For the Greater Good are legitimately Guided, so there is no KROOT exclusion.
>
> **Validation.** Kill-switch reproduces sc24a byte-identical (0/36,960 flips); sc25a built by merging the T'au-scoped cells into sc24a (the gate cannot affect non-T'au games). run.py --cli clean; scripts/audit_rules.py passes.

> **DEEP-STRIKE MISPARSE TRIAGE ADOPTED FIDELITY-FIRST (2026-07-01) — a universal deploy-AI bug fixed at per-unit scope. New standing anchor `data/_anchor_sc24a_n80_log.json`, gated mean absolute error 2.50 (down from sc23a's 2.64, −0.14 — the best standing frame yet). Restores the Drukhari alpha-strike by deploying its fast skimmers/transports on-board Turn 1 as competitive play does.**
>
> **The bug.** BSData attaches the standard OPTIONAL Core Rules Deep Strike ability directly to a set of fast Aeldari/Drukhari skimmers, transports and gunships (Raider, Venom, Ravager, Hellions, Ynnari Raider/Venom, Falcon, plus Reaper/Tantalus Legends). Deep Strike is ALWAYS a player choice ("set it up in Reserves instead of on the battlefield"), but the simulator's faction-blind deploy AI (SWEG_DEPLOY_AI, simulator.py ~7404) routes every deep-strike-capable unit into strategic reserves on Turn 1 — so all three Drukhari Raiders plus their pre-embarked passengers, and the Ravager anti-tank platform, were parked in reserves Turn 1, cancelling the entire Skysplinter Assault alpha-strike (the dominant cause of Drukhari's under-pole).
>
> **The fix.** `SWEG_DEEP_STRIKE_TRIAGE` (default-on, `=0` byte-identical kill-switch) clears `deep_strike=False` for the nine confirmed units so the deploy AI keeps them on-board. It is a per-unit DEPLOYMENT-PILOTING correction expressed through the deep_strike flag (the only input the deploy AI reads) — NOT a claim the units lack the ability. The correction is scoped, not global: the deploy AI over-reserves EVERY faction's deep-strikers, but a global fix would also deploy over-poles (Emperor's Children wins about +17% more on-board, per the ledger) in the wrong direction — so it is scoped to the specific under-pole units it was diagnosed on (the same Principle-2 logic as the Astra Militarum advance-discipline). The more faithful long-term fix is a smarter deploy AI that keeps deep_strike=True and chooses on-board; this per-unit gate is the only-improves interim.
>
> **Adversarial verification caught fabricated citations.** A first draft cited invented ability names ("Webway Portal / Webway Assault / Webway Gate") that have zero occurrences in the BSData Aeldari Library. The adversarial BSData verification (`data/_deepstrike_verify_report.md`) confirmed the clears are behaviourally correct (every unit has the real unconditional Core Rules Deep Strike) but rewrote the citations honestly, and CLEAR-CONFIRMED two more Legends units (Reaper, Tantalus) while KEEPing the other eight uncertain candidates (Land Speeders, Wasp Assault Walker, Wazdakka Gutsmek — all genuine baseline deep strike).
>
> **Result.** Drukhari + Aeldari scoped paired N=80 vs sc23a: Drukhari +5.03 DECISIVE (45.2 → 50.3, alpha-strike restored), gated 2.64 → 2.50. Collateral is Drukhari's opponents losing to a correctly-played Drukhari (T'au −0.66, Astra Militarum −0.63 — small; the Astra Militarum under-pole nudge is dominated by Drukhari's toward-real move, net mean absolute error −0.14). Kill-switch `SWEG_DEEP_STRIKE_TRIAGE=0` reproduces sc23a byte-identical. sc24a built by merging the Drukhari+Aeldari cells into sc23a (the gate cannot affect non-Drukhari/Aeldari games). run.py --cli clean; scripts/audit_rules.py passes.

> **ASTRA MILITARUM ADVANCE-DISCIPLINE ADOPTED FIDELITY-FIRST (2026-07-01) — THE FIRST PER-FACTION AI LEVER. New standing anchor `data/_anchor_sc23a_n80_log.json`, gated mean absolute error 2.64 (down from sc22a's 2.91, −0.27 — the best standing frame yet). Proof-of-concept for the per-faction AI thesis: an army-scoped piloting improvement banks its lift without the durable-over-pole contamination that washes faction-agnostic levers.**
>
> **The lever.** `SWEG_AM_ADVANCE_DISCIPLINE` (default-on, `=0` byte-identical kill-switch) makes Astra Militarum gunline tanks (Leman Russ variants, Rogal Dorn: ranged damage per activation ≥ 2.0, range ≥ 18 inches) HOLD and shoot instead of Advancing-and-forfeiting-their-Shooting-phase when a damaging target is in range and they are seeking an objective. Cadian Shock Troops (ranged damage per activation ~0.5) stay below the gate and still Advance to grab objectives. Identical logic to the faction-agnostic `SWEG_ADVANCE_DISCIPLINE` (screened +2.19 harmful, retired to default-off), restricted to `faction == "Astra Militarum"` — a second entry point on the shared `_suppress_advance` block in `simulator.py` `_do_move`. Grounds in the core-rules fact that Advancing forfeits non-[ASSAULT] shooting; cited `simulator.am_advance_discipline`.
>
> **The Principle-2 proof.** Paired N=80 vs sc22a: Astra Militarum +5.75 DECISIVE (34.1 → 39.9, its shooting under-pole closing), gated 2.91 → 2.64. Death Guard flat (−0.14) — the faction-agnostic version had lifted Death Guard +6.90 (its durable Plagueburst over-scoring under the survivor-snapshot representation over-reward), which cancelled Astra Militarum's gain and washed the generic lever. Scoping to the built-for faction banks the lift with zero durable-over-pole contamination. This is the recipe the per-faction target-selection batch (T'au / Leagues of Votann / Adeptus Custodes / Chaos Knights) follows.
>
> **The dips were noise; the refinement was a trap.** The per-matchup only-improves check (`scripts/faction_row_delta.py`) flagged five Astra Militarum matchups dipping at N=80; sharpening to N=100 dissolved them (Adepta Sororitas and Imperial Knights flipped positive; Emperor's Children / Adeptus Custodes / Chaos Daemons shrank toward zero — the noise signature). A mechanistic Astra-Militarum-vs-Chaos-Daemons trace confirmed the largest (Chaos Daemons) is self-cancelling: 30 games worse (tanks hold 10–18 inches from centre objectives while Daemons score uncontested), 26 better (held fire kills enough Daemons to stop the swarm), net −4/200. A "advance when more than 1.5 Normal moves from the objective" refinement built to fix it was screened and REVERTED — it cost the entire lift (+5.75 → −2.00, gated 2.64 → 3.03), because the benefit IS the far-holding. Adopted the unrefined gate; chasing noise-scale dips regresses the frame.
>
> **Validation.** Kill-switch `SWEG_AM_ADVANCE_DISCIPLINE=0` reproduces sc22a byte-identical (0/36,960 flips). sc23a built by merging the Astra-Militarum-scoped ON cells into sc22a (the gate cannot affect non-Astra-Militarum games — verified: paired sc22a vs sc23a reproduces the scoped result cell-for-cell). run.py --cli clean; scripts/audit_rules.py passes. **NEXT: the per-faction target-selection batch** (T'au markerlight-ignores-cover, Votann objective-hit-bonus, Adeptus Custodes Assemblage of Might, Chaos Knights dread-cascade targeting — each army-scoped, only-improves-gated, following this recipe), plus the universal deepstrike-misparse fix (in progress).

> **AUDIT WAVE 2 ADOPTED FIDELITY-FIRST (2026-07-01). New standing anchor `data/_anchor_sc22a_n80_log.json`, gated mean absolute error 2.91 (down from the pre-audit HEAD 3.08, −0.17 — the best standing frame yet). Nine faithful over-pole corrections found by a seven-agent override/rule sweep, every high-value finding re-verified against BSData; adopted on the owner's "fidelity first on all of them" directive.**
>
> **What it fixed.** (1) A systematic mapper prose-walk bug — the "bestowed-Feel-No-Pain leak" — stamped an aura's GRANTED Feel No Pain onto the granter's own profile: Acolyte Iconward, Technomancer, Contorted Epitome (plus Typhus, whose override instead cited the non-existent army-wide Disgustingly Resilient). All reset to fnp 7 (`SWEG_AUDIT2_FNP_FABS`). (2) The C'tan Necrodermis rule was the 9th-edition "halve the Damage"; the 10th-edition rule is "subtract 1 from the Damage" (BSData shared profile `61c9-1b37-b067-44ba`) — the old code over-protected every C'tan (`SWEG_NECRODERMIS_MINUS_ONE`, `code/units.py`). (3) World Eaters Blood Tithe is the Khorne Daemonkin detachment rule, not army-wide; the sim's Berzerker Warband archetypes were getting it fabricated (`SWEG_WE_BLOOD_TITHE_DETACHMENT`; a wash — the wave-260 scoped version was already tiny). (4) The Imperial Knights Bondsman giver gate wrongly admitted Knight Castellan / Valiant / Canis Rex (`SWEG_IK_BONDSMAN_SCOPED`; BSData shows the other Questoris/Cerastus variants DO carry Bondsman, so the fix excludes exactly those three rather than the agent's over-narrow "five"). (5) Fabricated ranged devastating_wounds on the Heldrake/Master of Executions, fabricated sticky_objective on two Acolyte squads, and a chimeric Warscythe-plus-Dispersion-Shield invuln on Lychguard — all removed.**
>
> **The one fidelity-vs-metric reversal.** Wave-260's `SWEG_EC_LUCIUS_FNP_FIX` had DELETED Lucius the Eternal's real Feel No Pain 5+ (the 2025 Emperor's Children codex grants it — BSData infoLink `01a8-58fe-9d82-ae3c`; a forbidden-zone violation, a faithful stat nerfed to help the metric). It is now RESTORED (gate retired to default-off). This is the batch's only metric-negative move (Emperor's Children +0.90, non-decisive), adopted because the stat is real.**
>
> **Result.** Combined scoped N=80 vs the pre-audit HEAD: gated 3.08 → 2.91 (−0.17); Genestealer Cults −3.75 DECISIVE (toward real 46.7), Death Guard −1.46, Chaos Daemons −0.62. Every new gate byte-identical-off (0/36,960 flips). Two citation corrections (Technomancer "Canoptek Cloak" → real "Rites of Reanimation"; Daemonettes Fights First is unconditional, not detachment-gated — the gate is now marked do-not-adopt). Full findings and per-faction reports: `data/_audit2_consolidated.md` + `data/_audit2_<faction>.md`.**
>
> **PLAGUE MARINES / BLIGHTLORD FABRICATED FEEL NO PAIN REMOVED — ADOPTED FIDELITY-FIRST (2026-07-01). New standing anchor `data/_anchor_sc21a_n80_log.json`, gated mean absolute error 3.06 (down from sc20a's 3.26, −0.20). FAITHFUL AND metric-positive — rare for this project: it deflates the #1 over-pole by removing invented durability rather than nerfing a real stat.**
>
> **The bug:** `data/overrides.json` carried `fnp: 5` on `death_guard_plague_marines`, `chaos_space_marines_plague_marines`, and `death_guard_blightlord_terminators` — the army-wide-Disgustingly-Resilient-as-Feel-No-Pain fabrication the project deleted at iter-15, leaked back onto the per-datasheet overrides. The 10e Plague Marines and Blightlord Terminators datasheets carry NO Feel No Pain (verified against BSData — zero "Feel No Pain" in either datasheet window); Disgustingly Resilient is ONLY the 2-command-point Virulent Vectorium stratagem giving −1 Damage, which is modelled separately at `simulator.py:4070` (`_try_disgustingly_resilient`). Surfaced by the owner's catch that the −1 Damage is a stratagem, not a rule.
>
> **The fix:** `SWEG_DG_PLAGUE_FNP_FAITHFUL` (loader gate, Lucius `SWEG_EC_LUCIUS_FNP_FIX` pattern) forces `fnp=7` on exactly those three keys, default-on; `=0` is a kill-switch that restores `fnp=5` and reproduces the sc20a anchor byte-identically. Plaguebearers / Typhus / Mortarion / Poxwalkers / Deathshroud are untouched — they carry real Feel No Pain (or, for Deathshroud, none in either state). Verified surgical: a both-states catalogue diff flips ONLY the three intended keys.
>
> **Validation:** scoped Death-Guard + Chaos-Space-Marines N=80 paired vs sc20a (the change is localized to those two factions' datasheets, so the Chaos-Knights Tyrant-range work-in-progress is automatically excluded): gated 3.26 → 3.06 (−0.20); Death Guard −3.37 DECISIVE (62.8 → 59.5, toward its real 47.6); only collateral Grey Knights −0.46; all others flat. sc21a built by merging the scoped DG/CSM cells into sc20a (the gate cannot affect non-DG/CSM games, so the merge is an exact full-frame gate-on anchor — verified to reproduce the scoped result cell-for-cell).
>
> **MAPPER MULTI-WEAPON-CHASSIS FIX ADOPTED FIDELITY-FIRST (2026-06-30) — the simulator now fires units' REAL datasheet weapons. New standing anchor `data/_anchor_sc20a_n80_log.json`, gated mean absolute error 3.26 (a WASH: sc19a 3.22 → 3.26, +0.04 within noise). The fix is a pure Stage-1 data-correctness restoration, adopted because the sim should fire the real guns.**
>
> **The bug: the BSData mapper's single-model weapon picker stripped the main guns off ~69 multi-weapon chassis** (Titans, Knights, Dreadnoughts, super-heavy tanks, T'au battlesuit Commanders) — they fired one short-range gun instead of their full arsenal. THREE distinct bugs in `code/bsdata/mapper.py` `_collect_single_model_weapons` / `_gather_group_candidates`: (1) a union-by-shared-weapon-name clustering merged genuinely-independent weapon slots (a Knight's two arms + carapace) and kept only ONE — replaced with per-group `max`-selections picking; (2) `_gather_group_candidates` skipped entryLinks that resolve to shared Library *groups*, losing those candidates; (3) a choice group re-picked an already-equipped fixed weapon, and the ranged fallback was skipped when a unit had melee (the Forgefiend fired NOTHING). Found by piloting the Chaos Knights under-pole (the Knight Tyrant's missing volcano lance), then a catalogue-wide mapper audit.
>
> **Validation: 69 single-model units regained their real weapons, 0 weapon regressions, no over-gunning** (max 6/unit). T'au Commanders 1→5 guns, Forgefiend 0→1 (fires at last), Warlord Titan (Belicosa volcano cannon 120" + Sunfury 72"), Knight Despoiler, Riptide, Rogal Dorn, Wraithknight all restored. The frame is proven sound (all-faction battle sweep clean, no hang, memory healthy); the full test suite's 23 failures are PRE-EXISTING (fail identically on the old frame — synthetic-profile / non-deterministic tests, several don't import the mapper). `parsed.json` regenerated; two `tests/test_model_loadouts.py` Wraithknight tests updated to the faithful loadout.
>
> **Metric is a WASH.** Paired N=80 vs sc19a: gated mean absolute error 3.22 → 3.26 (+0.04, within noise). The only decisive mover is Aeldari −2.44 (its Wraithknight/Wraithlord now fire their full arsenal), deflating it toward its real 41.5. Otherwise the restored firepower roughly cancels — the affected multi-weapon chassis sit on both over- and under-poles, so the extra guns help and hurt symmetrically (the same shape as the artillery-hold and over-pole-audit results). Flip counts confirm the games genuinely change (151-515/faction). Adopted fidelity-first; sc20a promoted as the standing anchor.
>
> **Knight Tyrant — FIXED via `data/overrides.json`.** The picker resolves its 'Carapace weapons' group to no candidates, so the catalogue Tyrant fired only its 12-inch harpoon + meltagun. A `model_loadouts` override keeps those two real weapons and restores the dropped Gheiststrike missile launcher (72-inch carapace, real datasheet weapon): Chaos-Knights-scoped +0.72 (within noise, toward its real 44.7). Faithful and net-positive; a first attempt that swapped the harpoon for the volcano lance cost −1.4 (the harpoon is stronger) and was discarded. The Tyrant still under-fires because the move AI advances it — the metric-blocked advance-discipline piloting issue, not the loadout. **Harness note:** the N=80 re-anchor had to be run as two N=40 halves — the full 64-minute background job was killed twice by an environment governor on long-running tasks (the frame itself is fine).

> **ARTILLERY-HOLD ADOPTED FIDELITY-FIRST (2026-06-30) — new standing anchor `data/_anchor_sc19a_n80_log.json`, gated mean absolute error 3.22 (down from sc18a's 3.26). The metric move is a WASH (−0.05); the lever is kept because the piloting is faithful (no real player advances a Basilisk), and the wash is itself the third independent confirmation of the durable-survivor representation over-reward.**
>
> **`SWEG_ARTILLERY_HOLD` flipped default-on (`=0` kill-switch reproduces sc18a byte-identically — 0 of 18,480 games flip).** Dedicated indirect artillery (Basilisk / Manticore / Plagueburst Crawler / Hive Guard / Exorcist) HOLDS in the backfield and bombards every round instead of the default AI marching it forward into melee, where it forfeits its shooting (the Advance lockout) and is destroyed by round 3-4. The classifier `_dedicated_indirect_artillery` (`code/strategy.py`) is model-count-weighted indirect-dominant ranged output AND ranged > melee AND not a CHARACTER — faction-agnostic, validated to fire only for genuine artillery (it correctly excludes T'au Strike Teams carrying a Smart-Missile turret, War Dog Karnivores, and Dark Reapers).
>
> **Provenance + the forbidden-zone catch.** The lever was recovered from an abandoned, uncommitted `SWEG_AM_ARTILLERY_HOLD` that was hard-gated to `faction == "Astra Militarum"` — an EVAL_PROTOCOL §9 violation (no faction branch on an uncited AI heuristic). The AM-only version screened gated 3.26 → 2.82 (−0.44) but that frame was a list-fit and is superseded; de-faction-gating it (the faithful form) is what surfaced the wash.
>
> **The wash IS the finding — durable-survivor over-reward confirmation #3.** Paired N=80 vs sc18a: Astra Militarum +6.56 (27.7 → 34.3 — the dominant under-pole fills as its Basilisk/Manticore survive and bombard) but Death Guard +6.90 (56.9 → 63.8, gated ~6.7 → 13.66), because its single T10/W12/2+/Feel-No-Pain Plagueburst Crawler, held alive, over-scores under the survivor-snapshot representation over-reward — the two nearly cancel. Direct evidence for the mechanism: ONE durable Death Guard Plagueburst out-lifts AM's TWO Basilisk + Manticore, while the fragile Tyranid Hive Guard (×12) barely move the needle (+0.5). This extends the durability-over-reward ledger (after the pilot gates and the going-first campaign): a faithful piloting improvement that strengthens units regresses the metric by feeding the over-reward; the lever turns net-positive only once the over-reward is corrected. Adopted anyway, fidelity-first (user directive), same disposition class as the pinned `SWEG_ADVANCE_DISCIPLINE`.
>
> **The indirect-fire "no-line-of-sight bug" was investigated and DOES NOT EXIST.** Earlier framing (the 2026-06-29 handover) claimed Wyvern / Field Ordnance Battery cannot fire their mortars without line of sight because their `indirect_fire` flag is False. That is the CATALOGUE flag; the simulator uses the BUILD-TIME flag, which the army-builder re-derives from the unit's best-expected-value weapon (`units.py:5208`) — for Wyvern/Field Ordnance that is the indirect mortar/field gun, so the built flag is True and the sim already fires them indirect (targeting at `simulator.py:12101`, blind-fire −1 at `units.py:3128`). The build-time derivation is in fact MORE correct than the catalogue: it refuses a Cadian Heavy Weapons Squad's autocannons the indirect treatment the catalogue flag would wrongly grant. No fix needed. Cosmetic residual only: the catalogue flags are wrong for ~5 units (no behavioural effect) and a Strike Team that rolls the Smart-Missile turret as its best weapon gets a small over-credit — both too minor for a lever.
>
> **Harness fix (separate change): `scripts/evaluate_vs_meta.py` re-launch.** The `os.execvpe` self-re-launch (which pins `PYTHONHASHSEED=0` for reproducibility) SEGFAULTS on this box (exit 139, empty log — the Windows exec emulation is flaky when the child's stdout is redirected); this is the "intermittent — screens get killed" failure the handover noted. Replaced with a `subprocess.run` + propagate-exit-code re-launch. Workaround for older checkouts: preset `PYTHONHASHSEED=0` to skip the re-launch.

> **GOING-FIRST / TEMPO CAMPAIGN (2026-06-29) — STRUCTURAL FLOOR DECLARED on the tempo axis. Standing anchor unchanged: `data/_anchor_sc18a_n80_log.json`, gated mean absolute error 3.26 (the Feel No Pain fabrication batch, post-wave-260). No new adoption — the campaign is a documented dead-end declaration so no future session re-burns it.**
>
> **The goal of this campaign** was to attack the dominant systematic faithfulness gap — the simulator's going-first win rate is ~69% (N=150) versus a real Chapter Approved 2025-26 target of 49–52% — by building a faithful going-second mechanism stacked with whatever over-pole correction it needed. The user authorised a full multi-wave stack campaign, then a cheap-probe-first de-risk.
>
> **VERDICT: gated 3.26 is the structural floor on the tempo axis, re-confirming the durability-axis floor (`docs/DURABILITY_OVERREWARD_INVESTIGATION.md`).** The going-first over-reward IS the durability over-reward viewed through tempo: every faithful going-second mechanism reduces going-first but regresses the headline by transferring wins to the durable over-poles. Three independent mechanisms, all paired vs sc18a at N=80:
> * **`SWEG_OVERWATCH_MOVE`** (Fire Overwatch on a Normal/Advance move — completes a verbatim-cited trigger the code only honoured on charges/reserves): **+0.40 worse**. Deflates aggressors (Death Guard −1.5, Chaos Daemons −1.3) but inflates the durable holders that do the overwatching (Emperor's Children +3.2 decisive).
> * **`SWEG_KITE_MOVE`** (objective-aware kiting retreat): **+1.12 worse**. Shooters retreat → durable melee walks onto objectives (Imperial Knights, Thousand Sons, Tyranids up).
> * **`SWEG_PROBE_RESERVE`** (general Strategic Reserves de-risk probe — reserve ~half, arrive Round 2+ to counter-attack): **+1.67 worse** (the worst). Inflates the durable Knights over-poles massively (Imperial Knights +12.6, Chaos Knights +14.4, Orks +15.9), craters gunlines (T'au −7.6, Chaos Space Marines −8.1). Probe gate removed after measuring (rejected).
>
> **The per-map check (prompted by the user: "are we testing the full board range?") found the over-reward is BROAD, not a single bad map.** Both the screens and `scripts/diag_signatures` rotate the five-map `PARIAH_NEXUS_2K_ROTATION` plus the ten-card primary deck. At N=60/map going-first is 64.9–84.5% on ALL five deployments (`scripts/diag_going_first_by_map.py`; worst Hammer and Anvil 84.5%, amplified by its 36-inch travel distance; best Crucible 64.9%). The reserve probe cut the spread maps (Hammer and Anvil 84.5→63.2) but back-fired on compact maps (Tipping Point +6) — flavor-specific, not a global fix, and MAE-negative regardless.
>
> **Shipped this campaign (all default-off, byte-identical, frame unchanged — no re-anchor):** `SWEG_OVERWATCH_MOVE` kept built+held as a genuine fidelity correction (rides on the existing `simulator.fire_overwatch` citation); `scripts/diag_going_first_by_map.py` registered in the toolbox; the Sweeping Engagement deployment restored at the correct 2000-point 44×60 footprint behind `SWEG_FULL_DEPLOY_ROTATION` (default-off) so the full five-deployment set can be screened in a future re-anchor (the stock map was 44×90, wrong size for 2K, and so was excluded); the lost `SWEG_CK_DOOM` lever recovered. `SWEG_PROBE_RESERVE` removed (rejected probe). run.py --cli clean; audit clean.
>
> **NEXT (open, all require a deliberate re-anchor — none is autonomous lever-hunting):** (1) adopt the six-map full deployment rotation (`SWEG_FULL_DEPLOY_ROTATION=1`) — re-bases the metric, screen at N=80; (2) the over-pole representation re-model remains retired/structural; (3) the going-first axis is now a declared floor — do NOT re-propose going-second AI mechanisms (kiting, overwatch, reserves all measured and reverted).

> **WAVE 260 (2026-06-26, hands-off ultracode) — OVER-POLE OVER-CREDIT AUDIT + REMOVAL. Eight unfaithful over-credits removed (all adopted default-on, fidelity-first); the metric is a WASH, which is the headline finding: it confirms the over-pole residual is STRUCTURAL, not ability-driven. NEW standing anchor `data/_anchor_sc17a_n80_log.json` — gated mean absolute error 3.45 (raw 6.20; scoped 3.48 → 3.45, the full re-anchor confirmed it cell-for-cell).** Branch `claude/sim-calibration-16` (fresh off -15's tip `2803388`; pull request #82 still open, no §H fold needed — `origin/main`'s only delta is the #81 merge whose content -15 already carries, `git diff` vs the merge-base empty). NOT pushed.
>
> An adversarial multi-agent over-pole audit (`docs/OVERPOLE_UNIT_AUDIT.md`, mirroring the under-pole audit `w6adydjru` that fed waves 254-259) hunted unfaithful OVER-credits on the +8..+14 over-poles (World Eaters, Emperor's Children, Chaos Daemons, Necrons, Death Guard, Adeptus Custodes): **27 candidates verified, 9 survivors, 17 refuted.** The refutations caught universal-approximation false-positives (the army-wide aura / phase→round transient conventions, uniform across all factions — not selectively nerfable), command-point-reallocation backfires (the Necron Conquering Tyrant family), and false rule bases (Chaos Spawn Feel No Pain 5+ is real BSData; the Emperor's Children Keeper of Secrets cross-faction aura is codex-correct). The user authorised building Waves A+B (8 levers); Wave C (Adeptus Custodes Arcane Genetic Alchemy) is HELD.
>
> **ADOPTED (all default-on, `=0` kill-switches), each a faithful narrowing-to-the-cited-rule or fabrication removal:** World Eaters Blood Tithe Lethal Hits scoped to one unit one phase (`SWEG_WE_BLOOD_TITHE_SCOPED`); World Eaters Blessings of Khorne gated to units carrying the ability (`SWEG_WE_BLESSINGS_ABILITY_GATE`, excludes the three daemon-ally datasheets via a new `has_blessings_of_khorne` mapper flag); Death Guard Typhus Destroyer Hive Feel No Pain proxy restricted to melee (`SWEG_DG_TYPHUS_MELEE_ONLY`); Death Guard Lord of Contagion +1-to-wound restricted to melee weapons (`SWEG_DG_CONTAGION_MELEE_WOUND`); Chaos Daemons Skarbrand Rage Embodied +1 Attack restricted to melee (`SWEG_DAEMONS_SKARBRAND_MELEE`); Emperor's Children Lucius the Eternal fabricated Feel No Pain removed (`SWEG_EC_LUCIUS_FNP_FIX`, mapper prose-walk artifact, no citation, same class as Azrael/Celestine/Lion); Death Guard Plague Company fabricated Necron-protocol placeholder emptied (`SWEG_DG_PLAGUE_NO_NECRON_PROTOCOLS`, a Rule-10 + Rule-13 fabrication firing via a silent NECRONS-keyword fallback); Chaos Daemons Daemonic Invulnerability flat-4+-for-the-round over-credit removed with the command-point spend preserved (`SWEG_DAEMONS_INVULN_FAITHFUL`).
>
> **THE RESULT IS A WASH (the key finding).** Combined scoped paired N=80 vs sc16a: gated 3.48 → 3.45 (−0.02). The four target over-poles barely moved despite heavy lever churn — World Eaters 58.8→58.7 (133 flips), Death Guard 57.1→56.9 (270 flips), Chaos Daemons 63.1→63.2, Emperor's Children 65.3→64.8 (−0.53, non-decisive). The over-credits are REAL (byte-identity + the flip counts confirm the levers fire often) but NOT load-bearing for the win rate. **This is a clean A/B confirmation that the elite/melee over-pole cluster is STRUCTURAL (per-model representation / durability-in-contest scoring), not an ability-over-credit gap** — extending the decision ledger's melee-cluster conclusion to the elite over-poles. Both-off validation reproduced sc16a EXACTLY (0/36960 flips). Audit clean; run.py --cli clean.
>
> **FOLLOW-UPS carried (cited in `docs/OVERPOLE_UNIT_AUDIT.md`):** (1) Daemonic Invulnerability gate-on spends the command point for zero effect (no reactive-invuln-reroll mechanism exists) — adopted as net-more-faithful than the flat-4+ over-credit, but build the reactive single-failed-invulnerable-save re-roll and make the firing reactive so no command point is wasted. (2) Typhus melee Feel No Pain is an interim proxy for the real melee-only −1-to-Hit; build the target-side hit-penalty when available.
>
> **STRATEGIC STATE — both poles are now confirmed structural.** The Astra Militarum under-pole (durability-in-contest wall) and the elite/melee over-pole (per-model representation) are both structural, and the faithful-lever families that move them autonomously are increasingly exhausted: this audit removed the last clean ability-over-credits and the metric did not move. The remaining addressable work is the PARKED structural / representation re-model — a user decision (authorise the representation work, or accept the current fidelity floor at gated 3.45). Autonomous faithful-lever candidates on the over-pole side are now thin; see `docs/NEXT_SESSION_HANDOVER.md`.

> **WAVE 259 (2026-06-26, hands-off ultracode) — "ALL THE FIXES" CAMPAIGN CLOSED + RE-ANCHORED. NEW standing anchor `data/_anchor_sc16a_n80_log.json` — gated mean absolute error 3.48 (raw 6.27, 9/22 factions in band).** The user-authorised campaign built all 13 verified survivors of the Wahapedia-primary under-pole audit (`underpole-unit-audit`, run `w6adydjru`). Final disposition: **7 adopted, 2 held, 3 refuted (Chaos Knights), 0 phantoms shipped.** Full combined frame: **4.20 (wave 253) → 3.48** — a −0.72 campaign improvement; the per-cluster isolated deltas summed to ~3.50, matching the combined 3.48 (clean stacking, minimal cross-interaction). **T'au Empire (47.4 → 50.5, gap −3.8) and Leagues of Votann (44.2 → 47.6, gap −0.5) brought INTO the noise band; Tyranids also moved in.** The durable over-poles deflated toward reality (Imperial Knights 55.4 → 54.5, Death Guard 57.9 → 57.1, Aeldari 49.3 → 47.8, all from the T'au anti-armour and tie-draw effects).
>
> **ADOPTED (all default-on, `=0` kill-switches):** T'au Markerlights line-of-sight (254, −0.24); Astra Militarum Cadian Shock Troops sticky-objective (255, +1.87 / −0.10); T'au Crisis Sunforge/Hammerhead anti-armour (256, +3.26 / −0.22) + Crisis battlesuit mapper-gap weapons (256, data-correctness); Leagues of Votann native re-roll-Hit-1 (257, +3.77, into band) + Hekaton MultiCOG Targeting (257, correctness); Drukhari multi-unit Empower + per-datasheet Pain abilities (258, +1.87 / −0.10).
>
> **HELD (built, faithful, cited, default-off, washed at screen):** Astra Militarum Duty-and-Honour Order (+0.55 non-decisive, 126-flip churn); Leagues of Votann Kahl Kindred Hero [LETHAL HITS] (−0.12 wash — the reroll-1s proxy is a better-calibrated compensating error). Both kept in code for re-confirmation; do NOT re-propose as fresh.
>
> **REFUTED — Chaos Knights cluster (built + screened + STASHED `ck-cluster-refuted-wave259`, none adopted).** The audit's three hypotheses for the Chaos-Knights-under-while-same-chassis-Imperial-Knights-over gap ALL FAILED: Infernal Lance / Malefic Surge `SWEG_CK_INFERNAL_LANCE` — **−8.66 DECISIVE WORSE** (the screen first mis-fired because the curated Chaos Knights archetype pins Iconoclast Fiefdom; once the resolve override forced Infernal Lance, swapping detachments CRATERS Chaos Knights — Iconoclast's Dread Tyrants re-roll aura is far stronger and the Malefic Surge self-mortal-wounds outweigh the [SUSTAINED HITS 1] gain; the minimal model covers only the Diabolic Power leg + the mandatory downside, but −8.66 is decisive); Knight Abominant Warp Storms + Vortex Terrors `SWEG_CK_ABOMINANT` (−0.28 wash); War Dog datasheet abilities `SWEG_CK_WARDOG_ABILITIES` (−0.00, 400-flip churn for zero net). **The Chaos Knights under-pole (−7.2) is NOT a detachment/ability-modelling gap — it is structural (composition or representation) and remains the open Chaos Knights question.**
>
> **THE DOMINANT RESIDUAL is now Astra Militarum (26.8, gated 15.27)** — the Cadian sticky-objective lifted it +0.9 (the first faithful lever to move it on its real win condition since the structural-wall conclusion), but the durability-in-contest wall persists. The over-pole half is the next-largest residual (World Eaters +13.9, Emperor's Children +12.0, Chaos Daemons +10.5, Necrons +9.4). Audit clean; run.py --cli clean. **Waves 254-258 are ready for push (stacked on the open pull request #81 / `claude/sim-calibration-14`).**
>
> **WAVE 258 (2026-06-26, hands-off ultracode "all the fixes" campaign, wave 5) — Drukhari Power from Pain made faithful. Multi-unit Empower + per-datasheet Pain abilities ADOPTED (`SWEG_DRUKHARI_PFP_MULTISPEND` + `SWEG_DRUKHARI_PFP_PERDATASHEET` default-on, `=0` kill-switches).** The wave-246 model Empowered ONE unit per round with a generic [LETHAL HITS]; the real rule has NO per-phase cap and each unit has its OWN printed Pain ability. Adopted both faithful upgrades: multi-spend (loop while the Pain-token pool holds, descending damage-per-activation, with the Cronos Pain Engine D6 5+ token refund) and per-datasheet abilities (Archon full Hit-roll re-roll, Kabalite Warriors re-roll-Wound-1, Incubi Devastating Wounds vs INFANTRY, Lelith Hesperax +1 Strength/+1 Armour Penetration melee, Wracks set-Attacks-to-3; the Ravager's Agonising Suppression is deferred to the lethal-hits fallback because it needs cross-round target-side plumbing). Paired N=80 Drukhari-scoped: **Drukhari +1.87 (43.2 -> 45.1) toward its real 52.4, gated mean absolute error 3.96 -> 3.86 (-0.10)** — rank 5 multispend (+1.75) the driver, rank 9 per-datasheet (+0.72). Non-decisive at scoped N (Drukhari is a high-variance glass-cannon), but all three arms positive and these are faithful corrections of an acknowledged approximation, so adopted; the campaign-end re-anchor confirms the combined number. Both-OFF validation reproduced sc15a EXACTLY (0/36960 flips). One cluster remains: Chaos Knights (Infernal Lance, Abominant, War Dogs). Audit clean; run.py --cli clean.**
>
> **WAVE 257 (2026-06-25, hands-off ultracode "all the fixes" campaign, wave 4) — Leagues of Votann accuracy fidelity. Native re-roll-Hit-1 ADOPTED (`SWEG_VOTANN_NATIVE_REROLL` default-on); Hekaton MultiCOG Targeting ADOPTED as a data-correctness fix (`SWEG_VOTANN_HEKATON_MULTICOG` default-on); Kahl [LETHAL HITS] BUILT + held default-off.** The per-datasheet native re-roll-a-Hit-roll-of-1 (Hearthkyn Panspectral Scanning, Hekaton Panspectral Scanner, Einhyr Decisive Destruction), previously granted only to the Kahl-led Hearthkyn squad, is the decisive lever: paired N=80 Votann-scoped, **Leagues of Votann +3.77 (44.2 -> 47.9), into the noise band of its real 48.0**, gated mean absolute error 3.96 -> 3.92 in isolation. The Hekaton MultiCOG Targeting fix (the simulator was wrongly applying the damaged -1-to-Hit to the Hekaton's ranged attacks, which the rule forbids) was metric-inert (2 game flips) but adopted for correctness — the reverse of the refuted turret-weapon phantom. The Kahl's faithful Kindred Hero [LETHAL HITS] ability was BUILT but HELD default-off: paired Votann-scoped -0.12 wash (186 flips) — the faithful lethal-hits does not beat the better-calibrated reroll-1s proxy (a compensating error), so the proxy is retained. CLEAN ATTRIBUTION: rank 11 alone (+3.77 decisive) OUTPERFORMED the full 3-lever package (+1.86 non-decisive), so only the decisive driver plus the harmless correctness fix were adopted. Adoptions so far (each measured vs the sc15a anchor in isolation): T'au line-of-sight (wave 254, baked into sc15a 3.96), Astra Militarum sticky-objective (-0.10), T'au anti-armour (-0.22), Votann native re-roll (-0.04, Votann now in-band: 7 -> 8 factions inside noise). The TRUE combined gated mean absolute error is confirmed at the campaign-end full re-anchor. Audit clean; run.py --cli clean.**
>
> **WAVE 256 (2026-06-25, hands-off ultracode "all the fixes" campaign, wave 3) — T'au Empire weapon + ability fidelity. Crisis Sunforge / Hammerhead anti-armour abilities ADOPTED (`SWEG_TAU_SUNFORGE_HAMMERHEAD_ABILITIES` default-on, `=0` kill-switch); Crisis battlesuit mapper-gap weapons ADOPTED as a data-correctness fix (`SWEG_TAU_BATTLESUIT_WEAPONS` default-on).** The rank-4 anti-armour abilities — Sunforge (re-roll the Wound and Damage rolls vs MONSTER/VEHICLE, ranged), Hammerhead Armour Hunter (+1 to Hit vs MONSTER/VEHICLE) and Targeting Array (one Hit-or-Wound re-roll per shooting activation), all verbatim on Wahapedia — lift T'au strongly: paired N=80 T'au-scoped, **T'au +3.26 (47.4 -> 50.7) decisive, gated mean absolute error 3.96 -> 3.74 (-0.22)**, with ideal collateral (the anti-armour output deflates the durable over-poles Imperial Knights -0.87 and Death Guard -0.88 toward reality while raising the T'au under-pole). The both-OFF validation reproduced sc15a EXACTLY (0/36960 flips). The rank-3 Crisis battlesuit weapons (Fireknife missile pod + Sunforge second fusion blaster, dropped by the BSData mapper) were metric-neutral (T'au +0.05) but adopted as a faithful data-correctness fix — the units genuinely carry these weapons. Campaign running total: gated mean absolute error 4.20 (wave 253) -> 3.74; adopted T'au line-of-sight (254), Astra Militarum sticky-objective (255), T'au anti-armour + battlesuit weapons (256). Full combined re-anchor at campaign end. Audit clean; run.py --cli clean.**
>
> **WAVE 255 (2026-06-25, hands-off ultracode "all the fixes" campaign, wave 2) — Astra Militarum objective-control fidelity. Cadian Shock Troops sticky-objective ADOPTED (`SWEG_AM_STICKY_CADIAN` default-on, `=0` kill-switch); 'Duty and Honour!' Order BUILT + held default-off.** The Cadian Shock Troops datasheet 'Shock Troops' sticky-objective ability (a held marker stays controlled after the unit leaves, until the opponent takes it — Wahapedia Cadian-Shock-Troops) was absent on every AM unit despite the simulator fully implementing the sticky_objective mechanic (Necron Warriors / Genestealer Cults Acolytes use it). Adopting it: paired N=80 AM-scoped, **Astra Militarum +1.87 (25.9 -> 27.8) decisive, gated mean absolute error 3.96 -> 3.86 (-0.10)**; the both-OFF validation reproduced the sc15a anchor EXACTLY (0/36960 flips, proving the new code byte-identical off). This is the FIRST faithful lever to move the dominant Astra Militarum under-pole on its real win condition (objective-holding) since the structural-wall conclusion. The 'Duty and Honour!' Order (+1 Objective Control, the 5th Voice-of-Command Order, previously unmodelled) was BUILT + cited but HELD default-off: paired AM-scoped +0.55 NON-decisive (CI ±1.74, 126 flips of churn) — faithful but metric-neutral, the Objective-Control channel does not overcome AM's durability wall (same class as the wave-248 order add-ons). Full combined re-anchor at campaign end. Audit clean; run.py --cli clean.**
>
> **WAVE 254 (2026-06-25, hands-off ultracode) — T'au Markerlights line-of-sight CONSUMER gate-read completed; the first lever of the user-authorised "all the fixes" Wahapedia-primary under-pole campaign. `SWEG_TAU_MARKERLIGHT_BASE_LOS` consumer flipped default-on (`=0` kill-switch).** Wave 252 made the PRODUCER (`simulator.py:12882`) build the line-of-sight Spotted set default-on, but left the CONSUMER gate-read (`units.py:2853`) at `== "1"` (default-off) — so the line-of-sight Marked set was built every Shooting phase and silently DISCARDED at the production default, and the wave-252 T'au gain came entirely from the all-detachment fix, never from this set. Faithful per the verbatim cited rule (`simulator.tau_markerlight_base_los`): the +1 Ballistic Skill is conditioned purely on "targeting an enemy unit that is visible to one or more friendly MARKERLIGHT units", with NO per-attacker to-hit roll — the legacy `guided_enemy_uids` set gated the buff behind a markerlight-carrier hit roll the rule does not have (an under-credit). Paired N=80 (T'au-scoped, merged into sc14a): **4.20 → 3.96, delta −0.24; T'au +3.81 (43.6 → 47.4)** toward real 54.3. `=0` reproduces the sc14a anchor EXACTLY (0 flips). Collateral is faithful: over-poles deflate (Aeldari −1.04, Tyranids −1.15 toward reality); under-poles Astra Militarum −0.71 and Chaos Knights −0.78 deepen because T'au genuinely beats them more — chased on their own merits, NOT by nerfing T'au. The audit's LEG B (broaden the Observer pool to any For-the-Greater-Good unit) was REJECTED as contradicting the verbatim "MARKERLIGHT units" wording. New standing anchor sc15a (full N=80, gated ~3.96) in flight. Audit clean; run.py --cli clean. THIS IS WAVE 1 of the full "all the fixes" campaign — building all 13 verified survivors of the Wahapedia-primary under-pole audit (`underpole-unit-audit`, run `w6adydjru`); the remaining levers span Astra Militarum (sticky-objective + Duty-and-Honour order), T'au (battlesuit/vehicle weapons + abilities), Chaos Knights (Infernal Lance + Abominant + War Dogs), Drukhari (Power-from-Pain spend), and Leagues of Votann (Kahl Lethal Hits + native re-roll + Hekaton MultiCOG).**
>
> **WAVE 253 (2026-06-23, hands-off ultracode) — RE-ANCHORED on fresh branch `claude/sim-calibration-14`. The equal-victory-point tie-draw fix ADOPTED (flipped default-on). New standing anchor `data/_anchor_sc14a_n80_log.json` — gated mean absolute error 4.20.**
> **`SWEG_VP_TIE_DRAW` flipped default-on (`=0` kill-switch).** Chapter Approved 2025-26 Determine Victor: "If the players are tied, the battle is a draw." The simulator had awarded tied-victory-point games to the side with >1.10x surviving points — an unfaithful tiebreaker that systematically inflated durable elites / tabling armies. Removing it is a clean common-random-number paired improvement of **4.86 → 4.20, delta −0.66** (identical games, only the tied-game call differs; all 22 factions moved with 95% confidence-interval clear of 0). Decisive deflation of the over-poles toward reality: Death Guard −3.3, Chaos Daemons −2.7, Adeptus Custodes −2.5, Imperial Knights −2.4, Grey Knights −2.2, Thousand Sons −2.2, Chaos Knights −1.9, World Eaters −1.8, Emperor's Children −1.8; **Astra Militarum held (−0.2)**, the under-pole untouched. Pre-adoption base preserved as `data/_anchor_sc14a_base_off_n80_log.json` (4.86). #80 (waves 248–252) is MERGED into main; this branch is the fresh §14 rolling start.
>
> **THE WAVE-253 SWEEP (combat-model / scoring fidelity, hands-off) — tie-draw is the LONE win; everything else washed or regressed.** The leverage on the dominant residual is narrowly "remove durable-survivor over-credit" (the tie-draw); these all screened and were REJECTED (faithful but the wrong direction, kept code-only on worktrees, none adopted): round-5 going-second end-of-turn scoring `SWEG_R5_SECOND_LAST` (+0.35 — faithful per Chapter Approved 2025-26, but it INFLATES durable round-5 survivors; the sim's command-phase round-5 scoring is a load-bearing compensating error — CORRECTS the wave-252 "round-5 already modelled" phantom-kill: the turn-order was modelled but the scoring-timing was not); melee defender wound-allocation continuation `SWEG_MELEE_DEFENDER_ALLOC` (+0.42 — defender-optimising allocator helps everyone symmetrically); cover save-cap `SWEG_COVER_SAVECAP` (+0.01 — real ±1-save-cap bug but too rare to matter); variable-Attacks roll `SWEG_ROLLATK` (+0.18 — regresses to the mean like rolled-damage); secondary-denial movement-intent `SWEG_SECONDARY_DENY` (+0.45 — widens the melee/shooting spread); and the Astra Militarum Leman Russ **Armoured Spearhead** lift `SWEG_AM_SPEARHEAD` (+0.06 — faithful and correctly scoped via BSData to the Leman Russ Battle Tank only, but the extra tank hits do not convert to wins; AM's under-representation is NOT its tank's missing re-roll). The workflow's `[TURRET WEAPON]` +1-to-hit candidate was a PHANTOM (a mis-recalled 9th-edition Grinding Advance; the 10e datasheet has no such ability) — caught by Rule-10 Wahapedia grounding.
>
> **HELD, NOT adopted: the first-player RNG-stream fix `SWEG_FIRSTPLAYER_STABLE`** (built + proven on a worktree). It draws the round-1 first player from an independent matchup+seed stream so screening is robust to upstream-random perturbation — but it is NOT mean-absolute-error-neutral (the headline is first-player-sensitive by ~0.7 because going-first is over-rewarded; the stable-stream baseline reads ~5.55 vs the production 4.86). It adopts only ALONGSIDE a working going-first fix, which we do not have. Use it (set `SWEG_FIRSTPLAYER_STABLE=1`) only when screening deployment-class levers that perturb pre-roll-off random.
>
> **THE DOMINANT RESIDUAL is now the Astra Militarum UNDER-pole (~26.5).** The tie-draw dented the over-pole half; the under-pole half is untouched — no fresh faithful lever lifts it (the secondary over-count and the command-phase round-5 scoring are compensating errors that prop it; a faithful Leman Russ buff does not move it). Further progress needs a different strategy (list / representation work the owner has parked) — a user decision, not autonomous lever-hunting. Full session detail in `data/_melee_shooting_am_crack.md` + `data/_secondary_vp_diagnosis.md`.

> **WAVE 252 (2026-06-20, hands-off ultracode) — RE-ANCHORED. Faithful fidelity batch + Challenger catch-up cards ADOPTED (flipped default-on); the "off-table representation floor" framing is RETRACTED.**
> **New standing anchor `data/_anchor_sc13a_n80_log.json` — gated mean absolute error 4.86, N=80.** It replaces sc12a, which was found stale (see below). The pre-adoption true base (all gates off, current code, N=80) is preserved as `data/_anchor_sc13a_base_off_n80_log.json` — gated 5.89. The adopted frame is a clean common-random-number, deterministic improvement of **5.89 → 4.86, delta −1.04** (both arms current code, paired on faction/faction/seed).
>
> **What landed (all rules-faithful + cited OR genuine AI-play improvements; none nerf the AI to fit stats; all were gated default-off and OFF byte-identical, now flipped default-on):** Grey Knights Fury-of-Titan deep-strike-turn gate; Aeldari Fire-and-Fade re-roll proxy removal; tabling victory-point fix; Creeping-affliction timing; T'au markerlight all-detachment + base-line-of-sight; Drukhari Succubus sustained + Power-from-Pain embarked; Votann prioritised-efficiency; and the **Challenger catch-up card pool** (the missing comeback mechanism — the unrefined draw weights were chosen; the `_CHALLENGER_DRAW_WEIGHTS` refinement was a wash and NOT adopted). Held default-off pending a grounding decision: the two Astra Militarum levers (`SWEG_AM_REALISM`, `SWEG_AM_RECON`).
>
> **Decisive per-faction movers (adopted vs true base, clean paired):** Grey Knights −9.3, Tyranids −4.1, Genestealer Cults −4.1, T'au −3.7, Adeptus Mechanicus −3.5, Orks −3.2, Drukhari −2.7, World Eaters −2.5, Emperor's Children −1.9 (over-poles eased); **Astra Militarum +4.4** (the under-pole sink finally absorbing). Collateral is modest: Astartes +2.0, Imperial Knights +2.1, Chaos Knights +3.7.
>
> **THE REFRAME (user-led, vindicated):** Stage 1 is NOT at a floor — the target is real-game data, so a residual means a missing or mis-modelled mechanism, not an irreducible representation limit. The prior "off-table durability-in-contest representation wall" conclusion is RETRACTED. The residual is systematic AI-play-quality + tempo: going-first win rate 66% (real ~50%), secondary victory-point over-scoring, no attrition trajectory, and (now fixed) no comeback-card mechanism. The Challenger-cards adoption directly attacks the going-first over-reward.
>
> **Eval integrity (root-caused this session):** the common-random-number eval is **deterministic** (two identical current-code OFF runs flip 0/18480 games). sc12a was a **stale** baseline — it predates current code and flips 41% of games versus a fresh current-code OFF run, but those flips are near-symmetric so the aggregate drift is only **+0.11** (sc12a 5.78 was ~0.11 optimistic versus the true base 5.89). All prior in-session deltas measured against sc12a are therefore good to ~0.1. **FOLLOW-UPS (non-blocking):** (1) a ~0.3 cross-working-directory discrepancy (the adopted frame read 4.57 in a worktree vs 4.86 in main — main is authoritative; cause unexplained, likely a stale compiled artifact in the worktree); (2) add a standing "anchor reproduces against current code" check so an anchor cannot silently drift again.
>
> **Refuted this session (kept code-only on worktrees, NOT adopted — no nerfing, just did not help):** AI-movement levers — kiting/`SWEG_KITE_MOVE` (moves going-first 74→56 but +0.70 to the headline at N=80), deployment-quality (barely moves going-first: 61.5% worst-case vs 66% default), charge-first-strike (+0.16, near-wash); overwatch-bait built but unscreened. **Phantoms killed (do NOT rebuild):** loser-displacement (invented rule), Battle-shock-rate & melee-lethality (faithful-by-construction), round-5 second-player-last-turn (already modelled at `simulator.py:9789` + `:706-709`).

> **WAVE 251 (2026-06-15, autonomous hands-off) — melee/AM on-table avenue CLOSED; two held fidelity fixes banked.**
> Two threads. **(1) The melee over-pole / Astra Militarum under-pole (~70 of the 5.78 MAE) is now
> conclusively the OFF-TABLE durability-in-contest representation/scoring wall — needs user auth, parked.**
> The last two on-table leads died: cover asymmetry REFUTED (`diag_cover`: a symmetric ~46% output tax,
> over-pole defenders 46.5% vs field 46.1%, not skewed); a defensive screening move-AI BUILT + REJECTED
> (`SWEG_SCREEN_AI`, `diag_screen_ab`: fires actively but decisively wrong-direction — AM 25.0→16.7,
> every melee over-pole up; AM's cheap bodies ARE its objective-holders so screening cedes the board).
> That is FIVE on-table AI levers refuted (mass-bodies, mass-tanks, gunline-hold, focus-fire, screening)
> plus cover — the on-table lever family is exhausted. **(2) Fabrication-removal on addressable over-poles
> (the auth-free path).** Aeldari +7.52 is NOT the representation wall (63.7 models) → addressable: Fire
> and Fade fabricated re-roll proxy removed (`SWEG_AELDARI_FNF_FAITHFUL`, `bdd531d`), Aeldari 52.2→50.2
> (paired −1.93), headline 5.78→5.74. A bounded triage then ranked Grey Knights #1: Teleport Strike Force
> applied Fury of Titan army-wide always-on instead of the codex deep-strike-turn gate — gated fix
> (`SWEG_GK_FURY_FAITHFUL`, `4134e9c`), GK 55.7→46.2 (paired −9.48 decisive), headline 5.78→5.66. Both
> KEPT fidelity-first, default-OFF, OFF byte-identical (sim_motion_proof unchanged), **held for the next
> re-anchor.** Standing anchor sc12a (gated 5.78) UNCHANGED (both fixes default-off). Wave-250 list-realism
> rebuilds (Emperor's Children / Death Guard / Chaos Daemons) remain queued in `docs/WAVE250_LIST_REALISM.md`.

**BRANCH `claude/sim-calibration-13` (2026-06-13) — wave 252 CLOSED and PUSHED (`origin/claude/sim-calibration-13` at `bc9159d`, in sync). The wave-247 checkpoint is pull request #79 (waves 247 + 248 part one, OPEN, pending Ed's review); `claude/sim-calibration-13` is the rolling branch stacked on its head (`5b312b5`). Fold origin/main per procedure §H when #79 merges.**

**⚠️ §14 ROLLING-BRANCH CHECKPOINT DUE (raised wave 252).** Both thresholds are exceeded: 21 commits versus origin/main and ~2,938 hand-written lines versus origin/main (the bulk is the stack on the still-open #79, not new debt). Per the standing sim-calibration-6→7 pattern: once #79 merges, fold origin/main and start wave 253 on a fresh `claude/sim-calibration-14`. Do NOT keep stacking new waves on -13 beyond what is in flight.

**STANDING ANCHOR (wave 252): `data/_anchor_sc13a_n80_log.json` — gated 4.86, N=80 (the wave-252 flipped frame: the faithful fidelity batch + Challenger catch-up cards, all flipped default-on, on top of the sc12a frame). Pre-adoption true base preserved as `data/_anchor_sc13a_base_off_n80_log.json` — gated 5.89. Clean paired improvement 5.89 → 4.86 (delta −1.04).**

**RETIRED ANCHOR: `data/_anchor_sc12a_n80_log.json` (gated 5.78) — superseded. Found STALE this session: it predates current code and flips 41% of games vs a fresh current-code OFF run (the aggregate drift is small and symmetric, +0.11, but the file no longer reproduces against current code). Kept on disk for history only; do NOT screen against it.**

**NEXT LEVERS (wave 253) — the frontier is now AI-play-quality + tempo, per the reframe (NOT the retracted off-table wall):** (1) **Secondary-victory-point over-scoring** — diag_signatures shows secondary VP 38.9 vs real 22.7, a default-mismatch between `secondaries.py:826` (off) and `simulator.py:2689` (on); the naive fix over-corrects to 11.6 and HURTS Astra Militarum, so it needs careful work to land ~22.7. Highest-value remaining signature gap. (2) **Going-first residual** — Challenger cards moved it but the sim is likely still above ~50%; re-measure the signature on the adopted frame, then look for the next tempo mechanism (NOT kiting/deployment/charge-timing — all three refuted at N=80). (3) **Grey Knights psychic / teleport-redeploy modelling** — a BUILD (adds mechanics, not a removal); lifts the Grey Knights under-side that the Fury gate revealed (the always-on re-roll was proxying these). (4) **Astra Militarum grounding decision** — `SWEG_AM_REALISM` + `SWEG_AM_RECON` are built + held default-off pending the user's call on whether Recon Element vs Grizzled Company is the faithful representation. (5) **Eval-integrity follow-ups:** root-cause the ~0.3 worktree-vs-main cross-directory discrepancy; add a standing "anchor reproduces against current code" check.**

---

**WAVE 249 (2026-06-13, CLOSED) — diagnostic-heavy wave on `claude/sim-calibration-13`: one win-condition lever built/screened/held, and a four-step diagnostic chain that ruled out four candidate mechanisms for the melee over-pole cluster (Death Guard 14.90, World Eaters 14.24, Emperor's Children 11.94, Chaos Daemons 11.19) and localized the residual to archetype-composition fidelity or the per-model representation limit. Standing anchor sc12a (gated 5.78) carries forward unchanged — no re-anchor.**

**Lever 1 — tabling decides on Victory Points not auto-win (`SWEG_TABLING_VP`) — FAITHFUL, near-inert, BUILT-AND-HELD default-off** (`ca3cba5`→`f59930f`). `_decide_winner` short-circuited on survivor count (a tabled opponent = automatic loss) before comparing Victory Points — but the run loop already plays out all five rounds on a one-sided tabling (cited `simulator.battle_length_five_rounds`), so the Victory-Point totals are complete at decision time and the auto-win contradicts that cited behaviour. Gate decides purely on Victory Points (verbatim Wahapedia matched-play, `data/rule_citations.d/core_win_condition.json`). N=40 paired vs `data/wf_w247_keeperset_n40.json` (6.86): 6.91 (+0.05 wash), only ~21/18,480 games flipped, melee cluster flat, two tiny away movers (World Eaters +0.36, Genestealer Cults +0.41). HYPOTHESIS REFUTED — the play-out-all-rounds fix already aligns Victory Points with the tabling outcome, so removing the auto-win flips only the rare tabled-but-ahead edge case. Faithful → kept; near-inert with a tiny adverse tilt → held off (not flipped off a 21-flip read with two wrong-direction movers); flagged for a free N=80 confirm at the next natural re-anchor.

**Melee over-pole — four mechanisms RULED OUT, residual localized.** (1) Durability mis-application REFUTED (`diag_durability`: the durable infantry class's effective save is explained by the armour-penetration faced; per-datasheet Feel No Pain is faithful). (2) Kill-efficiency REFUTED (`diag_overshooter`: Death Guard deals field-average damage, not an outlier). (3) Model-count list-inflation REFUTED by HARD-SOURCED lists — a first diagnostic ESTIMATED the archetype builds 2-3× real model count; a sourcing pass that fetched real May-2026 competitive lists (Best Coast Pairings via Grimhammer, with sources) found the OPPOSITE for the top two over-poles: World Eaters real ~67 vs sim ~61, Death Guard real ~48-58 vs sim ~39 (both UNDER), Emperor's Children ~46 vs ~54 (mild over), Chaos Daemons ~25-43 vs ~52 (over). Raising the seed fraction on the estimate would have cut World Eaters / Death Guard models — forbidden metric-tuning; the no-cited-anchor discipline caught it. (4) Order/stratagem throughput was ruled out in wave 248.

**Redirect (now cited): the real list-realism issue is archetype COMPOSITION / detachment, not model count** — metric-direction-NEUTRAL, genuinely faithful, but each a deliberate per-faction rebuild (held for careful handling, NOT a knob): World Eaters fields 500-pt Angron but three sourced lists are Khârn-anchored (130 pt) with no Angron (and field Bloodletters/Bloodcrushers/Chaos Terminators/Helbrute absent from real lists); Emperor's Children's "Slaaneshi Excess" detachment does not exist in 10e (real: Coterie of the Conceited / Court of the Phoenician / Peerless Bladesmen); Chaos Daemons runs four mono-god sub-archetypes but the meta is Be'lakor Scintillating Legion (Tzeentch). NOTE: the Emperor's Children archetype seeding Slaanesh daemon datasheets is FAITHFUL 10e codex design (native datasheets, as World Eaters gets Khorne daemons / Death Guard gets Nurglings) — a research over-claim refuted in code, NOT a bug.

**Method notes banked:** measure-don't-rule-read (an estimated real-list count overturned by actually sourcing the lists — cross-check a quantitative claim before acting); ground research-agent claims in code before action.

**Residual surface UNCHANGED from sc12a:** OVER — Death Guard 14.90, World Eaters 14.24, Emperor's Children 11.94, Chaos Daemons 11.19, Adeptus Custodes 9.89, Aeldari 7.52, Imperial Knights 6.36, Grey Knights 4.70, Necrons 4.19, Orks 3.70, Adeptus Mechanicus 3.67, Chaos Space Marines 2.33, Tyranids 0.93, Genestealer Cults 0.43. UNDER — Astra Militarum 19.89, Drukhari 4.59, Chaos Knights 4.34, Leagues of Votann 2.38. In band (4): Adeptus Astartes, T'au Empire, Thousand Sons, Adepta Sororitas.

**NEXT LEVERS (wave 250, on `claude/sim-calibration-13`; shared OFF anchor for N=40 screens = `data/wf_w247_keeperset_n40.json`, gated 6.86):** (1) **World Eaters Khârn-anchored composition correction — LIST-REALISM, now CITED, est. medium.** Three sourced competitive lists (Best Coast Pairings via Grimhammer, Sep/Oct/Dec 2025) are Khârn-anchored with no Angron; correct the `code/archetypes.py` "Berzerker Warband" template to match (drop Angron + the units absent from real lists). Metric-direction-neutral (Angron 500 → Khârn 130 frees points for more bodies — could go either way), genuinely faithful. FRAME-LEVEL change (archetype lists feed every eval) in the user-reserved list zone — handle deliberately, A/B before/after, and note this is the strongest-cited single case (start here, not the bigger Daemons/Emperor's Children detachment redesigns). (2) **Chaos Daemons Scintillating Legion archetype** — bigger reshape (mono-god → Be'lakor Tzeentch); needs its own sourcing + careful build. (3) **Emperor's Children detachment-name + composition** — verify the real detachment and reshape; mild model-count over. Carried: the wave-249 tabling-VP gate (N=80 confirm at next re-anchor) + the three wave-248 built-and-held Astra Militarum gates; Astra Militarum 19.89 is the per-model activation representation wall (structural, no order/list lever moves it); Imperial Knights 6.36 watch; `units.py:724` stale GATE-INERT comment (bundle with any units.py edit); Chaos Space Marines rolled-damage watch (low). (RETIRED wave 249: the `units.py:1568` extra-melee "lethal_hits" item is VERIFIED CORRECT — the wave-244 fix populates `melee_lethal_hits` at units.py:1591 and the melee resolution reads it under a `mode == "melee"` guard at units.py:3378-3379; chain complete end-to-end, no bug.) Architectural note: the melee over-pole may ultimately be the per-model representation limit — composition fidelity is the last faithful lever before that conclusion.

**The three levers (shared OFF anchor `data/wf_w247_keeperset_n40.json`, gated 6.86, config-equal to the wave-247 flipped defaults):** (1) **Ursula Creed "Lord Castellan" two-Order `SWEG_CREED_TWO_ORDERS` — FAITHFUL-BUT-INERT, built-and-held.** Cherry-pick `3031bfa`. BSData id `b6b2-9971-ec0c-349e` (led unit may receive two different Orders); adds the per-unit exception the no-stack guard lacked. Screen 6.86 (+0.00), ZERO flips all 22. Zero diagnosis: the greedy Order-allocation spreads Orders across squads and never reaches a second Order on one squad, so cap-of-two never bites. (2) **Reinforcements! stratagem dispatcher `SWEG_REINFORCEMENTS` — FAITHFUL-BUT-INERT, built-and-held.** Cherry-pick `5b312b5`. Registered-but-undispatched stratagem; adds `_try_reinforcements` at round end (restores a just-destroyed INFANTRY REGIMENT unit to Strategic Reserves at Starting Strength, verbatim Wahapedia, timing approximation documented). Screen 6.86 (+0.00), ZERO flips. Zero diagnosis: the trigger (dead INFANTRY REGIMENT non-CHARACTER + two command points + the sixty-point AI fire-gate + round-end timing) is never simultaneously met. (3) **Fix Bayonets! engagement routing `SWEG_FIXBAYONETS` — FAITHFUL-BUT-INERT, built-and-held.** Cherry-pick `fc9b026`→`9f1a989`. Piloting heuristic (cited as such, not a rules claim): the melee Order routes only to a squad within Engagement Range (`code.sim.geometry._er_gap`), else falls through. Screen 6.86 (+0.00), ZERO flips. Zero diagnosis: Astra Militarum archetype squads are all ranged, so the melee-Order scorer branch (`melee_dpa>0.5 and ranged_dpa<0.3`) never fires for any fielded squad — the guarded Order is never issued.

**Disposition + method.** None REJECTED (no decisive wrong-direction); none ADOPTED-with-a-flip (no measured effect to adopt) — all three are **built-and-held default-off**, ready to bite on a frame that exercises them, ledger-tracked for revisit at the next Astra Militarum representation / list-realism wave. Combined keeper-set screen SKIPPED as provably redundant (three gates each flipping 0/18,480 compose to zero — the flip-count-forensics byte-identity proof; the combined arm is byte-identical to the OFF anchor already in hand). N=80 re-anchor SKIPPED for the same reason (zero flips at N=40 → frame unchanged → sc12a stands). Low-priority caveat logged: an N=80-only seed could in principle reach a trigger the N=40 screens did not — to be confirmed for free at the next effectful wave's natural re-anchor. **Meta-finding: Astra Militarum order/stratagem fidelity is exhausted as a lever family (three independent levers across Order-cap, stratagem-dispatch, Order-routing all byte-inert); the 19.89 under-pole is per-model activation representation, not orders.**

**Residual surface UNCHANGED from sc12a (no re-anchor):** OVER — Death Guard 14.90, World Eaters 14.24, Emperor's Children 11.94, Chaos Daemons 11.19, Adeptus Custodes 9.89, Aeldari 7.52, Imperial Knights 6.36, Grey Knights 4.70, Necrons 4.19, Orks 3.70, Adeptus Mechanicus 3.67, Chaos Space Marines 2.33, Tyranids 0.93, Genestealer Cults 0.43. UNDER — Astra Militarum 19.89, Drukhari 4.59, Chaos Knights 4.34, Leagues of Votann 2.38. In band (4): Adeptus Astartes, T'au Empire, Thousand Sons, Adepta Sororitas.

**NEXT LEVERS (wave 249, on `claude/sim-calibration-13`; shared OFF anchor for forty-battle screens = reuse `data/wf_w247_keeperset_n40.json`, gated 6.86 — config-equal to current defaults; the wave-248 inert gates do not perturb it):** the wave-248 negative result pivots the focus OFF Astra Militarum order throughput and ONTO the melee over-pole cluster (Death Guard 14.90 top, World Eaters 14.24, Emperor's Children 11.94, Chaos Daemons 11.19). (1) **Death Guard kill-chain direction diagnostic — DIAGNOSTIC-FIRST, near-zero eval.** The wave-248 Death Guard diagnostic already ruled the 14.90 over-pole DOMINANTLY PRE-EXISTING kill-centric melee over-reward (the sc11b figure was already 11.87) and FALSIFIED the overkill-waste-under-rolled-damage hypothesis (World Eaters/Emperor's Children/Daemons carry far more variable-damage weapons than Death Guard's one, yet stayed flat/toward at N=80). Next step it recommended: run `scripts/diag_durability.py` + `scripts/diag_overshooter.py` scoped to Death Guard against sc12a to separate "dies too slowly" (defensive-layer over-value) from "kills too efficiently" (melee-kill over-reward) — this picks which downstream lever is even addressable (soft-VP / overextension-punishment AI is actionable; defensive-stat re-tune is forbidden metric-tuning). (2) **Cluster-shared screening-gap / overextension-punishment (wave-246 Fall Back deferred angles) — the four-faction melee cluster shares the kill-centric-no-soft-VP root, so a single positional/scoring lever could move all four.** Diagnose the shared mechanism before any build. (3) **Astra Militarum per-model activation representation — STRUCTURAL, the real 19.89 driver** (confirmed again by wave 248): hordes over-rated relative to Astra Militarum's tank-heavy / elite-infantry silhouette. Harder, structural; scope a diagnostic before committing. Carried: the three wave-248 built-and-held gates (Creed / Reinforcements / Fix Bayonets — revisit on an exercising frame); Imperial Knights 6.36 (improving, watch); `units.py:1568` extra-melee "lethal_hits" flag investigation; `units.py:724` stale GATE-INERT comment (trivial, bundle with any units.py edit); Wyvern not-in-archetype indirect-fire note; structural mapper per-profile flag retention (mapper phase); Chaos Space Marines rolled-damage watch (low priority, largely dissolved at N=80).

---

**WAVE 247 (2026-06-13, CLOSED) — third batch-screen wave, first on `claude/sim-calibration-12`: ALL THREE levers ADOPTED — rolling damage default-flipped after sitting dormant since wave 100 (fidelity-first), Born Soldiers REGIMENT keyword gate, Cadian Castellan Senior Officer sustained-hits grant. Re-anchor metric-neutral at eighty battles; in-band count 3 → 4.**

**The three levers (shared OFF anchor `data/wf_w246_keeperset_fixed_n40.json`, gated 6.78):** (1) **Born Soldiers REGIMENT gate `SWEG_BORN_REGIMENT` — WASH-KEEP.** Lethal Hits leg gates on codex-literal REGIMENT (31 datasheets) instead of the legacy BATTLELINE proxy (3). Screen 6.75 (−0.03), all flat; Astra Militarum +0.31 ± 1.91 on 55 flips. (2) **Castellan [SUSTAINED HITS 1] `SWEG_CASTELLAN_SH` — WASH-KEEP.** BSData ability id `a7f5-adb8-d1c9-2a2d`; the registry's "no such field" comment was stale. Screen 6.75 (−0.03), all flat; Astra Militarum +0.33 ± 0.36 on six flips. (3) **Rolling damage flip `SWEG_ROLLDMG` — KEEP per fidelity-first.** The user-approved wave-100 build (real per-shot Damage dice on the per-model firing path) had never been default-flipped — every prior production anchor was an expected-value-damage frame. Screen 6.92 (+0.14 wash); sole decisive Chaos Space Marines +5.84 ± 4.30 away — not the embark-class decisive rejection. Combined keeper set 6.86 (+0.08, exact sum of parts) PASSED; defaults flipped in `67d124a`, suite 1674 green.

**Diagnostic adjudication (method note banked):** a deep-dive agent claimed the rolling-damage gate "functionally inert" (empty top-level `damage_dice` + a stale GATE-INERT comment at `code/units.py:724`) and the Chaos Space Marines move a false positive. REFUTED by flip-count forensics — an inert gate gives ZERO flipped games in the deterministic paired join; this screen flipped ~200 games per faction (near-inert Born/Castellan showed 0–7). Coverage verified post-flip: per-model promotion defaults ON (`code/army.py:701`), 1384/1384 catalogue entries carry loadout damage-dice strings, 499 units have a variable-damage weapon — no non-promoted firing path exists in production. Salvaged: Chaos Space Marines has the field's highest dice-damage-weapon density (17.4 percent of loadout slots) — the faithful explanation for its screen response. Stale `units.py:724` comment queued as a trivial fix.

**Re-anchor (N=80, flipped frame): gated 5.78, raw 9.14, 4/22 in band — `data/_anchor_sc12a_n80_log.json` promoted.** Paired vs sc11b (36,960 matched games): ALL twenty-two factions flat — metric-neutral; the Chaos Space Marines watch item largely dissolves (+1.38 ± 3.18 at eighty vs +5.84 ± 4.30 at forty, an edge-of-interval read regressing to the mean; watch stays open at low priority). Non-decisive toward: World Eaters −3.01, Imperial Knights −2.75, Emperor's Children −2.27. Non-decisive away: Death Guard +3.03 — the new top over-pole. Adepta Sororitas enters the noise band.

**Residual surface on the new anchor (sim vs real, gated points):** OVER — Death Guard 14.90, World Eaters 14.24, Emperor's Children 11.94, Chaos Daemons 11.19, Adeptus Custodes 9.89, Aeldari 7.52, Imperial Knights 6.36, Grey Knights 4.70, Necrons 4.19, Orks 3.70, Adeptus Mechanicus 3.67, Chaos Space Marines 2.33, Tyranids 0.93, Genestealer Cults 0.43. UNDER — Astra Militarum 19.89, Drukhari 4.59, Chaos Knights 4.34, Leagues of Votann 2.38. In band (4): Adeptus Astartes, T'au Empire, Thousand Sons, Adepta Sororitas.

**NEXT LEVERS (wave 248, on `claude/sim-calibration-12`; shared OFF anchor for the forty-battle screens = a fresh N=40 run of the plain flipped frame, or reuse the keeper-set arm `data/wf_w247_keeperset_n40.json`, gated 6.86 — config-equal to the new defaults):** (1) **Ursula Creed double-Order (`code/orders.py` dispatch ~677; gap acknowledged at `code/leaders.py:1453`) — FAITHFULNESS, est. medium.** Lord Castellan issues two Orders per phase; the dispatch loop grants one. Astra Militarum is still the dominant under-pole (19.89) and orders throughput is its known value chain. (2) **Fix Bayonets! order routing (`code/orders.py:511`) — PILOTING, est. low-medium.** The melee order is issued by the same scorer as ranged orders; verify it only fires when the squad is actually engaged. (3) **Reinforcements! stratagem no-op — DIAGNOSTIC-FIRST.** Re-locate the live dispatch site before any build (the prior diagnostic's line number is implausible); confirm whether the stratagem is reachable at all in production evals. Melee over-pole cluster (Death Guard 14.90 now top, World Eaters 14.24, Emperor's Children 11.94, Daemons 11.19): the named deferred angles remain screening-gap and overextension-punishment (wave-246 Fall Back diagnostic); Death Guard's away-drift under rolled damage (+3.03 non-decisive) suggests durable multi-wound infantry under-dies less than expected-value assumed — a candidate diagnostic before any build. Carried: Imperial Knights 6.36 (improving, watch), `units.py:1568` extra-melee hot-swap "lethal_hits" flag investigation, `units.py:724` stale GATE-INERT comment fix (trivial, bundle with any units.py edit), Wyvern not-in-archetype indirect-fire note, structural mapper per-profile flag retention (mapper phase), Chaos Space Marines rolled-damage watch (low priority). Architectural note: none of the Astra Militarum levers alone closes its twenty points; the dominant driver remains per-model activation representation favouring hordes.

---

*Wave history older than wave 247 archived to `docs/CURRENT_STATE_archive.md`.*
*Long memory of what is landed / reverted / parked / forbidden: `docs/DECISION_LEDGER.md` — read it before proposing a lever.*

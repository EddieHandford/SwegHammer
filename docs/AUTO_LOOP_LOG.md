# Auto-loop log (recent)

Older iter blocks live in `AUTO_LOOP_LOG_archive.md`. Per
`AUTO_LOOP_PROCEDURE.md` §E this file keeps the most recent close + the
in-flight wave only.

## Wave 248 (2026-06-13, CLOSED) — fourth batch-screen wave, first on `claude/sim-calibration-13`: three faithful Astra Militarum order/stratagem levers built, cited, tested — and ALL THREE measured BYTE-INERT on the current archetype frame (zero flipped games, every faction, every screen). The wave's real product is a negative result: the dominant Astra Militarum under-pole (gated 19.89) is NOT an order/stratagem-throughput problem; the eval artificial-intelligence never reaches those decision points. The standing anchor `data/_anchor_sc12a_n80_log.json` (gated 5.78) carries forward unchanged — three byte-inert flips do not move the frame, so no re-anchor was run (no-redundant-sim-runs).

**Shared OFF anchor for the forty-battle screens: `data/wf_w247_keeperset_n40.json` (gated 6.86)** — config-equal to the wave-247 flipped production defaults, reused at zero eval cost (the anchor-promotion pattern). Three single-gate ON arms screened serially against it; each joined with `scripts.paired_delta` (18,480 matched games per join).

**1. Lever 1 — Ursula Creed "Lord Castellan" two-Order exception (`SWEG_CREED_TWO_ORDERS`): FAITHFUL-BUT-INERT, built-and-held.** Cherry-picked `3031bfa`. BSData unit id `b6b2-9971-ec0c-349e`: "While this model is leading a unit, that unit can be affected by up to two different Orders at the same time." The Order dispatcher's no-stack guard (`code/orders.py`) previously had no per-unit exception; the build adds a Creed-led-squad resolution (host keys `astra_militarum_cadian_shock_troops` / `astra_militarum_kasrkin`, both fielded) and a cap of two distinct Orders for that one squad. Screen: gated 6.86 (+0.00), ZERO flipped games across all twenty-two factions. Diagnosis of the zero: the greedy Order-allocation policy spreads Orders across distinct squads and never reaches a second Order on any single squad, so the cap-of-two is never exercised. The mechanic is correct and cited; it will bite only once the allocation policy can double-Order — which ties to the same Astra Militarum throughput chain the representation limit blocks.

**2. Lever 2 — Reinforcements! stratagem dispatcher (`SWEG_REINFORCEMENTS`): FAITHFUL-BUT-INERT, built-and-held.** Cherry-picked `5b312b5`. The stratagem was registered but had no dispatcher (unreachable in production); the build adds `_try_reinforcements` firing at round end alongside the Cult Ambush resurgence pass, restoring a just-destroyed INFANTRY REGIMENT unit into Strategic Reserves at Starting Strength (verbatim Wahapedia rule, round-end timing approximation documented in code and citation). Screen: gated 6.86 (+0.00), ZERO flips. Diagnosis: REGIMENT is an Astra-Militarum-only keyword, and the combined trigger (a just-destroyed INFANTRY REGIMENT non-CHARACTER unit, two command points available, the AI fire-gate at sixty points, and the round-end timing window) is never simultaneously met by the eval artificial-intelligence in any archetype game.

**3. Lever 3 — Fix Bayonets! engagement-range routing guard (`SWEG_FIXBAYONETS`): FAITHFUL-BUT-INERT, built-and-held.** Cherry-picked `fc9b026` → `9f1a989`. A piloting heuristic (correctly cited as an artificial-intelligence heuristic, not a tenth-edition rules claim): the melee Order is only routed to a squad within Engagement Range, reusing the existing `code.sim.geometry._er_gap` check; an unengaged melee-statted squad falls through to its next-best Order instead of wasting Fix Bayonets!. Screen: gated 6.86 (+0.00), ZERO flips. Diagnosis: Astra Militarum archetype squads are overwhelmingly ranged, so the melee-Order scorer branch (`melee_dpa > 0.5 and ranged_dpa < 0.3`) is never satisfied for any fielded squad — the Order it guards is never issued in the first place, so the guard has nothing to reroute.

**4. Disposition and close.** All three are faithful (two codex rules, one correctness-improving piloting heuristic), cited, and unit-tested on both gate arms (49 targeted tests green at the Fix Bayonets cherry-pick; audit clean 348/348; demonstration battle clean). None is REJECTED — a rejection is a decisive wrong-direction screen, and these are byte-zero, not wrong. None is ADOPTED-with-a-default-flip either — a default flip should follow a measured effect, and the screens demonstrate none. They are therefore **built-and-held default-off**, ready to bite on a frame that exercises them, and tracked in the decision ledger for revisit at the next Astra Militarum representation or list-realism wave. The combined keeper-set screen was skipped as provably redundant: three gates each flipping zero of 18,480 games compose to zero flips (the flip-count-forensics byte-identity proof), so the combined arm is byte-identical to the OFF anchor — already in hand. The N=80 re-anchor was skipped for the same reason: zero flips at forty battles means the production frame is unchanged, and `data/_anchor_sc12a_n80_log.json` (gated 5.78) remains the standing anchor. A low-priority caveat is logged — an N=80-only seed could in principle reach a trigger the N=40 screens did not — to be confirmed for free at the next effectful wave's natural re-anchor.

**Meta-finding (the wave's product).** Astra Militarum order/stratagem fidelity is exhausted as a lever family: three independent levers across the Order-cap, stratagem-dispatch, and Order-routing layers all measure byte-inert. The gated 19.89 under-pole is driven by per-model activation representation (hordes over-rated relative to Astra Militarum's tank-heavy / elite-infantry silhouette), not by any missing or mis-routed Order or stratagem. Next levers pivot away from Astra Militarum order throughput to the melee over-pole cluster (Death Guard 14.90 now top, World Eaters 14.24, Emperor's Children 11.94, Chaos Daemons 11.19) — for which the Death Guard away-drift diagnostic already returned a verdict (the over-pole is dominantly pre-existing kill-centric melee over-reward; the overkill-waste-under-rolled-damage hypothesis is falsified) — and to the per-model representation question for Astra Militarum.

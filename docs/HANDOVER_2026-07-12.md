# Handover — end of the correction weekend (2026-07-12)

Written for the next working session. Read `docs/CURRENT_STATE.md`'s top banner
for the narrative; this file is the operational state and the outstanding-items
register. The standing protocols (`docs/EVAL_PROTOCOL.md`,
`docs/LEVER_PROTOCOL.md`, `docs/PILOT_PROTOCOL.md`) all apply unchanged.

## Standing state

- **Anchor: `data/_anchor_sc63a_n80_log.json`** (ships in-repo), gated mean
  absolute error 2.46 (army-A frame) / 2.43 (symmetrized) — best army-A frame
  in project history, frames nearly agreeing. The anchor log carries margins
  (victory points and points destroyed per game) — a free 36,960-game dataset
  for any exchange-rate refit.
- **Branch:** `claude/sim-calibration-19` is the rolling branch;
  `claude/sim-calibration-20` is fast-forwarded to the same head and is the
  branch behind **pull request 90** (open, awaiting the co-owner's review —
  the single comprehensive sync). After it merges, start
  `claude/sim-calibration-21` fresh per the checkpoint convention.
- **Adopted this weekend** (all default-on, `=0` kill-switches restore the
  sc62a frame, digest-verified): `SWEG_STRAT_ONCE_PER_PHASE`,
  `SWEG_WARGEAR_MUTEX`, `SWEG_COVER_ANGLE`.
- **Research branch:** `claude/layer-stack-research` (pushed) holds the full
  layer stack — calibrated threat field (allocation × measured attack
  propensity × line of sight), value field, offense term, job channels,
  denial channel, reactive hold, the shoot-value consumer, and the canary
  tooling — all default-off, all instrumented, none adopted.

## Residual ladder at sc63a

Astra Militarum (~28 points under — half the remaining error, see item 1),
Death Guard (+12 to +13), Aeldari (+5 to +6), everything else thin
(10-11 of 22 factions inside their noise bands per seed-window half).

## Outstanding items

1. **The Astra Militarum durability/economy question — the Stage-1 boundary.**
   The canary program's terminal finding: with movement, charging, shooting,
   and garrisons made provably honest, and the kill arithmetic hand-audited
   faithful end-to-end, Astra Militarum still loses the Death Guard cell
   roughly 47-to-25 on primary. The residual is not piloting and not
   per-activation math. Candidate directions, none yet registered: the
   real-world aggregate rates may encode matchup or meta effects a closed
   round-robin cannot reproduce (there is no pairwise ground truth); the
   mission or secondary economy; an Astra Militarum orders/stratagem coverage
   audit; a second list-realism pass. Every instrument needed is built
   (`scripts/diag_canary_am_dg.py`, the victory-point ledger, the margins).
2. **Wargear-mutex follow-up lane** (registered in the ledger): 129 further
   units carry illegal simultaneous wargear; 107 of them have their
   mapper-picked primary weapon inside the optional set, so the correction
   needs a per-unit curated policy, not a blanket drop. The audit tooling and
   the drop-table pattern are in `code/units.py` (`_WARGEAR_MUTEX_DROPS`).
3. **The swarm-concentration allocation structure** (named, not registered):
   the calibrated threat field's remaining over-prediction sits on swarm
   defenders (Tyranids, Genestealer Cults) because smooth allocation spreads
   an enemy's output across many bodies while real fire concentrates to
   delete whole units. A new registered structure if funded; the calibration
   instrument (`scripts/diag_threat_calibration.py`) is the falsifier.
4. **The World Eaters calibrated-by-bug question:** `_screen_target_bonus`
   (code/strategy.py:693, unconditional and ungated) mislabels Khorne
   Berzerkers as chaff-screens, so every opponent over-prioritises shooting
   them by 1.4×. Tightening the tag to require the horde role is a small
   provable change, but it alters committed defaults and the anchor's in-band
   World Eaters reading may depend on the bug — gate, screen, and re-examine
   the pole together. This also partially explains why the shoot-value
   consumer "broke" World Eaters (it removed the compensating error), so
   re-screening `SWEG_SHOOT_VALUE` after this fix is the consumer's reopen
   path (it parked held at +0.07 with three poles moving toward real).
5. **Held gates awaiting a future fold-in:** `SWEG_PRIMARY_DECK_FULL`,
   `SWEG_TRANSPORT_PLAY` (both held-fidelity, near-flat);
   `SWEG_EC_LIST3` (likely obsolete after the real-squads change — verify,
   then retire). The five-faction value-move mask (1.95/1.79 on the sc62a
   frame) is parked per the owner and would need re-screening on sc63a if
   ever revisited.
6. **Waaagh advance-and-charge is unmodeled in resolution** (found during the
   offense-term build; the offense pricing correctly refuses to price it).
   A genuine rules-fidelity candidate — but Orks currently sit inside their
   noise band, so screen carefully and expect the fidelity-first ruling to
   govern.
7. **Catalogue hygiene** (from the kill-arithmetic audit, both inert in live
   play under the per-model path): Mortarion's flattened top-level profile
   has zero attacks; Plague Marines' aggregate anti-keywords leak the Plague
   Spewer's keyword onto the wrong weapon; the `receive_damage` docstring
   still claims Feel No Pain values the overrides removed.
8. **Stale documents corrected by ledger, not yet annotated in place:**
   `docs/TERRAIN_REALISM_PROPOSAL.md` and the caveat section of
   `docs/THREAT_LAYER_PROPOSAL.md` still assert the simulator has no
   line-of-sight blocking — false since May (see the ⛔ ground-truth
   correction, 2026-07-12). `docs/TERRAIN_LOS_SPEC.md` supersedes them; a
   cleanup pass should add banner notes to the stale files.
9. **The Chaos Knights inconclusive probe:** the shoot-value instrument's
   Chaos Knights cell read opposite-signed to the aggregate and was honestly
   left unresolved; needs a properly-harnessed follow-up if the shoot-value
   reopen is pursued.
10. **Distributed screening with the co-owner:** the job-ticket harness
    (`scripts/screen_job.py`, `docs/DISTRIBUTED_SCREENING.md`) is built and
    the anchor ships in-repo, but the onboarding was never sent.

## Operational notes (hard-won this weekend)

- **Long anchor evaluations do not survive as the orchestrator's background
  shell commands** — they were killed externally twice. Run them inside an
  agent that launches the evaluation and waits in-turn with a bounded loop,
  split into two 40-seed halves merged with uniqueness assertions (the
  pattern is in the ledger and worked first time).
- The eval lock's stale-holder cleanup works, but after an external kill the
  lock file may need manual removal (verify no python evaluation processes
  first — the owner's other applications also run python).
- The digest matrix (every gate arm, before/after) has caught both spec
  omissions and integration errors that review missed — keep demanding it of
  every build agent.
- The owner keeps 25 percent of the machine (twelve workers) when present;
  the full box is only available when explicitly granted.
